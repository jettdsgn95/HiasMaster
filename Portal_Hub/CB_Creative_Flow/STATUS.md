# STATUS.md — Module Progress Tracker

> Tracker tiến độ CB Media Hub. Cập nhật sau mỗi task có thay đổi module/file/progress, hoặc khi user gõ `check_update`.
>
> *Last updated: 2026-07-20 · Project state: **UAT prep — data reset kit sẵn sàng** · **🆔 REFACTOR PIC → user_id — STAGE 1 (CONTENT) DONE 2026-07-20** (`app.js`+`content-team.js`+`content-workbench.js`+`supabase/add-pic-user-id.sql`): PIC lưu bằng chuỗi TÊN toàn hệ thống → đổi tên sinh trùng cũ/mới + RLS content_tasks (khóa theo tên) khiến PIC đổi tên mất quyền sửa task cũ. Chuyển sang **user_id nguồn sự thật (dual-write, giữ cột tên làm snapshot; hiển thị resolve id→tên hiện tại → đổi tên phản ánh tức thì)**. Migration additive `add-pic-user-id.sql`: thêm `*_user_id` (orders 5/tasks 3/content_tasks 3) + backfill từ tên + đổi 2 RLS content_tasks sang `auth.uid()` + orphan report. app.js helper `MH.setUserDir`/`userName`/`picLabel`/`picOptionsById` + sửa `enhancePicSelects` đọc `data-name` (value=id không lộ UUID). **Stage 1 chỉ content_tasks**: 3 select Content value=id, saves ghi id+snapshot, notify theo id, hiển thị qua `ctPicName`, bỏ seed cứng + bỏ gộp tên lịch sử (nguồn ghost/dupe); content-workbench `isMineTask` theo id + self-create set id. Verified CDP rename-proof + save id + 0 lỗi. **⚠ chạy `add-pic-user-id.sql`. CÒN Stage 2 (orders: ct-pic/ads/Media PIC + sửa RPC update_brief_wording) + Stage 3 (tasks production-board)** — các select đó tạm giữ value=tên (pool đã dọn). · **📥 FIX LEAD KHÔNG THẤY ĐƠN TRONG CONTENT INBOX DONE 2026-07-20** (`content-team.js` + ⚠ migration DB): Account chuyển Content Wording, Lead nhận noti nhưng Inbox rỗng. Root cause = READ-side RLS: `lead_content` không thuộc `is_staff()`, đọc orders chỉ qua policy `orders lead_content read` (add-content-team.sql B4); thiếu policy → noti vẫn bắn (chạy phiên Account) nhưng Lead `orders.list()` bị RLS lọc sạch trả `[]` im lặng → Inbox rỗng (cùng class `orders content read` vừa fix). Fix chính = chạy `add-content-team.sql` (hoặc tạo policy). Fix code: `loadOrders` set `ordersReadBlocked` (chỉ role lead_content thật + supabase on + 0 đơn), `renderInbox` đổi empty-state "Yêu cầu mới" thành cảnh báo nêu file SQL + role check; KHÔNG toast (tránh false-positive hệ thống mới). Parity SSO/@cb.vn đã sẵn (RLS qua current_user_role đọc users.role). Verified CDP 0 JS errors. · **🧩 BATCH FIX LUỒNG CONTENT TEAM + MÃ TASK DONE 2026-07-20** (`content-team.html/js` + `content-workbench.html/js` + `data-store.js` + `supabase/add-content-task-code.sql`): đóng 5 lỗ hổng phát hiện khi audit UAT. **(1)** Content Inbox nay có panel thứ 3 **"Task nội bộ — chờ Lead duyệt"** (đọc `content_tasks` status `submitted_to_lead`, sort theo hạn duyệt, chip ⚠ quá hạn) + badge tab Inbox cộng cả task nội bộ — trước đây Inbox/Board/Danh sách đều build từ `orders` nên task Content gửi duyệt KHÔNG BAO GIỜ hiện, Lead tưởng mất task. **(2)** Deep-link `?task=`/`?id=` nay **chuyển đúng tab** trước khi mở drawer (task chờ duyệt→Inbox, còn lại→Task nội bộ; order engaged→Inbox) + toast danger khi không tìm thấy thay vì im lặng. **(3)** Đổi PIC nay **notify 2 chiều** (PIC mới "được giao lại" + PIC cũ "task đã chuyển, không còn quyền sửa") — trước chỉ notify khi PIC đang trống nên reassign = 0 thông báo (verified CDP). **(4)** `saveTaskAssign` thêm try/catch + verify row trả về + toast lỗi rõ (trước không có try/catch → update throw là nút "chết" im lặng, không đổi gì không báo gì) + **confirm** khi đổi PIC lúc task đang chạy (cảnh báo PIC cũ mất quyền sửa ngay do RLS match theo tên) + activity `Lead chuyển PIC: A → B`. **(5)** `notifyLeadTask` chuyển sang RPC `notify_roles`, trả boolean → PIC gửi duyệt mà báo Lead thất bại thì thấy toast warning thay vì tưởng đã xong. **🔖 Mã task `CT-YYYY-NNN`**: cột `content_tasks.task_code` sinh bởi SEQUENCE + trigger DB (không sinh ở frontend vì task tạo ở 4 chỗ + tránh đua), backfill theo created_at, unique index; hiển thị ở cột "Mã" đầu bảng 2 trang, eyebrow drawer, dòng Thông tin task, mọi notification, và tìm kiếm được theo mã; fallback `CT-<4 ký tự cuối id>` khi chưa migrate. ⚠ chạy `add-content-task-code.sql`. Verified CDP 4 kịch bản, 0 JS errors. · **🏷️ ĐỔI NHÃN "INITIATIVE" → "TASK NỘI BỘ" DONE 2026-07-20** (`content-team.html`+`content-team.js`+`content-workbench.js`; KHÔNG cần SQL): nhân sự không nhận ra "Initiative" = task nội bộ team Content ⇒ đổi 11 chuỗi hiển thị (tab · h3 + mô tả · nút tạo · info · empty-state · KPI · modal · toast · activity · notif title · `SOURCE_LABEL.content_initiated` 'Chủ động'→'Task nội bộ' ở CẢ 2 file). GIỮ NGUYÊN key nội bộ: `data-ctm-view="initiatives"`, id DOM, tên hàm, và giá trị DB `source='content_initiated'` → data cũ không đổi. Verified CDP headless role lead_content, 0 JS errors. **⚠ Kèm phát hiện 2 lỗ hổng luồng (CHƯA fix, chờ kết quả SQL)**: (1) **Content Inbox / Board / Danh sách / Việc của tôi đều build từ `orders`** (`byStatus` trên `ORDERS` lọc `brief_wording_status`) nên `content_tasks.status='submitted_to_lead'` **KHÔNG BAO GIỜ vào Content Inbox** — task Content tự đề xuất chỉ nằm ở tab "Task nội bộ" ⇒ Lead nhìn Inbox thấy 0, tưởng mất task (tái hiện được bằng CDP: inboxCount=0 / initCount=2); (2) **deep-link `?task=` và `?id=` KHÔNG chuyển tab** (khác `?plan=` có chuyển) → drawer mở đè lên Dashboard, đóng ra là Dashboard trống; cộng thêm `notifyLeadTask` (content-workbench.js) insert noti trực tiếp + `catch→console.warn` nuốt lỗi, chưa dùng RPC `notify_roles`. · **🔓 FIX NÚT "GỬI LEAD CONTENT DUYỆT" MÙ LÝ DO DONE 2026-07-20** (`content-workbench.js`; KHÔNG cần SQL): bug UAT — PIC content tick đủ 9 Quality Checklist + set SLA nhưng nút "Gửi Lead Content duyệt" vẫn xám và không báo thiếu gì. Root cause KHÔNG phải RLS: gate đòi đủ 9/9 checklist VÀ ≥1 ô nội dung, mà "nội dung" chỉ đếm 8 cột (`CT_BODY_FIELDS`: draft_body/caption/headline/script/voice_over/ads_primary_text/landing_copy/email_sms_zalo_copy) — task `social_post` render 4 ô nhưng `draft_title`/`cta`/`mandatory_info` KHÔNG tính ⇒ điền đủ 3 ô đó mà bỏ trống Body thì khóa vĩnh viễn. Fix = hàm `submitBlockers()` liệt kê lý do cụ thể (n/9 + tên từng mục checklist thiếu; tên ô nội dung hợp lệ theo `output_types`) → `refreshTaskSubmit` set disabled + tooltip `btn.title` + hint đỏ `#wt-submit-hint` dưới cụm nút; toast trong `submitTaskToLead` dùng cùng blockers. Gate nghiệp vụ GIỮ NGUYÊN (vẫn bắt có body thật). Verified CDP headless 5 bước (A khóa nêu đủ 2 lý do → B tick đủ chỉ còn lý do nội dung → C nhập Body ENABLED hint ẩn → D xóa Body khóa lại → E chỉ CTA vẫn khóa), 0 JS errors. · **⏳ P0 DEADLINE FLOW CLIENT↔ACCOUNT DONE 2026-07-15** (`database-orders.js`+`client-dashboard.js`+`calendar.js`+`data-store.js`+`supabase/add-agreed-deadline-flow.sql`): 3 lớp deadline (requested gốc KHÔNG ghi đè / agreed thống nhất / internal nội bộ không lộ client); effective = agreed‖requested áp bảng orders + drawer tile + Client Portal + Calendar; drawer block "Deadline thương lượng với Client" (admin/account/lead_media đề xuất deadline + lý do, validate, ghi DB trước verify sau mới notify); Client Action Center card + drawer panel Đồng ý / Cần trao đổi lại (note bắt buộc) qua RPC `respond_deadline_proposal` (client không có UPDATE orders dưới RLS); noti 2 chiều type mới deadline_proposed/accepted/rejected (+deadline_updated dự phòng); deadline_history jsonb audit đủ vòng; cảnh báo internal_deadline trễ hơn deadline Client (box + toast, không block); data-store allowlist 10 cột mới. ⚠ chạy `add-agreed-deadline-flow.sql`. · **📊 DASHBOARD WORKLOAD DYNAMIC DONE 2026-07-15** (`dashboard.html`+`task-dashboard.html`): bỏ toàn bộ 6 PIC hardcode ở Team Workload (Master) + Workload by team member (Task Dashboard) → render động từ users thật (role design/editor/account/lead_media/admin, active) + tasks.assigned_to (nhân sự cũ còn task vẫn hiện); user mới 0/max 8; filter PIC dashboard build động (`#filter-pic`, "Tên · Role"); count "Hiển thị N orders" thật; escapeHtml; users fail → fallback tasks; CSV export tương thích. Đóng nốt ⚠ "còn hardcode workload bars + filter-pic" từ 2026-07-13. KHÔNG cần SQL. · **🧹 UAT RESET + TEST KIT DONE 2026-07-15** (`supabase/clear-demo.sql` v2 + `supabase/seed-user-role-map.sql` + folder `UAT/`): chuẩn bị đợt test nhân sự công ty (Google SSO @cbcentres.com). **clear-demo v2**: bản v1 (Phase 1) chỉ xóa 9 bảng gốc → v2 xóa đủ **17 bảng** (thêm `notifications`/`lead_tasks`/`lead_task_comments`/`content_plans`/`content_tasks`/`content_task_comments`/`brand_checks`/`brand_check_criteria`) qua DO block guard `to_regclass` (bảng chưa migrate → skip, không chết script) + xóa file **4 Storage buckets** (`brief-files`/`deliverables`/`plan-files`/`brand-check-images`; GIỮ `avatars`); GIỮ `users` (5 demo @cb.vn làm fallback + client test) + `user_role_map` + `settings` + schema/RLS/buckets; verify counts cuối file. **seed-user-role-map.sql**: template upsert roster (email công ty + role + TÊN khớp assigned_pic — data keyed theo tên) + block SYNC `public.users` cho ai lỡ SSO trước khi seed (trigger chỉ chạy lần đầu → vào trước = dính role client) + verify LEFT JOIN ai chưa SSO. **UAT/UAT_TestPlan.csv**: 78 case UTF-8 BOM import Google Sheets, 7 nhóm — F0 Login/SSO (12) · F1 Media Order end-to-end (26) · F2 Ads (8) · F3 Content nội bộ (7) · F4 Strategy Board (5) · F5 Brand Safety (6) · F6 Phân quyền + Cross (14); cột Kết quả Pass/Fail/Blocked + mức độ Critical/Major/Minor + ảnh + người test. **UAT/UAT_HuongDan.md**: pre-flight admin (check migration pending qua `pg_proc`/`to_regclass`, SSO provider, seed roster, chạy clear-demo, verify empty-state) + nhân sự clear site data 1 lần (localStorage demo sót gây data ma) + kế hoạch 3 vòng (smoke → kịch bản phân vai đúng role thật → free test phá) + triage cuối ngày + phân vai 6 người. **UAT/uat-guide.html** (2026-07-15): bản HTML trực quan của hướng dẫn cho nhân sự — self-contained (brand navy/đỏ, inline SVG, sticky nav 7 mục, hero 4 facts, steps/timeline/role cards/flow groups, admin pre-flight thu gọn `<details>`, responsive + focus states); sửa nội dung hướng dẫn → sync cả .md lẫn .html. ⚠ SQL anh chạy tay trên Supabase Dashboard (thứ tự: migration pending → seed-user-role-map → clear-demo). KHÔNG đụng code app. · **🔽 PIC DROPDOWN = USERS THẬT + NHÃN ROLE DONE 2026-07-13** (`database-orders.js`+`production-board.js`): dropdown gán PIC (Order drawer: Account PIC / Production PIC / PIC Quay / PIC Chụp; Task Tracker: drawer `#edit-pic` + modal `#tm-pic`) hardcode 4 tên seed → user mới từ User Management không hiện. Fix: load `users.list()` thật (bỏ inactive), Production PIC = role design/editor, Account PIC = admin/account/lead_media, option text "Tên · Role" (value vẫn = tên, data không đổi); fallback seed chỉ khi Supabase off; giữ PIC cũ không còn trong pool; filter "Mọi PIC" 2 trang append tên thật; @mention TEAM_MEMBERS append users thật. + UI custom dropdown: role = badge pill navy cạnh tên (`app.js enhancePicSelects` wrap `select[data-pic-dd]` — select gốc ẩn giữ logic `.value`/disabled; option `data-role`; CSS `.pic-dd*`/`.pic-role-badge`; trigger là div để applyDrawerReadonly không ẩn mất field; áp 4 select Order drawer + `#edit-pic` + `#tm-pic`). KHÔNG cần SQL. ⚠ Còn hardcode: workload bars + filter-pic ở dashboard.html/task-dashboard.html. · **🔔 FIX NOTI CLIENT→STAFF DONE 2026-07-13** (`order-form.js`+`ads-order-form.js`+`client-dashboard.js`+`brand-check.js`+`supabase/add-notify-roles-rpc.sql`): client gửi order/rating/feedback... nhưng admin/account/lead_media không nhận noti — RLS `users` chỉ cho client đọc chính mình → lookup `in('role',...)` trả `[]` im lặng → 0 noti insert (9 chỗ: order_new Media + Ads, 6 sự kiện client-dashboard gồm rating, brand-check notifyMedia). Fix = RPC `notify_roles` SECURITY DEFINER (whitelist role nội bộ, không mở RLS users); frontend đổi 9 chỗ sang `supabase.rpc('notify_roles',...)`, helper `notifyRolesRpc` ở client-dashboard; brand-check giữ nhánh DEMO. ⚠ chạy `add-notify-roles-rpc.sql`. · **📧 EMAIL NOTIFY 2 SỰ KIỆN DONE 2026-07-13** (Edge Function `supabase/functions/notify-email/index.ts`): email về mail công ty khi (1) order mới từ client (loại order nội bộ) + (2) client hoàn thành rating (satisfaction_score NULL→có). Kích hoạt = Database Webhook bảng `orders` INSERT+UPDATE → function tự lọc; provider Resend; deploy `supabase functions deploy notify-email --no-verify-jwt` + secrets RESEND_API_KEY/NOTIFY_EMAIL_TO/NOTIFY_EMAIL_FROM/NOTIFY_EMAIL_SECRET/APP_BASE_URL. Chưa deploy → không ảnh hưởng app. · **🕒 FIX LAST LOGIN DONE 2026-07-11** (`login.html`+`user-management.js`): cột `users.last_login_at` có trong schema + UI đọc nhưng KHÔNG chỗ nào ghi → luôn "Chưa đăng nhập". Fix: `touchLastLogin()` ghi `last_login_at=now()` sau đăng nhập thật (password + Google SSO), KHÔNG đặt ở onAuthStateChange (SIGNED_IN bắn lại khi restore session); + `fmtStamp()` convert ISO timestamptz → `YYYY-MM-DD HH:MM` local cho relTime/hiển thị. RLS `users self update` đã có → KHÔNG cần SQL. · **⏱️ DEADLINE FORM DONE 2026-07-11** (`request.html`+`order-form.js`): bỏ field "Giờ mong muốn" (`requested_deadline_time`) + "Deadline có cố định không?" (`is_fixed_deadline`); chặn deadline < ngày gửi + 3 ngày (`input.min`=today+3d; change/submit nếu sớm hơn → popup `#rush-warn-modal` "task gấp — liên hệ phòng Marketing" + clear ô). Preview bỏ 2 dòng tương ứng. KHÔNG cần SQL. · **🧩 GỘP TYPE REQUEST FORM DONE 2026-07-11** (`request.html`+`order-form.js`): gộp Thiết kế/POSM + Digital Design → 1 tile "Thiết kế / Digital" (value `design`, bỏ tile `digital`) + bỏ tile Post/Bài đăng (`post`) tích hợp vào type gộp (deliverable group Post + sub-form Post đổi `data-*-for` sang `design`). Giữ dữ liệu bài đăng: submit gói `ads_objective/ads_cta/ads_key_message` vào content_brief khi design. Backward-compat: digital/post vẫn trong TYPE_LABEL cho order cũ; revision rule + reports role không đổi; value design vốn hợp lệ → KHÔNG cần SQL. · **🏠 FIX "BẤM HOMEPAGE BỊ OUT" DONE 2026-07-11** (`index.html`+`help.html`+`tracking.html`+`app.js`): header public luôn hiện "Đăng nhập" dù đã login (pill không đọc mh-user) → tưởng bị out (session còn nguyên). Fix mọi role: `app.js syncPublicLoginPill()` đổi thành "Vào Dashboard" + link ROLE_HOME; id `#header-login-btn`/`#footer-login-link` trên 3 trang public (request.html tự lo). **v2 2026-07-11**: XÁC THỰC session thật (`getSession`) trước khi đổi — v1 đọc mh-user sync hiện nhầm "Vào Dashboard" khi chưa login mà mh-user còn sót; chưa login → giữ "Đăng nhập"; bỏ inline block index.html. KHÔNG cần SQL. · **👥 VÁ NOTIF PIC CONTENT (Media prod status) DONE 2026-07-11** (`production-board.js notifyContentOnProdStatus`): audit content/lead_content — link target chuẩn (content→content-workbench, lead_content→content-team). Thủng: đơn nội bộ đổi status chỉ báo lead_content, không báo PIC content (người tạo Media Request). Fix: thêm nhánh notify PIC qua `contentTasks.get→assigned_pic→findUserIdByName`, link `content-workbench.html?task=` (content mở được), dedupe PIC≡lead. KHÔNG cần SQL. · **🔔 ĐỒNG BỘ NOTIF STATUS NỘI BỘ DONE 2026-07-11** (`production-board.js`) — Feedback #1.3 v2 (KHÔNG đụng client): vá 3 chỗ thủng nội bộ — `inprogress`→admin/account, `delivered`/`completed`→admin/account (+ lead_content cho order nội bộ qua `CONTENT_PROD_NOTIFY`). Bỏ hẳn nhánh requester/client (bản v1 rollback vì báo client task nội bộ). Notif client (Preview/Final) giữ nguyên. KHÔNG cần SQL. · **🔐 FIX "TỰ ĐĂNG XUẤT" / ĐIỀU HƯỚNG VỀ LOGIN DONE 2026-07-10** (`supabase-client.js onAuthStateChange`) — Feedback Portal Hub #1.1+#1.2: sau khi gửi yêu cầu / dùng lâu bị "đăng xuất" hoặc bấm Trang chủ-Quay lại về `login.html`. Root cause = mirror session→`mh-user`: (a) ghi đè vô điều kiện mỗi auth event → khi `users` query transient-fail (`res.data=null`) fallback `role='client'` (SSO không có metadata.role) → admin/account bị HẠ role → guard trang đá về login; (b) xóa `mh-user` ngay khi SIGNED_OUT (token-refresh churn / nhiều tab). Fix: chỉ ghi khi có users row (hoặc lần đầu); MERGE với mh-user cũ (không regress role/name, `!u && prev.role` cùng id → giữ nguyên); SIGNED_OUT double-check `getSession()` trước khi xóa. **Các bug feedback còn lại (chưa fix, phần lớn migration/design)**: #1.3 notif realtime (cần `enable-realtime.sql` cho notifications + orders/tasks); #1.4 Content tạo task = chạy `add-content-self-initiative.sql` + name-match RLS (`users.name`≡`mh-user.name`); #1.5 task gấp chưa có push ưu tiên riêng; #1.6 lag = poll 60s + full `list()` mỗi trang; #1.7 link Source/Working/Final phụ thuộc cột order + Storage bucket. · **🔒 DESIGN/EDITOR KHÔNG GIAO VIỆC NỘI BỘ DONE 2026-07-08** (`task-dashboard.html`+`production-board.js`): chỉ admin/account/lead_media tạo task nội bộ. Ẩn nút "Giao việc nội bộ" (`#td-create-task`) cho design/editor + gate `handleCreateFromURL` (deep-link `?createTask=1`) + chặn CREATE trong `openTaskModal` (trước chỉ chặn EDIT). Design/Editor vẫn nhận & đổi status task. · **⭐ RATING BẮT BUỘC FEEDBACK DONE 2026-07-08** (`client-dashboard.js` `rating-submit` + `client-dashboard.html` modal): client rating order phải kèm nhận xét — chỉ chọn sao mà bỏ trống ô Nhận xét → toast "Cần nhận xét" + focus, không submit (`comment.trim()===''`). Label modal → dấu `*` bắt buộc. · **🐛 FIX NOTI "MA" Content Wording DONE 2026-07-08** (`database-orders.js transferToWording`): Lead nhận noti "Order mới trong Content Inbox" nhưng Inbox rỗng — do order UPDATE và notification INSERT độc lập; update không land (lỗi CHECK/thiếu migration `add-brief-wording-fields.sql`+`add-content-team.sql`, HOẶC RLS khớp 0 dòng → `.maybeSingle()` trả null im lặng) trong khi noti vẫn bắn. Fix: `transferToWording` async — ghi DB trước, await + verify `saved.brief_wording_status='assigned'`/`account_status='wording'` rồi mới notify+toast success; fail → toast danger nêu migration/RLS + KHÔNG notify. Đơn stuck: Account bấm lại "Chuyển Content Wording" để retry (giờ hiện lỗi thật). · **⏰✅ SLA LEAD DUYỆT + PIC SELF-CHECKLIST DONE 2026-07-08** (`content-workbench.js`+`content-team.js`+`data-store.js`+`supabase/add-content-review-sla.sql`): 2 cột mới `content_tasks.lead_review_due`+`pic_checklist`. **Q2 SLA**: PIC gửi Lead → set `lead_review_due` (nhập tay `#wt-review-due` hoặc auto +24h); drawer PIC block "Hạn Lead duyệt (SLA)"; Lead thấy `reviewLateCT` tô đỏ ở drawer (dl + callout Review Panel) + chip "quá hạn duyệt" ở bảng Initiatives/task con Plan. **Q3 PIC checklist**: drawer PIC content-workbench block "Checklist của PIC" (add `#wt-picchk-add`/xóa/tick, progress, lưu ngay khi add/remove, tick lưu khi Lưu nháp/submit; KHÔNG bắt buộc gửi duyệt); Lead xem read-only. data-store `contentTasks.create/update` thêm loop-strip + allowlist 2 cột → chưa migrate vẫn submit được. ⚠ chạy `add-content-review-sla.sql`. Q1+Q2+Q3 (3 yêu cầu Content team) HOÀN TẤT. · **🔽 PIC CONTENT = DROPDOWN ĐỒNG BỘ DONE 2026-07-08** (`content-team.js`): 6 ô gán PIC Content (`ct-pic`/`ctm-t-pic`/`cct-pic`/`cim-pic`/`ads-pic`/`ads-tt-pic`) đổi `<input list=datalist>`→**`<select>`** như PIC Media. Root cause "không đồng bộ": `CONTENT_USERS` chỉ load khi `supabaseEnabled` → demo/chưa seed = rỗng. Fix helper `picSelectOptions`/`contentPicNames` gộp users role=content + PIC đã dùng + fallback seed `['Thu Hà','Minh Anh','Bảo Trân']` (luôn có option) + giữ giá trị hiện tại + sort vi. KHÔNG cần SQL. Q2 (SLA Lead duyệt content) + Q3 (PIC tự add checklist trong drawer) CHƯA làm — chờ anh xác nhận. · **✍️ CONTENT TỰ ĐỀ XUẤT TASK (1 GATE LEAD DUYỆT NỘI DUNG) DONE 2026-07-06** (`content-workbench.html/js`+`content-team.js`+`supabase/add-content-self-initiative.sql`): role `content`/admin có nút **"Đề xuất task mới"** (`#cwbt-new-task`) ở tab "Content Tasks của tôi" → modal `#cwbt-modal` (Tiêu đề* · Brief · Output chips default social_post · Ưu tiên · Hạn · checkbox "Cần Media thiết kế" → type/deadline/note) → `contentTasks.create` source=`content_initiated`/assigned_pic=CHÍNH MÌNH/status=`in_progress`/created_by=mình → mở drawer viết luôn + notify lead_content (link `content-team.html?task=`). **1 gate duy nhất trước Media = Lead duyệt nội dung** (không có gate "chấp thuận đề xuất"); flow sau tạo dùng nguyên đường sẵn có: Gửi Lead duyệt → Lead approve (`ctm-rev-approve`) → form "Gửi sang Media" (`canSendMediaTask` không đổi) → order nội bộ `MEDIA-…-CT` client_visible=false. Lead Initiatives table thêm chip "PIC tự đề xuất" khi created_by===assigned_pic. Guard double-submit `selfModalSaving`. ⚠ chạy `add-content-self-initiative.sql` (policy INSERT content_tasks: role content + source content_initiated + assigned_pic=tên mình; comments INSERT đã sẵn). Verified CDP 3 phase (gate chặn đúng trước duyệt, mở đúng sau duyệt, order mock đúng kind), 0 JS errors. · **👁 LEAD CONTENT XEM CLIENT ORDERS (READ-ONLY + COMMENT) DONE 2026-07-06** (`database-orders.js`+`app.js`+`content-team.js`+`data-store.js`+`styles.css`+`supabase/add-lead-content-order-view.sql`): guard database-orders mở cho `lead_content` — READONLY tái dùng nguyên cơ chế monitor supervisor (kebab ẩn · input/select/textarea disabled · mọi nút mutation ẩn qua `applyDrawerReadonly`; ngoại lệ `RO_KEEP_IDS` = drawer-close + ô comment + nút gửi) + badge "Chế độ chỉ xem · Lead Content/Giám sát" cạnh H1. **Comment nội bộ**: section drawer "Comment Lead Content" (mọi role thấy thread; form gửi chỉ lead_content) → cột MỚI `orders.lead_content_notes` qua RPC **`append_lead_content_order_note`** (SECURITY DEFINER lead_content/admin, CHỈ append `[dd/mm/yyyy hh:mm · Tên · Lead Content] text`, cap 2000 ký tự; demo fallback mutate mock; chống double-click) — **KHÔNG dùng `internal_note`** (lộ ra Client Portal khi account_status=needinfo); gửi xong notify admin+account (type system). Nav "Client Orders" + badge **View only** reveal RUNTIME qua `app.js revealClientOrdersForLeadContent()` (né substring trap content⊂lead_content); Content Team drawer hiện "Mở Order gốc (chỉ xem)"; deep-link nhận `?id=`/`?order=`; role `content` vẫn chặn; lead KHÔNG confirm/push/PIC/status/deadline/hủy/preview/final (UI khóa + không có đường ghi DB ngoài RPC note). ⚠ chạy `add-lead-content-order-view.sql`. · **🔔 ADS CLIENT NOTIFY + lead_media order_new DONE 2026-07-06** (`content-team.js`+`order-form.js`+docs; audit khép kín 2 flow 2026-07-06): vá 2 hở noti tìm thấy khi audit end-to-end — **(1)** Ads flow trước đây client CÂM sau submit (Media flow có 5+ noti client, Ads = 0): thêm `ADS_CLIENT_NOTIFY` + `notifyAdsClient(o,newStatus,prevStatus)` gọi trong `adsAdvance` — báo client 3 nấc `running` (resume `paused`→`running` không re-notify) · `completed` · `cancelled`; type `order_status_changed` (sẵn trong CHECK), lookup `requester_id` fallback `requester_email`, link `tracking.html?code=` (bell client resolve `client-dashboard.html?order=`); 3 status này chỉ set qua `adsAdvance` nên 1 chokepoint đủ (workbench sync tối đa `creative_ready`). **(2)** `order-form.js` notify `order_new` mở rộng `['admin','account','lead_media']`. Audit kết luận: Media flow (order→wording→confirm→push→production→preview→3 vòng feedback→final→rating) KHÉP KÍN; Ads flow khép kín sau fix (1); Ads KHÔNG có rating (by design — `isFinalDelivered` không nhận ads, kết thúc ở completed/report_updated). Số đếm build sync: **21 HTML (+5 demo `_*.html`) · 21 JS**. · **🛡️ AI BRAND SAFETY CHECKER — module #15 DONE (MVP) 2026-07-03** (`brand-check.html`+`assets/brand-check.js`+`app.js injectBrandCheckNav`+`data-store.js` namespace `brandChecks`+`styles.css` prefix `bc-*`+`supabase/add-brand-check.sql`+**Edge Function** `supabase/functions/brand-check-analyze/index.ts`; spec `../Brief_Wflow/CB_Brand_AI_Safety/`): kiểm duyệt hình ảnh AI theo checklist thương hiệu CB. **Upload** JPG/PNG/WEBP ≤10MB + metadata (nhóm nội dung 1-nội bộ/2-tự kiểm/3-Media duyệt + 7 quick flags logo/mascot/đồng phục/cơ sở/quảng cáo/đối tác/nhạy cảm) → **AI Vision** chấm 6 tiêu chí /100 (logo_identity 25 · brand_color/text_quality/ai_artifacts/education_suitability/communication_risk 15 mỗi loại; 85+=PASS · 70–84=NEEDS_REVISION · <70=FAIL) → **RULE ENGINE v2 (2026-07-04)**: CHỈ Nhóm 3 ⇒ REQUIRES_MEDIA_REVIEW; Nhóm 1/2 status theo ĐIỂM, AI muốn Media xem → `ai_warnings[]` khuyến nghị (không chặn/không notify); điểm = chất lượng khách quan (có logo/mascot KHÔNG trừ điểm); **tag ĐÃ DUYỆT** — PASS Nhóm 1/2 auto-duyệt `approval_source='auto_ai'`, Media APPROVED → `'media'`, REJECTED/REVISION thu hồi; mã duyệt = check_code (BSC-… · ĐÃ DUYỆT + copy); badge/filter/search theo mã; ⚠ chạy lại add-brand-check.sql (cột approved_at/approval_source) — engine nằm CẢ 2 nơi: Edge Function (chuẩn) + brand-check.js mirror (demo/fallback), sửa rule sửa cả hai) → lưu `brand_checks`+`brand_check_criteria` (fallback localStorage `mh-brand-checks`) → **drawer kết quả** (ảnh signed URL · điểm · criteria table · issues/actions · override box · manual review panel) + **History** (filter ai_status/manual/nhóm + search + CSV BOM; non-media chỉ thấy của mình qua RLS) + **Dashboard hậu kiểm** (media+supervisor: KPI + 4 chart bars: status/top lỗi tiêu chí từ `ai_result_json.criteria`/chi nhánh/trend 14 ngày) + notify admin/lead_media khi REQUIRES_MEDIA_REVIEW hoặc NEEDS_MANUAL_REVIEW (type `system`, link `brand-check.html?id=`) + notify uploader khi Media hậu kiểm + deep-link `?id=` + realtime + poll 60s. **AI call**: upload bucket private `brand-check-images` (path `{uid}/{key}/{ts}-{file}`) → `functions.invoke('brand-check-analyze')` — Edge Function tải ảnh service-role, provider theo secret `BRAND_CHECK_PROVIDER` (**gemini mặc định** model `gemini-2.5-flash` ép JSON `responseSchema` + safety BLOCK_ONLY_HIGH / anthropic `output_config.format` / openai `response_format`), override server-side; **API key CHỈ trong Supabase secrets**; function lỗi/chưa deploy → fallback NEEDS_MANUAL_REVIEW (hoặc REQUIRES_MEDIA_REVIEW nếu metadata rủi ro); Supabase off → **DEMO** `simulateAiResult()` deterministic (badge DEMO, confidence low). **Roles**: mọi role đăng nhập upload + xem của mình · admin/account/lead_media = Media hậu kiểm (APPROVED/REVISION_REQUIRED/REJECTED/ARCHIVED + note) + dashboard + CSV · system_supervisor read-only all · **client/giáo viên ĐÃ MỞ (2026-07-03)**: tự kiểm từ Client Portal (sidebar tối giản, không lộ nav nội bộ) + **banner kết luận `actionVerdict`** (Nhóm 1 đạt → "Được dùng nội bộ" · Nhóm 2 đạt → "Đủ điều kiện tự đăng" · Nhóm 3/ca rủi ro → "Bắt buộc gửi Media duyệt"); 2 tầng kiểm duyệt (AI tự động cho Nhóm 1&2 → tự dùng, chỉ ca rủi ro về Media). Backend RLS `auth.uid()` sẵn cho client, chỉ chỉnh frontend. **⚠ chạy `add-brand-check.sql`** (2 bảng + RLS + bucket policy `cbbrand_` + Realtime) **+ `supabase functions deploy brand-check-analyze` + `supabase secrets set BRAND_CHECK_PROVIDER=anthropic ANTHROPIC_API_KEY=...`**. Phase sau (spec §22): multi-upload · so sánh trước/sau chỉnh · thư viện lỗi mẫu · báo cáo tuân thủ tháng/quý · gắn request Media khi REQUIRES_MEDIA_REVIEW. **20 HTML · 17 JS**. · **📣 ADS ORDERS — Client → Content Team DONE 2026-07-01** (`request.html`+`ads-order-form.js`+`content-team.html/js`+`client-dashboard.html/js`+`database-orders.js`+`production-board.js`+`data-store.js`+`styles.css`+`reports.js`+`supabase/add-ads-orders.sql`): luồng thứ 2 độc lập, TÁCH khỏi Media/Production. **Client**: 2 CTA riêng (thiết kế/Media → `request.html`; chạy Ads → `request.html?type=ads`); form Ads **5 section** tự chứa (`ads-order-form.js`, ẩn form Media; KHÔNG hỏi Creative — Content chủ động tạo Internal Media Request nếu cần). Chọn loại: **toggle** `.rf-type-switch` (thumb gradient brand trượt) + **popup chooser** `#rf-chooser` (2 card Media/Ads + gợi ý hạng mục) hiện khi vào request không có `?type`; `?type=`/`mode=revision` bỏ qua popup; đóng → mặc định Media. Form Ads: **layout 3 cột** (stepper `data-ads-step` scroll-spy trái + tips/quy trình phải), **đính kèm File kế hoạch** (upload brief-files + link Drive), **Lưu nháp** (`mh-ads-draft`) + **Preview Brief** (`#ads-preview-modal`). **`tracking.html`** tra cứu `ADS-*` (map `ads_status`→public, ẩn đơn nội bộ). Tile Media cũ "Ads/Post Basic"→**"Post"** (`request_type` `ads`→`post`). Order: `order_kind='ads_order'`/`request_type='ads'`/`owner_team='content'`/`ads_status='submitted'`/`ads_detail`(jsonb)/prefix **`ADS-2026-xxx`**; notify lead_content. **Lead Content** (`content-team.html?tab=ads-orders`, tab + drawer `#ctm-ads-drawer`): lifecycle `ads_status` 18 trạng thái (state-machine `adsAdvance`), gán PIC, **Tách Content Tasks** (source=`ads_order`→PIC ở content-workbench), **Tạo Internal Media Request** (`order_kind='internal_ads_media_request'`/`origin='ads_order'`/`client_visible=false`/`source_ads_order_id`/prefix **`ADS-MEDIA-2026-xxx`**→Production). **database-orders** lọc bỏ ads_order (Account không bottleneck) + badge "Internal · From Ads". **Client Portal** chỉ thấy **public status** (`ADS_PUB` 8 nhãn, `isAdsOrder`/`adsInfo`); KHÔNG task nội bộ. Badges CSS `.badge-ads-order`/`.badge-from-client`/`.badge-need-creative`/`.badge-internal-ads`/`.badge-client-hidden`. **⚠ chạy `add-ads-orders.sql`** (cột ads + request_type CHECK += post + content_tasks.source += ads_order + RLS lead_content + **RLS content update ads**). Prefix: `ADS-`/`MEDIA-`/`ADS-MEDIA-`. **KHÉP KÍN 2026-07-02 (4 fix)**: noti PIC hết bounce (link workbench) · workbench media request task ads → đúng kind `internal_ads_media_request`+`source_ads_order_id`+prefix ADS-MEDIA · **auto-sync `ads_status`** theo Content Task 2 phía PIC/Lead (start/submit/return/approve/media/complete, guard không kéo lùi launch) · Dashboard card Ads (trạng thái + cần chú ý) + Calendar event **`ads-launch`** "Lịch lên Ads". · **🔐 Google Workspace SSO @cbcentres.com 2026-06-19** (`login.html`+`data-store.js`+`add-google-sso-cbcentres.sql`): login có 2 cách song song: email+mật khẩu + nút Gmail công ty (Google SSO). `auth.signInWithGoogle` (hd=cbcentres.com); `handleOAuthReturn` + cờ `mh-oauth-pending` + domain guard signOut nếu ngoài @cbcentres.com → route role; `roleHome.lead_content`→`content-team.html`. Provision: bảng `user_role_map` + trigger `handle_new_auth_user` gán role/name theo email. ⚠ cần bật Google provider Supabase + OAuth Client Google Cloud (app Internal) + chạy migration + seed user_role_map + mỗi người SSO 1 lần. · **🎯 PIC Content gửi Internal Media Order + checklist hoàn tất trong content-workbench 2026-06-19** (`content-workbench.js`+`content-team.js`+`add-content-to-media-order.sql`): chuyển creator order nội bộ Lead→**PIC Content**, trọn trong Content Task Drawer. PIC (sau khi Lead duyệt) thấy **form inline "Gửi sang Media"** (prefill handoff) → `createMediaRequestInline` (order `requester_role='content'`, `MEDIA-…-CT`, notify admin/account/lead_media+Lead) → block **theo dõi sản xuất** (`fillMediaTrack`) + **Checklist hoàn tất task** (Lead duyệt ✓ · đã gửi Media · Media Final; progress X/N). Order có Final → nút **"Hoàn tất task"** → `completeTask` `status=completed`+notify Lead. Lead drawer: **bỏ** nút tạo, thêm checklist read-only + giữ tracking. ⚠ RLS mới `orders content insert internal` (SELECT đã sẵn). · **🔁 KHÉP KÍN loop noti Content↔Production (order nội bộ) 2026-06-19** (`production-board.js`+`database-orders.js`): vá lỗ hổng chiều Media→Content cho order nội bộ từ Content (`source_content_task_id`). **(A)** `notifyContentOnProdStatus` (gọi trong `updateStatus`) — Media đổi `inprogress`/`ready` → noti **Lead Content** deep-link `content-team.html?task=`. **(B)** `isInternalOrder()` + `notifyClient` early-return khi internal (hết bắn noti văn phong client tới Lead) + `sendDelivery`→`notifyContentRequester` báo Lead khi Preview/Final (deep-link content task). Content thấy link sống qua `fillMediaTrack`. CHỈ insert notifications (RLS chặn Media/Account ghi `content_tasks`); `related_entity_type='orders'`. Không cần migration mới. · **🎬 Content Team Deep Workflow — Phase 5 Content→Media + Phase 6 Dashboard/Calendar DONE 2026-06-18** — **HOÀN TẤT 6/6 PHASE**. **P5 Internal Media Request** (`content-team.js`+`database-orders.js`+`client-dashboard.js`+`data-store.js`+`supabase/add-content-to-media-order.sql`): trên Content Task `lead_approved`+need_media+chưa tạo → CTA **Tạo Internal Media Request** → handoff modal (validate min: body/script·visual·format·channel·deadline·request_type·deliverable) → `MH.store.orders.create` order nội bộ (`origin=content_team`/`order_kind=internal_media_request`/`client_visible=false`/`source_content_task_id`/`source_content_plan_id`/`account_status=confirmed`/`brief_wording_status=completed`, map handoff→order fields, gen `MEDIA-…-CT`); task→`media_order_created`+`media_request_created=true`+`media_order_id`; notify admin/account/lead_media; Lead Media/Account push Production như order thường (Option A). **Badge "Internal · From Content"** ở Database Orders drawer (`d-internal-badge`). **Client Portal lọc bỏ** (`client_visible===false`/`order_kind`/`origin`). **Content/Lead theo dõi order nội bộ** (2026-06-19): nút "Mở Media Order" cũ link database-orders → lead_content/content bị guard chặn (redirect) ⇒ thêm block **"Media Order — theo dõi sản xuất"** read-only NGAY trong Content Task Drawer (`fillMediaTrack` fetch `orders.get` qua RLS lead_content SELECT: PIC Media · trạng thái SX `PROD_STATUS` · tiến độ % · hạn · link Preview/Final), KHÔNG cần vào database-orders; link "Mở Media Order" chỉ còn hiện cho admin/account. `orders.create` nâng loop-strip nhiều cột thiếu (migration chưa chạy vẫn tạo order base). ⚠ chạy `add-content-to-media-order.sql` (cột internal + RLS lead_content INSERT/UPDATE order nội bộ). **P6 Dashboard/Calendar/QA**: content-team Dashboard view thêm **KPI Content Tasks & Plans** (12 KPI: tổng/client-order/initiative/plan/sắp-hạn/trễ/chờ-Lead/đã-duyệt/media-request/duyệt-vòng-1%/vòng-sửa-TB/Content→Media%) + breakdown theo Status/Source/Output + Plans progress + Task vòng-sửa-cao; Calendar thêm event **Hạn Content Task** (`ct-deadline`) + **Hạn Content Plan** (`cplan-deadline`) role-filtered (lead_content/admin all · content task mình PIC) click→content-workbench/content-team `?task=`/`?plan=`. **Dashboard panels redesign compact 2026-06-19** (`.ctm-card`/`.ctm-cards-2` thay `.table-card.ctm-panel`+`.table-head`, header nhẹ + action "Xem tất cả"→tab Plans): Content Plans = compact progress rows (name·status badge·meta chip chờ-duyệt/hoàn-thành/trễ·bar 6px); Workload theo PIC GỘP order-wording + content_tasks active, scale bar denom=max(busiest,4) tránh value nhỏ full-width; empty-state có chủ đích (`ctmEmpty` icon Lucide+title+helper). **Bỏ emoji 🎉 structural** → inline SVG (xem memory icon-style). ⚠ vẫn cần `add-content-initiatives.sql`+`add-content-to-media-order.sql`) · **🛂 Content Team Deep Workflow — Phase 4 Lead Review & Internal Revision DONE 2026-06-18** (`content-team.js`+`styles.css`: nâng **Content Task Drawer** thành cổng duyệt nội bộ Lead↔PIC. Lead thấy **Bản thảo Content** (read-only WS_ORDER cột content_tasks) + **Quality Checklist (n/9)** + **Production Handoff Draft** (nếu need_media) + **Revision History (N vòng)** + Missing Info. **Lead Review Panel** (status `submitted_to_lead`): dropdown 14 lý do (`REVISION_REASONS`) + ghi chú → **Trả Content chỉnh** (status→`lead_revision`, `internal_revision_count++`, append `revision_history`, lưu `last_revision_reason`/`lead_review_note`, notify PIC) **KHÔNG giới hạn vòng** · **Duyệt** (status→`lead_approved` + `lead_approved_at/by`) định tuyến theo source/need_media: cần Media→`lead_approved` chờ Media Request (Phase 5) · `client_order`→`submitted_to_account` (notify Account/Admin) · còn lại→`completed`. Mỗi đổi status gọi `syncPlanStatus` roll-up plan cha. PIC nhận `lead_revision` ở content-workbench (Phase 3) sửa+gửi lại không giới hạn. ⚠ vẫn cần `add-content-initiatives.sql`) · **✍️ Content Team Deep Workflow — Phase 3 PIC Workbench DONE 2026-06-18** (`content-workbench.html`+`content-workbench.js`: thêm tab **"Content Tasks của tôi"** (model `content_tasks`) TÁCH BIỆT bảng order-wording cũ. PIC content thấy task được gán (`isMine(assigned_pic)`; admin/lead/account xem tất cả) — sub-tabs Tất cả/Đang viết/Cần chỉnh/Đã gửi Lead/Đã duyệt + stats + filter output. **Content Task Drawer**: Task Summary+Brief (kèm key message/đối tượng từ Plan) · Missing Info/Assumptions/Questions/Risk (không block) · **Writing Workspace multi-output** (field động theo `output_types` → map cột content_tasks: draft_body/caption/headline/script/voice_over/ads_*/landing_copy/email_sms_zalo_copy/cta/mandatory_info/visual_direction) · **Production Handoff Draft** (chỉ khi need_media) · **Quality Checklist 9 mục** (jsonb) · Files/Links · Activity/Revision History. Flow: pic_assigned→in_progress(Bắt đầu viết)→submitted_to_lead(Gửi Lead duyệt); lead_revision↺. **Submit chỉ pic_assigned/in_progress/lead_revision**, validate checklist đủ + ≥1 ô nội dung; notify lead_content (`task_status_changed`, link `content-team.html?task=`). **Content KHÔNG bypass Lead** (không nút gửi Account/Client/Media). Deep-link `?task=`. Dùng `MH.store.contentTasks` (Phase 1). Flow order-wording cũ KHÔNG đụng. ⚠ vẫn cần `add-content-initiatives.sql`) · **🧭 Content Team Deep Workflow — Phase 2 Lead Workspace DONE 2026-06-18** (`content-team.html`+`content-team.js`+`styles.css`: 2 tab mới **Content Plans** + **Content Initiatives** (Lead/Admin/Account/Supervisor xem; chỉ Lead/Admin tạo). **Content Plans** = bảng plan cha (progress bar roll-up · #task con · trễ · chờ duyệt · status badge) + **Plan Drawer** (thông tin KH/file/objective/channels/key msg/CTA/deadline + Progress summary 6 ô + bảng task con + "Thêm task con" + activity). **Content Initiatives** = bảng content_tasks `source=content_initiated` + form tạo (output_types checkbox · PIC · hạn · ưu tiên · cần Media → media type/deadline/note). **Content Task Drawer** (Lead follow/reassign PIC+hạn+ưu tiên+cần Media · Missing Info/Assumptions/Questions/Risk — KHÔNG trả brief về Account · thông tin task · activity). **Roll-up tự động** plan status/progress từ task con (spec §6: completed/at_risk/pending_review/in_progress/active) + persist best-effort. Modal generic (tạo/sửa Plan · Initiative · task con) + cảnh báo hạn task con vượt deadline plan (non-block) + deep-link `?plan=`/`?task=` + notify PIC khi gán. Dùng `MH.store.contentPlans`/`contentTasks` (Phase 1). Flow Inbox/orders cũ KHÔNG đụng. ⚠ vẫn cần `add-content-initiatives.sql` để persist Supabase) · **🧱 Content Team Deep Workflow — Phase 1 Foundation DONE 2026-06-18** (data model nền: 3 namespace data-store mới `contentPlans`/`contentTasks`/`contentTaskComments` + `contentEnums` (SOURCES `client_order`/`content_initiated`/`strategy_board`/`campaign_package` · PLAN/TASK statuses · OUTPUT_TYPES); fallback localStorage `mh-content-plans`/`mh-content-tasks`/`mh-content-task-comments` (pattern leadTasks, cross-tab) + **seed demo 1-lần** (flag `mh-content-seed-v1`: 1 Plan "Khai trương Q3" + 5 task con + 1 Initiative độc lập + 1 task từ Client Order); migration `supabase/add-content-initiatives.sql` (3 bảng + RLS Content-Team-only KHÔNG đụng `is_staff` + index + trigger + Realtime). Phase 3–6 còn lại: PIC Workbench multi-output · Lead Review no-limit revision · Content→Media Handoff Internal Order · Dashboard/Reports/Calendar/Notif/QA. ⚠ SQL pending thêm `add-content-initiatives`) · **🗂️ Supervisor Planning DONE 2026-06-17** (module #14 — `supervisor-planning.html`+`.js`: Supervisor/Admin lập+giao kế hoạch nội bộ cho Lead Media/Content/**cả 2 (`both`)**, TÁCH BIỆT task production; bảng `lead_tasks`+`lead_task_comments` + **đính kèm PDF/link** + **tiến độ per-lead** + **cổng duyệt** (Lead Nộp duyệt→Supervisor Duyệt/Trả chỉnh) + **workspace** (comment/checklist/timeline) + **List/Board** + realtime + notify 2 chiều; **role mới `lead_media`**; sidebar **"Strategy Board"**; ⚠ chạy `add-supervisor-planning.sql`; **19 HTML · 16 JS**) · **🛡️ Role `system_supervisor` (Giám sát hệ thống) DONE 2026-06-14** (monitor READ-ONLY cấp hệ thống: redirect dashboard, xem mọi màn vận hành + Reports/export, MỌI mutation khóa, ẩn Order Form/User Mgmt/Settings; ⚠ chạy `add-system-supervisor.sql` — SELECT-only RLS, KHÔNG đụng `is_staff()`) · **📊 Reports wired LIVE 2026-06-12** (`reports.js`: load orders+tasks+users qua `MH.store` → aggregate client-side 12 KPI/6 charts/funnel/rating/3 bảng; filters recompute thật từ RAW snapshot; empty-state khi 0 data — fix crash `FUNNEL[0]`; poll 60s; module cuối cùng nối Supabase) · **🧑‍🤝‍🧑 Content Team Workspace DONE 2026-06-11** (`content-team.html`+`content-team.js`: role mới **`lead_content`** + `content` — team TÁCH BIỆT Production; views Dashboard / Content Inbox (Lead) / Board 7 cột / Danh sách / My Content Tasks + drawer: Lifecycle 7 bước · Lead Assign (PIC + hạn wording) · Workspace · Quality Checklist · Lead Review Panel · Account&Client status · Files/Links · Activity; flow `assigned`→`pic_assigned`→`in_progress`→`submitted_to_lead`→(`lead_revision`↺)→`submitted_to_account`→ Account/Client flow cũ; notify transfer→lead · gán→PIC · submit→lead · duyệt→admin+account; guard 2 chiều content/lead↔production; login redirect content+lead_content→content-team; content-workbench = legacy đọc-được status mới nhưng KHÔNG bypass Lead; ⚠ chạy `add-content-team.sql`) · **📅 Calendar / Lịch DONE** (`calendar.html`+`calendar.js`: **Month/Week/Day** read-only; chấm deadline task/order + lịch quay/chụp + bàn giao; **role-filtered** admin/account full · design/editor chỉ task PIC · content chỉ order wording; **Day view ref-style** mini-calendar + danh sách task của ngày ("+N khác"/click số ngày để mở); click event→popover→drawer; nav inject `app.js injectCalendarNav()`; ⚠ chạy `add-shoot-date.sql`) · **⏰ Hạn wording DONE** (`orders.wording_deadline`: Account đặt ở Order drawer khi/sau Chuyển Content Wording; Content Wording hiển thị cột "Hạn wording" + drawer + tô đỏ khi trễ; **chuyển wording → notify team Content** (`notifyContentWording`, type task_assigned); **Calendar role Content CHỈ hiện "Hạn Content Wording"** (event `wording-deadline` tím, ẩn deadline order/shoot); ⚠ chạy `add-wording-deadline.sql`) · **Content = team tách biệt**: tạm ẩn nav Master/Task Dashboard + Internal Task Tracker (CSS `:has`) · Content Role Phase 1+2+3+4 DONE (Brief Wording: role → gate → Content Workbench → **Client confirm**; ⚠ chạy `add-brief-wording-fields.sql` + `add-brief-wording-workspace-fields.sql` + `add-brief-wording-confirmation.sql`) · **Workflow notifications khép kín** (order → PIC → duyệt nội bộ → bàn giao → rating) · **Bàn giao Preview/Final trong Order drawer** (trang Delivery Log ĐÃ GỠ) · **Order type `media` "Quay/Chụp" gộp → Push tách 2 task (PIC Quay + PIC Chụp)** (`add-media-pics.sql` đã chạy production 2026-06-03) · Order Form rút gọn theo type (media skip mục 4,5 + đánh số liền mạch; video gộp 1 ô nội dung+định hướng; slide thả link Google Doc; **bỏ wording toàn bộ**) · Master Dashboard "New Orders" card + animation + NEW badge ở Client Orders · notif-icons shared module · fix date/timezone (deadline không mất) · **Order drawer**: Push CTA giữ sáng + nút "Xác nhận & Chuyển Production" + khóa PIC sau push + ô Production Status read-only + banner next-step (review/ready/delivered) · **Auto-sync Production Status task→order** (bottleneck rule; ⚠ chạy `sync-order-status-from-tasks.sql`) · **🐛 FIX: task status/link không lưu** (persistTask partial-upsert vi phạm NOT NULL → đổi sang `tasks.update`) · List mới-nhất-trước · **Preview → 3 vòng Feedback → Final + Approve Preview + Link sync Task↔Order** (⚠ chạy `add-revision-rounds.sql` + `add-preview-approval.sql`) · **Order Lifecycle Timeline** (Brief→Production→Preview&Feedback→Final&Rating, display-only) · **Task Tracker: "Sửa công việc" chỉ Admin/Account** (P.I.C chỉ Files&Links + status flow review) · ⚠ **6 SQL pending**: `sync-order-status-from-tasks` · `add-revision-rounds` · `add-preview-approval` · `add-shoot-date` · `add-wording-deadline` · `add-content-team` · **18 HTML · 15 JS** · Railway deploy LIVE ✓*

---

## Quick View

| # | Module | Status | Files | Spec |
|---|---|:---:|---|---|
| 0 | Public site | Done | `index`, `request`, `tracking`, `help`, `login` | [page spec](../Brief_Wflow/media-hub-page-module-spec.md) |
| 1 | Master Dashboard | Done | `dashboard.html` · `order-dashboard.html` · `task-dashboard.html` | [01](../Brief_Wflow/CB_Creative_Flow_Master_Dashboard_Module.md) |
| 2 | Order Form | Done | `request.html`, `order-form.js` | [02](../Brief_Wflow/CB_Creative_Flow_02_Order_Form_Module.md) |
| 3 | Database Orders | Done | `database-orders.html`, `database-orders.js` | [03](../Brief_Wflow/CB_Creative_Flow_03_database_orders_module.md) |
| 4 | Task Tracker (Production Board) | Done | `production-board.html`, `production-board.js` | [04](../Brief_Wflow/CB_Creative_Flow_04_production_board_module.md) |
| 5 | ~~Delivery Log~~ → **Bàn giao trong Order drawer** | Removed 2026-06-03 | `database-orders.js` (section "Bàn giao cho client") | — |
| 6 | Reports | Done | `reports.html`, `reports.js` | [06](../Brief_Wflow/CB_Creative_Flow_06_reports_module.md) |
| 7 | User Management | Done | `user-management.html`, `user-management.js` | [07](../Brief_Wflow/CB_Creative_Flow_07_user_management_module.md) |
| 8 | Settings | Done | `settings.html`, `settings.js` | [08](../Brief_Wflow/CB_Creative_Flow_08_settings_module.md) |
| 9 | AI Tools | Done | `ai-tools.html`, `ai-tools.js` | [09](../Brief_Wflow/CB_Creative_Flow_09_ai_tools_module.md) |
| 10 | Chatbot | Done | `chatbot.html`, `chatbot.js` | [10](../Brief_Wflow/CB_Creative_Flow_10_chatbot_module.md) |
| 11 | Client Portal | Done | `client-dashboard.html`, `client-dashboard.js` | [11](../Brief_Wflow/CB_Creative_Flow_11_Client_Portal_YeuCauSangTao_module.md) |
| 12 | Calendar / Lịch | Done | `calendar.html`, `calendar.js` | — (2026-06-08) |
| 13 | **Content Team Workspace** | Done | `content-team.html`, `content-team.js` (+ `content-workbench` legacy) | — (2026-06-11) |
| 14 | **Supervisor Planning** | Done | `supervisor-planning.html`, `supervisor-planning.js` | — (2026-06-17) |
| 15 | **AI Brand Safety Checker** | Done (MVP) | `brand-check.html`, `brand-check.js` + Edge Function `brand-check-analyze` | [spec](../Brief_Wflow/CB_Brand_AI_Safety/CB_AI_Brand_Safety_Checker_Planning_Prompt_Claude.md) (2026-07-03) |

**Overall**: 15/15 internal modules done · 5/5 public pages done · 0 module pending.

---

## Completed Modules

### 0. Public Site

Status: Done

- `index.html`: hero, quick actions, workflow overview, tracking lookup, FAQ preview, support strip. **Support strip repurposed 2026-06-12**: bỏ CTA ngoài "Cần tư vấn dự án lớn / booking dài hạn" → block nội bộ **"QUY TRÌNH NỘI BỘ"** ("Yêu cầu hợp lệ sẽ được tiếp nhận và phân công theo quy trình" + nút "Xem quy trình tiếp nhận →" → `help.html#requesting`); giữ nguyên gradient/layout/radius/responsive, chỉ đổi text+action. **Contact info đồng bộ toàn site** (index+help footer · help contact card · settings g-phone): email `marketing@cbcentres.com`, hotline `1900 866 855`, giờ làm việc T2–T6 7:30–11:30 · 13:30–17:00 / T7 7:30–11:30. **GSAP motion (2026-06-11)** via `assets/home-motion.js` (count-up, scroll-reveal, parallax, scroll-progress, header shadow, hover, magnetic CTA) — additive, `prefers-reduced-motion` + fallback an toàn, KHÔNG đụng `.fade-up`. _(Flow trang chủ/đăng nhập giữ nguyên gốc.)_
- `login.html`: 5 demo account tiles (click-to-fill credentials), password show/hide, role-based redirect after login, `?redirect=` param support to return to original page after auth.
- `request.html`: 7-section order form (requester → brief → type → content → assets → deadline → confirm), conditional service sub-forms, autosave draft, preview, success state. Auth-gated: blocks submit if not logged in, auto-fills requester info from session, redirects to `login.html?redirect=request.html` preserving draft.
- `tracking.html`: search by `MEDIA-*` code, client-scope guard, mock order timeline, progress, feedback modal.
- `help.html`: FAQ sections, search, scroll-spy, contact card.

> Note: `deliveries.html` was listed in earlier docs but never existed in the codebase. Removed from all references.

### 1. Master Dashboard + Order Dashboard + Task Dashboard

Status: Done

- **Master Dashboard** (`dashboard.html`): Module 5 combined overview cleaned up 2026-05-22. KPI layout có 4 sections rõ ràng — **Client Orders Overview** (8 order metrics), **Internal Tasks Overview** (8 task metrics), **Alerts** (5 alert categories), và **Team Workload** (6-PIC bar chart với pressure threshold MAX_PER_PIC=8). Legacy mixed KPI row + Workflow Health + Production Status donut + Delivery Funnel + Recent Activity đã removed (~302 lines). Drilldown wired qua `data-card-key` + optional `data-drill-key` → `database-orders.html` hoặc `production-board.html`. dashboard.html giờ 55.0 KB / 774 lines.
- **Order Dashboard** (`order-dashboard.html`): KPI cấp Order — Total / Pending / Need Info / Checking / Confirmed / Completed. Funnel theo `account_status`. Phân bố theo chi nhánh + theo loại request. Mỗi KPI click-through tới `database-orders.html?dl=<key>`.
- **Task Dashboard** (`task-dashboard.html`): KPI nội bộ team — Total Tasks / In Production / Internal Review / Due Soon / Overdue / On-time Rate. Workload theo P.I.C, phân bố loại task, Linked vs Standalone. KPI click-through tới `production-board.html?dl=<key>`.
- Cả 3 page dùng chung sidebar (Master / Order / Task Dashboard tách thành 3 entries dưới group "Vận hành").

### 2. Order Form

Status: Done

- 7 sections A–G: requester, brief, request type, content, assets, deadline, confirm. Smart flow stepper với active/done states, progress bar, icons; submit bar sticky.
- **Per-type behavior** (cơ chế `data-subform-for` / `data-subform-hide-for` / `data-subform-show-for`):
  - **Quay / Chụp ảnh = type `media`** (gộp Quay+Photo): service selector ☑Quay ☑Chụp + onsite info (ngày/giờ/địa điểm/người onsite) + Số lượng ảnh (bật khi tick Chụp); **ẩn mục 4 & 5, đi thẳng Deadline + đánh số liền mạch 1-5**.
  - **Video/Motion**: ẩn kích thước; Link kịch bản (bắt buộc) + Link source; Section 4 gộp 1 ô "Nội dung & định hướng".
  - **Slide**: ẩn kích thước; "Nội dung" → thả Link Google Doc/Slide; ẩn Headline/CTA/Thông tin bắt buộc.
  - **Bỏ "Hỗ trợ wording" toàn bộ type.**
- Upload simulation với max-size validation; priority logic; autosave `mh-order-draft-v2`; preview modal.
- Auth guard: inject requester identity, lock email, preserve draft on redirect.

### 3. Database Orders

Status: Done

- Saved views, search, filters, sortable table, pagination.
- Detail drawer: **Order Workbench** single-column flow with summary strip, animated next-action banner, requester/brief/internal sections, dedicated internal note/comment block, push validation, related tasks, delivery summary, and activity log.
- **Related Tasks block (T)**: list mọi task gắn với order (`order_id` match) — gộp từ in-memory built-in TASKS snapshot + `localStorage['mh-extra-tasks']`. Mỗi item click → mở Task Tracker với `?id=<task_id>` auto-open drawer.
- **Create Task from this Order** button: chỉ enable khi `account_status === 'confirmed'`. Redirect `production-board.html?createTask=1&order_id=...&project_name=...&task_type=...&priority=...&internal_deadline=...&production_pic=...&content=...` → auto-mở Create Task modal có prefill.
- Drawer actions: Check, Need Info, Confirm, Push to Production, Cancel.
- Push validation: confirmed brief + PIC + internal deadline + deliverable + active status.
- **Push UX (2026-06-03)**: sau Confirm brief, nút **Push → Production giữ sáng** (CTA), chỉ mờ khi đã push (bỏ gate `readyToPush`); validate khi bấm. Thêm nút **"Xác nhận & Chuyển Production"** trong section Điều phối (lưu PIC/deadline rồi push 1 chạm). **Khóa PIC select sau push** (`isOrderPushed()`) + note "đổi PIC ở Task Tracker" (tránh desync order↔task). Hint push media-aware (Quay/Chụp).
- **Auto-sync Production Status task→order (2026-06-03)**: order.production_status + progress tự đồng bộ theo task (bottleneck = task chậm nhất). Ô Production Status ở order drawer **read-only** (task-driven). Nguồn chính = DB trigger `supabase/sync-order-status-from-tasks.sql` (⚠ cần chạy production 1 lần — SECURITY DEFINER bỏ qua RLS cho design/editor); client-side chỉ admin/account.
- **Preview → 3 vòng Feedback → Final (2026-06-03)**: bàn giao trong Order drawer (Delivery Log vẫn gỡ; client đọc `orders.preview_link`/`final_delivery_link`). Bàn giao đầu = "Preview"; order design tối đa **03 vòng** feedback (helper text/policy theo vòng ở Client Portal), vòng 4 → tạo task/order mới. Order drawer có panel Feedback/Vòng chỉnh sửa (round X/3, latest feedback, badge giới hạn). Migration `supabase/add-revision-rounds.sql` (⚠ cần chạy) + notif type `client_feedback_received`. Notif: delivery_preview · client_feedback_received · delivery_final · rating_received.
- **Order Lifecycle Timeline (2026-06-04)**: đầu Order drawer có timeline display-only 4 nhóm **Brief → Production → Preview & Feedback → Final & Rating** (done/active/pending + status ngắn) + dòng summary trạng thái hiện tại. Tách bạch với Quick Actions (buttons). `orderNextAction` suy ra từ lifecycle stage. KHÔNG khôi phục Delivery Log.
- **Approve Preview + Link sync Task↔Order (2026-06-04)**: Client **"Duyệt Preview"** → `feedback_status=approved` + notify `client_preview_approved`. Order drawer: **"Gửi feedback cho PIC"** (comment type=feedback + task `feedback_fix` + notify PIC + order `revision_in_progress`) và **"Links from Task Tracker"** (Copy / Dùng làm Preview / Dùng làm Final — chỉ điền ô, KHÔNG tự gửi client). Task drawer: block **"Client Feedback Round N/3"** + badge "Đang chỉnh Feedback Round N" khi `feedback_fix`. Gatekeeper: chỉ Account/Admin gửi Preview/Final. Migration `supabase/add-preview-approval.sql` (⚠ cần chạy) + notif type `client_preview_approved`.
- 18 mock orders.

### 4. Task Tracker (Production Board)

Status: Done

- Views: Table, Kanban, My Tasks. Default view per role: admin/account → table; design/editor → My Tasks.
- Sidebar label rename: **Production Board → Task Tracker**. Page H1 dual-labeled: "Task Tracker / Production Board". File path unchanged (`production-board.html`).
- Page-head `[+ Tạo Task]` button → opens Create Task modal in-page.
- Create / Edit Task modal: supports `is_standalone` checkbox (ẩn order_id row khi bật), prefill từ URL params, save mới push vào TASKS + `localStorage['mh-extra-tasks']`. **"Sửa công việc" (edit metadata) CHỈ Admin/Account** (2026-06-04) — P.I.C design/editor KHÔNG thấy nút + `openTaskModal(editId)` chặn (toast). P.I.C chỉ sửa link (Source/Preview/Final) + status flow (pending→received→inprogress→review, revision/feedback_fix→review); KHÔNG ready/completed/cancelled/paused/đóng task; KHÔNG đổi PIC/deadline/priority.
- Quick filter chip row (`#quick-filter-chips`): Tất cả · Due Today · Due This Week · Overdue · Unassigned · My Tasks · Standalone. Tách biệt với summary card quick-filter (giữ nguyên).
- Linked Order block trong task drawer: hiển thị order_id + project name + button "Mở Order" → `database-orders.html?id=<order_id>`. Task `is_standalone` hiển thị note thay vì link.
- Summary cards, filters, sortable table, drag-drop Kanban giữ nguyên.
- Drag/status validation, link requirement cho Ready/Delivered transitions.
- 16 mock tasks + cross-page tasks từ `mh-extra-tasks` (created via modal hoặc "Create Task from Order").

### 5. ~~Delivery Log~~ → Bàn giao trong Order drawer

Status: **Removed 2026-06-03** (trang `delivery-log.html` + `delivery-log.js` đã xóa).

- **Lý do**: trang Delivery Log thao tác trên bảng `deliveries` riêng, KHÔNG sync sang `orders` mà Client Portal đọc → link không tới client; delivery cũng không tự tạo cho order thật.
- **Thay bằng**: section **"Bàn giao cho client"** trong Order drawer (`database-orders.js`) — Account nhập Preview/Final Link → set `orders.preview_link`/`final_delivery_link` + `delivery_status`/`production_status` + notify client (`delivery_preview`/`delivery_final`). Client Portal đọc cột order → thấy link + rating prompt.
- Bảng `deliveries` trong DB vẫn tồn tại nhưng không còn UI thao tác.

### 6. Reports

Status: Done — **wired LIVE Supabase 2026-06-12** (trước đó UI xong nhưng data rỗng chờ Phase 3)

- `loadReportsFromStore()`: load `orders.list()` + `tasks.list()` + `users.list()` song song qua `MH.store` → snapshot `RAW`. Supabase chưa enabled → empty state (không mock).
- 12 KPI cards + 6 charts (trend 14 ngày, donut task_type, role bars, PIC stacked, heatmap thứ-trong-tuần, quality grid) + Delivery Funnel 7 bước (Tiếp nhận→Brief xác nhận→Sản xuất→Preview→Feedback→Final→Đánh giá) + rating distribution + SLA — tất cả aggregate từ data thật trong `computeReportData()`.
- 5 filters (period/role/PIC/branch/type) **recompute client-side thật** từ RAW, không refetch; `#filter-pic` options rebuild từ PIC thật trong data. Role suy từ task_type (`ROLE_OF_TASK_TYPE`).
- 3 tables: PIC KPI (composite score 60% on-time + 25% rating + 15% ít revision, sort desc) · Overdue & Risk (open task trễ hoặc còn ≤2 ngày, link `production-board.html?id=`) · Feedback top 6 rating mới nhất.
- Empty-state mọi chart/table khi 0 data — đồng thời fix crash sẵn có (`renderFunnel` đọc `FUNNEL[0].count` trên mảng rỗng làm chết init từ đó trở xuống, export buttons không gắn listener).
- Đọc an toàn cột thuộc migration pending (`revision_round`, `production_pic_video/photo`): undefined → 0/skip, không lỗi.
- Poll 60s + reload khi `visibilitychange` visible. CSV export + print PDF giữ nguyên, giờ xuất số liệu thật.

### 7. User Management

Status: Done

- Summary cards, filters, user table.
- Detail drawer: profile, role/permission, module access, assigned work, activity, security.
- Create/Edit modal with validation and permission preview.
- Safeguards: unique email, cannot deactivate the only admin, warns on open tasks.
- 14 mock users. CSV export.

### 8. Settings

Status: Done

- 12 panels: General, Workflow Status, SLA, Notifications, Brand, AI Tools, Chatbot, Files & Drive, Departments, Client Portal, Reports & Export, Security.
- Per-panel Save/Cancel, localStorage persistence to `mh-settings`.
- Test connection mocks, reset defaults, export JSON.
- Settings Activity Log in `mh-settings-activity`.

### 9. AI Tools

Status: Done

- Built from spec 09.
- Category tabs, search, tool cards, workspace form, CB brand preset, output panel, usage log.
- 13 mini apps: Post Generator, Ads Copy, Caption Builder, Brief Optimizer, Missing Info Checker, Visual Prompt, Video Concept, Slide Outline, Campaign Idea, Hashtag/CTA, Tone Adjuster, Summarizer, **AI Voice (Supertonic)**.
- **AI Voice tool** (new): on-device TTS — engine target `Supertonic` (github.com/supertone-inc/supertonic, ONNX runtime, ~99M params, 44.1kHz). Demo runtime dùng Web Speech API. UI clone preset M1-M5/F1-F5, 16 ngôn ngữ, expression tags `[laugh]/[breath]/[sigh]/...`, quality steps. Có audio player (Play/Pause/Stop), waveform animation, export SSML, link repo.
- Role permission: admin/account/design/editor; client blocked.
- Mock generation with CB guardrails, copy, regenerate, export markdown, feedback, save demo.
- Demo persistence: `mh-ai-usage-log`, `mh-ai-saved-outputs`.

### 10. Chatbot

Status: Done

- Built from spec 10.
- Dedicated page: suggested prompts by role, context panel, chat thread, safe actions, feedback, history clear.
- Floating CB Assistant widget injected on internal/public pages when user session exists.
- Supports process guidance, order/task status lookup demo, brief missing-info, content-gen handoff to AI Tools, navigation help.
- Role permission simulated for admin/account/design/editor/client.
- Demo persistence: `mh-chatbot-history`, `mh-chatbot-feedback`.

### 11. Client Portal

Status: Done

- Built from spec 11.
- `client-dashboard.html` + `assets/client-dashboard.js`.
- Auth guard: `role === 'client'` only; admin/staff redirected to `dashboard.html`.
- Sections: greeting strip, order status summary cards, order list table, order detail drawer, notification panel, profile view.
- Order detail drawer: order info, status timeline, deliverables, feedback.
- Profile tab: view/edit name, initials, title, avatar, phone, department, bio — persists to `mh-user`.
- Demo account: `client@cb.vn` / `cb2026` → redirect to `client-dashboard.html`.
- **Status label client (2026-06-12)**: `pending` (vừa gửi) = **"Yêu cầu đã gửi"** · `checking` (account bấm "Kiểm tra brief") = **"Đã nhận yêu cầu"** (trước: pending="Đã nhận yêu cầu", checking="Đang kiểm tra thông tin"). Đồng bộ cả `client-dashboard.js PUB_STATUS` + `tracking.html STATUS_PUB`. (TL_STAGES node 1 vẫn gộp "Đã nhận" pending+checking — milestone thô, không đổi.)

### 12. Calendar / Lịch

Status: Done (2026-06-08)

- `calendar.html` + `assets/calendar.js` (clone cấu trúc `task-dashboard.html`). Internal page; client → redirect `client-dashboard.html`.
- **3 view**: Month (mặc định, lưới 7 cột Monday-first giống ref) · Week · **Day**. Nav ‹ › + Hôm nay + toggle view + lọc loại sự kiện.
- **Day view (ref ClickUp "Date", 2026-06-08)**: bấm **"+N khác"** hoặc **click số ngày** ở Month → mở Day view = **mini-calendar trái** (`‹ ›` đổi tháng, chấm ngày có event, today/selected highlight) + **panel phải danh sách ĐẦY ĐỦ task của ngày** (mỗi dòng: dot loại + giờ + tiêu đề + loại + tên & avatar PIC). Toolbar `‹ ›` ở Day view dịch ±1 ngày; click dòng → popover → mở drawer. State `selectedDay`/`miniCursor`; `gotoDay(d)`. (Agenda cũ bỏ — Day thay thế.)
- **4 loại event** chấm theo ngày, màu theo brand token: `task-deadline` (navy, `tasks.internal_deadline`) · `order-deadline` (amber, `orders.requested_deadline`→fallback `internal_deadline`) · `shoot` (đỏ, lịch quay/chụp) · `delivery` (green, `orders.delivery_date`/final).
- **Role filter** (yêu cầu nghiệp vụ): admin/account = FULL (mọi task+order) · design/editor = CHỈ task `isMyTask(assigned_to)` (deadline+shoot của task mình, không xem order-level) · content = order đang wording active.
- **Read-only**: click event → popover (ngày/giờ/PIC/trạng thái) → "Mở chi tiết" navigate `production-board.html?id=` (task) / `database-orders.html?id=` (order admin/account) / `content-workbench.html?order=` (content). Task vẫn tạo/sửa ở Task Tracker.
- **Shoot date**: cột mới `orders/tasks.shoot_date`+`shoot_time` (migration `add-shoot-date.sql` ⚠ pending). `order-form.js` lưu `media_date`→`shoot_date`; `database-orders.js pushToProduction` kế thừa sang task quay/chụp. calendar.js fallback regex bóc `Ngày: YYYY-MM-DD` từ content_brief khi chưa migrate.
- Nav "Lịch" inject 1 chỗ qua `app.js injectCalendarNav()` → hiện trên mọi trang nội bộ. **"Hôm nay" = `new Date()` ngày THẬT** (mặc định mở tháng hiện tại). **Đồng bộ realtime**: Supabase Realtime orders/tasks (best-effort) + poll 60s + reload khi quay lại tab (silent, giữ view/filter). CSS `.cal-*` cuối `styles.css`.

### 13. Content Team Workspace

Status: Done (2026-06-11)

- `content-team.html` + `assets/content-team.js` — workspace RIÊNG cho team Content, TÁCH BIỆT Production/Task Tracker (không tạo production task cho việc wording, workload không trộn).
- **2 role team Content**: `lead_content` (nhận request ở Content Inbox, gán PIC, đặt hạn wording, duyệt/trả chỉnh) + `content` (làm wording được gán, lưu nháp, gửi Lead). Admin = full; Account = theo dõi read-only (action của Account vẫn ở Client Orders drawer); design/editor/client = chặn (redirect).
- **5 view** (chip tab, role-gated JS): Dashboard (7 stat cards + workload bar theo PIC + danh sách trễ hạn + hoạt động gần đây) · Content Inbox (Lead: 2 cột "chờ phân công" + "chờ duyệt") · Content Board (Kanban 7 cột: Inbox/Đã gán PIC/Đang viết/Chờ Lead duyệt/Cần chỉnh/Chờ Account-Client/Hoàn tất, click card → drawer, KHÔNG drag-drop — đổi status qua action có gate) · Danh sách (search + filter status/type/PIC) · My Content Tasks (PIC match kiểu `isMyTask` word-boundary).
- **Content Wording Drawer**: Lifecycle 7 bước → Lead Assign panel (PIC datalist từ users role=content + hạn wording) → Brief gốc (read-only) → Wording Workspace (7 field, 2 bắt buộc) → Content Quality Checklist (7 mục, bắt buộc tick đủ trước khi gửi Lead) → Lead Review Panel (note + Duyệt & chuyển Account / Trả Content chỉnh) → Account & Client status (read-only) → Files/Links workspace (4 link + nút Mở) → Activity timeline.
- **Status flow mở rộng `brief_wording_status`**: `assigned` (Inbox Lead) → `pic_assigned` → `in_progress` → `submitted_to_lead` → (`lead_revision` ↺) → `submitted_to_account` → flow Account/Client CŨ GIỮ NGUYÊN (`account_revision`/`sent_to_client`/`client_feedback`/`client_approved`/`completed`). Gate Confirm Brief (`isWordingApproved`) không đổi.
- **Notify khép kín**: Account transfer → mọi `lead_content` active (fallback `content` nếu chưa có lead) · Lead gán PIC → PIC (`findUserIdByName`) · Content submit → mọi `lead_content` · Lead duyệt → admin+account · Client wording feedback → admin+account+lead_content+content (link `content-team.html?id=`).
- **Persist**: localStorage-first (cache CHUNG `mh-wording-drafts` với content-workbench — draft/checklist/links/activity share; Supabase off → ghi `mh-submitted-orders`); Supabase on → RPC `update_brief_wording` v2. DB là nguồn thật cho lifecycle, chỉ override khi DB có giá trị (cột chưa migrate không clobber cache). Poll 60s + reload on visibilitychange.
- **Sidebar nhóm "Content Team" 2 sub (2026-06-11)**: h6 riêng sau "Vận hành" — **Content Workspace** (`content-team.html`, admin/account/**lead_content**) + **Content Wording** (`content-workbench.html`, admin/account/**content**). Inject qua `app.js injectContentTeamGroup()` (thay 2 inject cũ); CSS override `:has(a[href="content-team.html"])` ẩn Workspace khỏi role content (substring trap).
- **Phân vai trang**: Lead làm ở Workspace; **Content làm ở Content Wording** — `content-workbench.js` nâng cấp: WS_CONTENT_EDITABLE thêm `pic_assigned`/`lead_revision`, nút **"Gửi Lead Content duyệt"** (`submitted_to_lead` + notify lead) thay gửi thẳng Account (hết bypass), LIFE bước "Lead Review". Account vẫn thao tác `submitted_to_account` ở đó/Client Orders.
- **Guard 2 chiều**: content/lead_content → chặn `production-board` + `database-orders` + `task-dashboard` (**redirect theo role**: lead→content-team, content→content-workbench); role content vào content-team → redirect content-workbench (giữ `?id=`); nav Master/Task Dashboard/Task Tracker ẩn qua CSS `:has` (thêm rules cho `lead_content`). Client KHÔNG thấy Content Board/note nội bộ (`wording_lead_note` không map sang client adapter).
- ⚠ **Migration `supabase/add-content-team.sql`** (sau `add-content-role.sql`): role check + status CHECK + 4 cột lead + RLS lead_content + RPC v2. Login redirect: `lead_content` → `content-team.html` · `content` → `content-workbench.html`. Tạo user lead qua Supabase Auth metadata `{name, role:'lead_content'}`.

### 14. Supervisor Planning

Status: Done (2026-06-17)

- `supervisor-planning.html` + `assets/supervisor-planning.js` — module **TÁCH BIỆT** (sidebar **"Strategy Board"**): System Supervisor (+ Admin) lập + giao **kế hoạch nội bộ** cho 2 nhóm Lead (Media / Content / **cả 2**). KHÔNG dính `tasks` (production) hay flow wording → planning không lẫn với task design/editor.
- **Bảng mới `lead_tasks`**: `id` · `title`* · `description` · `assigned_lead` (`lead_media`/`lead_content`/**`both`**) · `status` (tổng hợp/derived) · **`lead_status` jsonb** (tiến độ RIÊNG từng Lead, vd `{lead_media:'in_progress',lead_content:'pending'}`) · **`checklist` jsonb** (đầu việc → progress %) · `priority` · `deadline` · `attachment_path`/`name`/`url` · `created_by` · `created_at`/`updated_at`. Bảng **`lead_task_comments`** (thread 2 chiều + activity: kind comment/status/system).
- **Tiến độ RIÊNG từng Lead**: plan `both` theo dõi Media & Content độc lập (Lead chỉ sửa phần mình; Supervisor sửa cả 2 + Lưu trữ). `status` tổng hợp = derived (all completed→completed; any active→in_progress; archived sticky).
- **Đính kèm kế hoạch**: upload PDF lên **bucket Storage `plan-files`** (path `{plan_id}/{ts}-{file}`, signed URL khi tải) HOẶC dán link (Drive/PDF). Demo (Supabase tắt) → chỉ link. Storage RLS: supervisor/admin upload+xóa; Lead chỉ tải file plan thuộc bucket mình (join `lead_tasks`).
- **Role mới `lead_media`** (song song `lead_content`): login redirect → `supervisor-planning.html`; badge teal `r--lead_media`/`rt--lead_media`; `app.js roleLabel`="Lead Media" + `ROLES` += lead_media; user-management ROLE_LABEL + defaultPermByRole + 2 dropdown. **Guard `production-board.js`** redirect lead_media → supervisor-planning (tách khỏi task sản xuất, như lead_content). Nav Production (Master/Task Dashboard/Task Tracker) ẩn cho lead_media qua CSS `:has`.
- **Data layer**: `data-store.js` namespace `leadTasks` (`list`/`get`/`upsert`/`update`/`delete`) + **`leadTaskComments`** (`byPlan`/`add`); Supabase bảng `lead_tasks`/`lead_task_comments`, fallback `MH_MOCK_LEAD_TASKS`/localStorage (`mh-lead-tasks`, `mh-lead-task-comments`). Seed demo 4 plan (gồm 1 `both` + checklist + attachment link). Upload file qua `files.upload('plan-files')`, tải `signedUrl`.
- **Drawer Workspace cộng tác** (B): **lane card từng Lead** (tiến độ + cổng duyệt) · **checklist** (tick → progress %, supervisor thêm/xóa item) · **timeline trao đổi** (comment 2 chiều + log status/submit/review theo thời gian) · attachment. Lead chỉ thao tác lane mình + comment + tick checklist; Supervisor sửa tất cả.
- **Cổng duyệt per-lane (review gate)**: Lead **"Nộp duyệt"** → form (Kế hoạch/Sản phẩm + link/upload file + note) → lane `submitted` (hồ sơ lưu `lead_submissions` jsonb). Supervisor thấy hồ sơ → **Duyệt** (`completed`) / **Trả chỉnh** (`revision` + lý do, notify Lead) → Lead "Nộp lại" (round++). **Lead KHÔNG tự "Hoàn thành"** (`LEAD_MANUAL`=pending/in_progress; completed = Supervisor duyệt). `displayOverall` hiện "Chờ duyệt"/"Cần chỉnh"; Board cột "Chờ duyệt". Plan `both` chỉ Hoàn thành khi CẢ 2 lane được duyệt.
- **Lead chủ động đề xuất (bottom-up)**: Lead "Đề xuất kế hoạch" cho **bucket chính mình** (chịu trách nhiệm chính) + tùy chọn **mention Lead kia "nắm thông tin"** (read-only, không chịu trách nhiệm). `origin=lead`/`approval=proposed` → Supervisor **Duyệt** (`approved`→plan chính thức, chạy luồng bình thường) / **Từ chối** (`declined`+lý do). Cột `origin`/`approval`/`informed_leads`; badge "Đề xuất"/"Từ chối"; banner drawer + nút Duyệt/Từ chối; stat "Đề xuất chờ duyệt"; RLS lead INSERT (proposed+bucket mình) + informed SELECT; informed Lead bị `canParticipate` gate (ẩn comment/control, chỉ xem). Notify: đề xuất→Supervisor (+mention→Lead kia), duyệt/từ chối→Lead.
- **UI**: stats (Tổng/Media/Content/**Cả 2**/Hoàn thành/Trễ) · **List + Board kanban** (toggle; board read-only cột theo status tổng hợp, click→drawer) · form tạo (file/link) · filters · bảng có **per-lead chips** (plan `both`) + mini progress bar.
- **Notify 2 chiều** (C, type base-safe): tạo/reassign → Lead (`task_assigned`); Lead đổi status → Supervisor+Admin (`task_status_changed`); comment → chiều còn lại (`task_comment`); link `?id=` deep-link mở drawer. **Realtime** subscribe `lead_tasks`+`lead_task_comments` + nhắc overdue 1 lần.
- **Phân quyền vào trang**: admin/system_supervisor/lead_media/lead_content; role khác → redirect (toast). Lead lọc client-side về bucket mình incl `both` (`leadCanSee`); RLS chặn server-side.
- **Nav** "Strategy Board" inject qua `app.js injectPlanningNav()` (data-show-roles `system_supervisor,lead_media,lead_content,admin`); CSS reveal `lead_media` + **override ẩn cho `content`** (substring "lead_content"⊃"content").
- ⚠ **Migration `supabase/add-supervisor-planning.sql`** (sau `rls.sql` + `add-system-supervisor.sql`): `users_role_check` += `lead_media` + bảng `lead_tasks` (assigned_lead incl `both` + `lead_status`/`checklist`/`lead_submissions` jsonb + `origin`/`approval`/`informed_leads` + attachment cols) + bảng **`lead_task_comments`** (kind +submit/review) + RLS **lead INSERT đề xuất** + **informed SELECT** + index + trigger + **bucket Storage `plan-files`** + RLS 2 bảng (supervisor/admin **ALL**; lead **SELECT/UPDATE/DELETE** plan bucket mình incl `both`, comment SELECT+INSERT, **KHÔNG INSERT plan**) + storage policy + **Realtime publication** (lead_tasks + lead_task_comments). Tạo user qua Supabase Auth metadata `{name, role:'lead_media'}`.
- ⚠ **Lưu ý invariant**: `system_supervisor` vốn read-only toàn hệ thống — module này CỐ Ý cho supervisor GHI trên bảng `lead_tasks` cô lập (không nới quyền supervisor ở module khác).

---

## Pending Modules

No pending MVP modules. Remaining work is production integration:

- Replace mock data with backend APIs (orders, tasks, deliveries, users).
- Implement real auth and password validation.
- Implement real upload/storage (avatar, assets, deliverables).
- Replace simulated notification, test-connection, and AI generation flows.
- Replace `window.print()` PDF export if server-side export is needed.

---

## File Inventory

Build total: **18 HTML pages · 15 JS files · 1 CSS file · 1 logo asset · Supabase SQL migrations**. (Delivery Log page removed 2026-06-03; Calendar/Lịch added 2026-06-08; Content Team Workspace added 2026-06-11.)

New files 2026-06-11 (Content Team Workspace):
- `content-team.html` — Content Team Workspace (Dashboard/Inbox/Board/List/My Tasks + drawer)
- `assets/content-team.js` — toàn bộ logic team Content (roles, views, drawer, lead review, notify)
- `supabase/add-content-team.sql` — role lead_content + 3 status mới + cột lead review + RLS + RPC v2 (⚠ pending)

New files 2026-06-08 (Calendar / Lịch):
- `calendar.html` — Lịch / Calendar internal page (Month/Week/Day, role-filtered)
- `assets/calendar.js` — calendar logic, event build + role filter, popover→drawer
- `supabase/add-shoot-date.sql` — orders/tasks.shoot_date + shoot_time (⚠ pending)

New file 2026-06-02:
- `assets/notif-icons.js` — Shared notification icon module (`window.MH.notifIcons`), single source of truth dùng bởi app.js bell + client-dashboard.js panel. Load trước app.js trên 12 page có chuông/panel.

New files 2026-05-18:
- `order-dashboard.html` — Order-level KPI dashboard
- `task-dashboard.html` — Task-level / internal production KPI dashboard
- `assets/config.js` — Runtime config (Sentry DSN + Supabase URL/KEY + feature flags)
- `assets/supabase-client.js` — Supabase SDK loader via esm.sh CDN (zero-build)
- `assets/data-store.js` — Abstraction layer `window.MH.store` với 10 namespaces
- `supabase/schema.sql`, `seed.sql`, `rls.sql`, `storage.sql`, `clear-demo.sql` — DB setup migrations
- `robots.txt`, `.env.example` — Phase 0 hardening

New files 2026-05-20:
- `supabase/add-notifications.sql` — notifications table + trigger
- `supabase/enable-realtime.sql` — ALTER PUBLICATION add notifications
- `supabase/add-cancel-fields.sql` — cancel_reason / cancel_cause / cancelled_by / cancelled_at + notifications.type extend với `order_cancelled` + `order_new`

| File | KB |
|---|---:|
| `assets/styles.css` | 238.2 |
| `assets/production-board.js` | 73.4 |
| `request.html` | 64.3 |
| `assets/database-orders.js` | 67.4 |
| `dashboard.html` | 55.0 |
| `order-dashboard.html` | 50.9 |
| `settings.html` | 49.1 |
| `task-dashboard.html` | 46.4 |
| `assets/ai-tools.js` | 45.7 |
| `assets/user-management.js` | 42.8 |
| `assets/client-dashboard.js` | 36.5 |
| `production-board.html` | 34.3 |
| `client-dashboard.html` | 30.6 |
| `tracking.html` | 29.2 |
| `reports.html` | 27.3 |
| `assets/order-form.js` | 26.3 |
| `user-management.html` | 25.0 |
| `assets/app.js` | 24.3 |
| `assets/settings.js` | 23.3 |
| `database-orders.html` | 23.3 |
| `assets/chatbot.js` | 21.0 |
| `assets/reports.js` | 20.7 |
| `index.html` | 17.8 |
| `ai-tools.html` | 15.7 |
| `help.html` | 14.9 |
| `login.html` | 14.8 |
| `assets/logo.png` | 13.0 |
| `chatbot.html` | 10.9 |
| `assets/hero/cb_character_cutout.png` | 178.7 |
| `assets/hero/icon-document-pencil.png` | 29.3 |
| `assets/hero/icon-image.png` | 10.3 |
| `assets/hero/icon-bell.png` | 9.0 |

---

## Changelog

| Date | Module | Action |
|---|---|---|
| 2026-06-18 | **Media Lead = Trưởng nhóm Production (mở quyền)** | Role `lead_media` từ "chỉ Strategy Board" → **quyền vận hành NGANG Account**: Client Orders / Internal Task Tracker / 3 Dashboards / Reports / Order Form. **KHÔNG** Content Team (Workspace+Wording), **KHÔNG** Settings/User Mgmt. **Cơ chế**: (1) RLS `supabase/add-media-lead-production.sql` — CREATE OR REPLACE `is_staff()` + `is_admin_or_account()` thêm `lead_media` (mọi policy đang dùng 2 helper tự áp dụng). (2) Frontend **alias `lead_media`→`account`** trong `production-board.js` + `database-orders.js` (đặt SAU `data-user-role`+chip → mọi check `['admin','account']` pass; data-user-role giữ 'lead_media' thật cho CSS; RLS DB thấy role thật). (3) Guard allow-list += lead_media (`database-orders.js`, `reports.js`); **bỏ redirect** lead_media ở `production-board.js`. (4) `login.html roleHome` lead_media → `dashboard.html`. (5) CSS: bỏ rule ẩn nav Production cho lead_media; reveal nav admin/account-only (Orders Dashboard/Client Orders/Reports/Order Form) theo `:has` + reveal `.dash-section/.kpi/.qa-btn/.dash-card[data-show-roles]`; **KHÔNG** reveal `.dash-nav li` chung → Content Team group vẫn ẩn (data-show-roles không chứa lead_media). (6) user-mgmt defaultPermByRole lead_media → manager·order_mgmt·all. order-form.js chỉ chặn design/editor → lead_media tạo order OK. ⚠ chạy `add-media-lead-production.sql` (sau `add-supervisor-planning.sql`). |
| 2026-06-17 | **Supervisor Planning (module mới #14) + role `lead_media`** | **Trang `supervisor-planning.html` + `supervisor-planning.js`**: System Supervisor (và Admin) lập + giao **kế hoạch nội bộ** cho 2 nhóm Lead (Media / Content), TÁCH BIỆT khỏi `tasks` (production) và flow wording. **Bảng mới `lead_tasks`** (id/title/description/assigned_lead/status/priority/deadline/created_by/created_at/updated_at). **Role mới `lead_media`** (song song `lead_content`): login redirect → `supervisor-planning.html`, badge teal `r--/rt--lead_media`, roleLabel "Lead Media", thêm vào `app.js ROLES` + user-management 2 dropdown + ROLE_LABEL + defaultPermByRole. **Data**: `data-store.js` namespace `leadTasks` (list/get/upsert/update/delete + fallback `MH_MOCK_LEAD_TASKS`/localStorage `mh-lead-tasks` + seed demo 3 plan khi Supabase tắt). **UI**: stats strip · form tạo (supervisor/admin) · filters (lead/status/search) · bảng (badge Media teal vs Content purple, status, priority, deadline tô đỏ khi trễ, người tạo) · drawer chi tiết. **Phân quyền**: vào trang = admin/system_supervisor/lead_media/lead_content; supervisor/admin sửa mọi field + xóa, Lead chỉ xem bucket của mình + cập nhật trạng thái. **Nav** "Kế hoạch" inject qua `app.js injectPlanningNav()` (data-show-roles=`system_supervisor,lead_media,lead_content,admin`; CSS reveal lead_media + override ẩn cho content do substring "lead_content"⊃"content"; ẩn nav Production cho lead_media như lead_content). **Guard** `production-board.js` redirect lead_media → supervisor-planning (tách khỏi task sản xuất). **DB** `supabase/add-supervisor-planning.sql` (⚠ pending): `users_role_check` += lead_media + bảng lead_tasks + trigger updated_at + RLS (supervisor/admin ALL; lead_media/lead_content SELECT/UPDATE/DELETE bucket mình, KHÔNG INSERT). Notify lead khi tạo (type base-safe `task_assigned`). **Refine cùng ngày (theo feedback)**: (1) `assigned_lead` thêm **`both`** = giao đồng thời CẢ 2 Lead (RLS + filter + badge gradient + notify cả 2 role; Lead thấy plan `both` của mình via `leadCanSee`); (2) **đính kèm file kế hoạch** — upload PDF lên Storage bucket **`plan-files`** (cột `attachment_path`/`attachment_name`) HOẶC ô **link** ngoài (`attachment_url`), Lead tải qua signed URL; storage RLS supervisor/admin upload, lead tải file plan mình; (3) sidebar đổi **"Kế hoạch" → "Strategy Board"**. **Nâng cấp A+B+C (cộng tác)**: (a) **tiến độ RIÊNG từng Lead** (`lead_status` jsonb; plan `both` theo dõi Media/Content độc lập; `status` = derived tổng hợp); (b) **Drawer workspace** — comment 2 chiều + activity timeline (bảng `lead_task_comments`, kind comment/status/system) + **checklist/progress %** (jsonb); (c) **notify 2 chiều** — Lead đổi status → báo Supervisor/Admin, comment → báo chiều còn lại, deep-link `?id=`; (d) **List + Board kanban** (read-only, cột theo status tổng hợp); (e) **Realtime** subscribe `lead_tasks`+`lead_task_comments` + nhắc overdue. **Cổng duyệt per-lane (review gate)**: Lead **"Nộp duyệt"** (loại Kế hoạch/Sản phẩm + link/file + note → lane `submitted`) → Supervisor **Duyệt** (lane `completed`) / **Trả chỉnh** (lane `revision` + lý do → Lead nộp lại, round++). Lead **KHÔNG tự set "Hoàn thành"** (lane completed chỉ khi Supervisor duyệt). Cột mới `lead_submissions` jsonb; per-lane state `submitted`/`revision` trong `lead_status` (jsonb, không cần đổi CHECK); `displayOverall` badge "Chờ duyệt"/"Cần chỉnh"; Board thêm cột "Chờ duyệt". Notify 2 chiều (nộp→Supervisor, duyệt/trả chỉnh→Lead). **Lead chủ động đề xuất kế hoạch (bottom-up)**: Lead "Đề xuất kế hoạch" (bucket chính mình, có thể **mention Lead kia "nắm thông tin"** read-only) → `origin=lead`,`approval=proposed` → Supervisor **Duyệt đề xuất** (`approved`, thành plan chính thức) / **Từ chối** (`declined`+lý do). Cột mới `origin`/`approval`/`informed_leads`; `displayOverall` badge "Đề xuất"/"Từ chối"; stat "Đề xuất chờ duyệt"; filter + banner drawer; RLS lead INSERT (proposed/bucket mình) + informed SELECT; informed Lead chỉ xem (canParticipate gate ẩn comment/control). Notify đề xuất→Supervisor (+ mention→Lead kia); duyệt/từ chối→Lead. **UI polish (skill ui-ux-pro-max, 2026-06-18)**: thay emoji structural (📋/👀/📎/↩/✓/×) bằng **inline SVG Lucide-style** (`ic()` helper, stroke 2 đồng nhất design system) ở banner đề xuất/informed + clip đính kèm + revision note + "đã duyệt" + checklist delete; banner đề xuất gọn (icon + title/subtitle 2 dòng). Toast `✓` giữ (transient, đồng bộ cả app). **19 HTML · 16 JS**. _(⚠ system_supervisor vốn read-only toàn hệ thống — module này cố ý cho supervisor GHI trên bảng lead_tasks cô lập; không nới quyền ở module khác.)_ |
| 2026-06-14 | Roles / Auth — role `system_supervisor` (Giám sát hệ thống) | **Thêm role GIÁM SÁT chỉ-đọc cấp hệ thống** (monitor-only, redirect `dashboard.html`). **Xem** toàn bộ Orders/Tasks/Content Team/Calendar/Reports (+ export); **mọi mutation khóa** (confirm/check/needinfo/push/assign PIC/đổi deadline/đổi status/drag kanban/tạo-sửa task/upload-gửi Preview-Final/duyệt wording/hủy đơn/CRUD user/sửa settings). **Frontend**: `app.js` roleLabel + ROLES + inject Content Team/Calendar nav (data-show-roles += system_supervisor); `user-management.js/html` ROLE_LABEL + defaultPermByRole(report_viewer/all) + 2 dropdown; guards mở read-only: `database-orders.js` (allow + `READONLY` const → kebab chỉ View Detail + `applyDrawerReadonly()` khóa input/ẩn nút + updateStepperState early-return), `production-board.js` (allow sẵn + `READONLY` → buildStatusActions=[] + updateStatus guard + composer disabled + create/edit/drag đã loại sẵn), `reports.js`/`calendar.js`(IS_FULL)/`content-team.js`/`content-workbench.js` (allow-list, action tự ẩn vì cờ role false). **CSS** `styles.css`: badge `.role-badge.r--system_supervisor`+`.rt--` (slate, kèm dark) + block reveal `[data-show-roles*="system_supervisor"]` + reveal nav admin,account-only (Orders Dashboard/Client Orders/Reports) qua `:has` theo href (Order Form CỐ Ý giữ ẩn). Sidebar ẩn: Order Form / User Management / Settings. **DB** `supabase/add-system-supervisor.sql` (⚠ pending): users_role_check += system_supervisor + helper `is_system_supervisor()` + SELECT-only policies orders/tasks/task_comments/users/deliveries/activity_log (KHÔNG đụng `is_staff()` vì nó gate cả write). Tạo user: Supabase Auth metadata `{name, role:'system_supervisor'}`. |
| 2026-06-05 | Wording Lifecycle (fix tick cuối) | **Client đã xác nhận nhưng bước cuối cycle chưa tick**. `client_approved` map `reached=5` → bước 6 ("Approved" / "Sẵn sàng Confirm Brief") render `active (số 6)` thay vì `done (✓)`. Client xác nhận = cycle wording xong → đổi `client_approved: 5 → 6` ở cả 2 drawer (`content-workbench.js LIFE_REACHED` + `database-orders.js reachedMap buildWordingWorkflow`) → toàn bộ 6 bước hiện tick. |
| 2026-06-05 | Order Drawer (Hủy đơn → footer) | **Dời nút "Hủy đơn" xuống đáy drawer**. Trước: nằm trong `.drawer-actions` (trên cùng, cạnh 4-step). Giờ: chuyển `.wf-actions-danger` ra **footer** (`<div class="drawer-foot wf-actions-danger">`) — sibling sau `.drawer-body`; vì `.drawer` là flex-column + `.drawer-body{flex:1;overflow:auto}` nên footer **luôn hiển thị ở đáy**, body cuộn phía trên. CSS `.drawer-foot` (border-top solid, bg surface-2, flex-shrink:0). JS `updateStepperState`: `.drawer-actions` chỉ còn hiện theo `briefStage\|\|pushStage` (bỏ canCancel); `danger`/`act-cancel` giữ selector cũ nên show/hide + handler không đổi. **Restyle nút**: style cũ scope `.drawer-actions .act-cancel` (gradient đỏ + chữ trắng) không còn khớp ở footer → đổi selector `.act-cancel` + style danger mới: nền đỏ nhạt `--accent-soft`, **icon + chữ đỏ brand `--accent`**, hover `--red-100` + viền đỏ. |
| 2026-06-05 | Order Drawer (Next Action UI) | **Khối "Hành động kế tiếp" lệch size + chữ mô tả chồng**. Root cause: `.order-next-action` là flex `space-between` → title (`<b>`) bên trái, mô tả (`<p>`) bị đẩy sang phải chen cạnh → trông như cỡ chữ lẫn + mô tả mờ chồng. Fix (theo skill ui-typography/design-audit): đổi sang **stack dọc** (`flex-direction:column`), title `text-lg → text-base` (16px, bước nhảy nhỏ so với body 14px), mô tả full-width dưới title (`max-width:640px`). Gỡ override mobile `display:grid` thừa. Hierarchy: label uppercase (text-sm) → title (text-base bold navy) → mô tả (text-sm muted). |
| 2026-06-05 | Client Portal (card align) | **Card "Đang xử lý" lệch hàng progress/PIC** do tên dự án dài/ngắn khác nhau → footer ở các độ cao khác nhau. Fix CSS (client-dashboard.html): `.co-card{height:100%}` (cao bằng nhau trong lưới grid stretch) + tên `.co-card-name` clamp 2 dòng (`-webkit-line-clamp:2` + `min-height:2.8em` reserve) cho đầu card đều + `.co-card-prog{margin-top:auto}` ghim progress+PIC+"Chi tiết" xuống **đáy** → thẳng hàng. JS thêm class `co-card-prog` + PIC fallback "—". Không đụng logic. |
| 2026-06-05 | Client Portal (label + ladder %) | **Đổi nhãn wording + cân bằng lại thang tiến độ %**. `PUB_STATUS.wording.label`: "Đang chuẩn hóa nội dung brief" → **"Chuẩn hóa brief"**. `PUB_PROGRESS` rà soát lại toàn bộ cho **tăng đều theo 6 mốc timeline, không tụt ngược**: pending 10→**5** (gửi đơn), checking 15→**10** (Account kiểm tra brief), needinfo 15→10, wording 18→15, confirmed 20→25, received 30→35, inprogress 50→55, revision 75→60, review 75→70, feedback_wait 90→80, feedback_fix giữ 85 (trước đây 85 < feedback_wait 90 → tụt ngược, nay 85 > 80 OK), ready/delivered/completed giữ 90/95/100. Giữ nhãn checking "Đang kiểm tra thông tin". KHÔNG đụng logic flow / thang % nội bộ. |
| 2026-06-05 | Content Workbench (bugfix + lifecycle UI) | **(1) Content gửi Account duyệt rồi vẫn bấm lại được**. Root cause: `buildActions`/fields gate theo `canEditWording` (role) chứ không theo pha wording → nút "Lưu nháp"/"Gửi Account duyệt" + textarea vẫn mở khi đã `submitted_to_account`. Fix: helper **`contentEditable(o)`** = `canEditWording && ws ∈ {none,assigned,in_progress,account_revision,client_feedback}`; áp cho buildActions (ẩn nút khi không editable, hiện tag **"Đã gửi Account — chờ duyệt"** cho Content), `ro`/checklist `disabled`, lockNote theo pha, + guard trong `saveDraft/startWork/submitToAccount` (chặn submit lại khi đã `submitted_to_account`). Nút account đổi sang `.btn-warning/.btn-info` (bỏ inline). **(2) Wording Lifecycle UI**: thiết kế lại stepper dạng **flow dọc có đường nối**, đổi màu **brand navy** (done = nền `--primary` + ✓ trắng; active = viền navy + glow `--primary-soft`; segment done navy) thay green/purple cũ — dùng chung cho cả Content drawer + Account Order Drawer (`.bw-steps`/`.bw-step`/`.bw-dot`). Thêm `.wf-wait-tag`. KHÔNG đổi logic flow. |
| 2026-06-05 | Client Portal (bugfix chuông) | **Bấm chuông thông báo bị nhảy sang tab "Thông báo"**. Root cause: `#notif-btn` bị bind 2 lần — `app.js buildDropdown` (mở dropdown, chuẩn dùng chung mọi `button[aria-label="Thông báo"]`) + `client-dashboard.js:1047` `switchTab('notif')` → 1 click chạy cả 2. Fix: gỡ handler `switchTab('notif')` trên chuông; chuông CHỈ mở dropdown, danh sách đầy đủ vẫn vào qua sidebar/tab "Thông báo". |
| 2026-06-05 | Drawers UI + flow (audit theo Skill design-audit/typography) | **Chuẩn hóa màu + cỡ chữ các khối callout trong 3 drawer + 1 fix flow**. Audit phát hiện: 3 drawer dùng 3 hệ style khác nhau + ~97 chỗ inline hardcoded `rgb(...)` soft-bg thay vì token `--*-soft`. Fix UI (targeted): thêm **component dùng chung `.dw-callout`** + tone `.dw--info/success/warning/brand/muted` (token-only: `--*-soft` bg + accent; cỡ chữ chuẩn: body `--text-sm`, meta `--text-xs`) trong `styles.css`; thêm `.btn-success/.btn-warning/.btn-info` (token, bỏ inline `#fff`). Refactor `client-dashboard.js` (action blocks needinfo/preview/approved/feedback/delivered + Action Center cards + Phase 4 wording section), `content-workbench.js` (client panel + revisionNote) sang `.dw-callout` — **0 chỗ `rgb()` hardcoded còn lại** ở 3 file drawer. `database-orders.js` đã class-based sẵn (không đổi). **Fix flow A1**: client thấy raw "wording" (0%, timeline trống) suốt giai đoạn wording vì `PUB_STATUS`/`PUB_PROGRESS`/`TL_STAGES` thiếu key `wording` → thêm `wording: {label:'Đang chuẩn hóa nội dung brief', cls:'s--wording'}`, progress 18, vào stage "Đã nhận". KHÔNG đổi logic flow (gate/RPC/Phase 4 nguyên vẹn). |
| 2026-06-05 | Order Form (bugfix) | **Checkbox xác nhận "yêu cầu chỉnh sửa/phát sinh sau 03 vòng feedback" lọt sang order MỚI**. Markup `#revision-confirm-wrap` đã có `hidden` + `initRevisionMode()` chỉ unhide ở `?mode=revision`, nhưng `.checkbox{display:inline-flex}` (styles.css:479) **đè** thuộc tính `[hidden]` (cùng specificity, author thắng UA) → vẫn hiện ở order mới. Fix: thêm `.checkbox[hidden],.radio[hidden]{display:none!important}` (cùng pattern `.wf-hint[hidden]` sẵn có). Order mới: ẩn hẳn, không tính section/không chặn submit; order revision: vẫn hiện + required như cũ. |
| 2026-06-05 | Roles/RLS — enable `content` user (backend) | **Mở đường cho user role `content` thật + ghi wording an toàn qua RPC**. Trước đó: `users.role` CHECK (schema.sql) thiếu `content` → trigger `handle_new_auth_user` fail → KHÔNG tạo được content user; RLS `is_staff()` thiếu content + `orders` write = admin/account → content không đọc/ghi được wording; UI "Tạo user" là MOCK (`USERS.push`). **Migration mới `supabase/add-content-role.sql`** (idempotent): (1) `users_role_check` thêm `content`; (2) policy **`orders content read`** (content CHỈ SELECT orders, KHÔNG đụng tasks/deliveries/settings); (3) **RPC `update_brief_wording(p_order_id, p_patch jsonb)`** SECURITY DEFINER — đường GHI orders DUY NHẤT cho content/client, whitelist cột wording + validate status theo nhánh: internal (admin/account/content) ghi workspace + status nội bộ (assigned…sent_to_client); chủ order (client) chỉ client_approved/client_feedback + field phản hồi. **KHÔNG cấp UPDATE orders trực tiếp cho content/client.** Frontend wiring: `data-store.js` thêm `orders.updateWording()` (gọi rpc); `content-workbench.js` `persist()` + `client-dashboard.js` `approveWording`/wording-feedback submit → đổi sang `updateWording` (giữ fallback localStorage). `database-orders.js sendWordingToClient` giữ direct (account có UPDATE sẵn). **Tạo user**: làm thủ công ở Supabase Auth (Add user + metadata `{name, role:'content'}` + password) — trigger tự tạo `public.users` row (cần A1 trước). ⚠️ Chạy `add-content-role.sql` SAU 3 migration wording. Client rating persist (`satisfaction_score`) có cùng gap "no orders self update" — cần verify/RPC sau (ngoài scope). |
| 2026-06-05 | Client Portal (bugfix Timeline/Status) | **Timeline + nhãn trạng thái + % không nhảy khi order đã bàn giao/đánh giá** (vd order đã rating 3★ nhưng timeline vẫn kẹt "Kiểm tra nội bộ" 90%, status "Chờ phản hồi từ bạn"). Root cause: `pubStatus`/`pubProgress`/`tlHtml` key theo `o.status` thô (production_status). Fix: helper **`pubEffStatus(o)`** — cancelled→cancelled; có `rating` hoặc status completed→**completed** (Hoàn thành 100%); `isFinalDelivered`→**delivered** (Đã bàn giao 95%); còn lại giữ `o.status`. `pubStatus`/`pubProgress`/timeline drawer dùng `pubEffStatus`. **`isFinalDelivered` thêm `o.rating`** (đã đánh giá ⟹ đã bàn giao) → khối drawer + Action Center + bảng đồng bộ. Sửa wording khối "đã gửi feedback Vòng 3": vòng cuối ghi "sẽ **bàn giao bản Final**" thay vì "gửi bản Preview tiếp theo". KHÔNG đổi schema. |
| 2026-06-05 | Client Portal (bugfix Final + Rating) | **(1) Client không thấy bảng sau khi Account gửi Final** → không có nút "Tạo Order mới" khi quá 3 vòng. Root cause: mọi nơi phát hiện "đã bàn giao Final" gate theo `o.status==='delivered'` (= production_status); nếu order kẹt `feedback_wait`/final set qua đường khác thì rơi vào nhánh Preview feedback. Fix: helper **`isFinalDelivered(o)`** = có `final_link` **hoặc** status delivered/completed **hoặc** `feedback_status==='final_sent'`; áp dụng cho Action Center (rating card), drawer (nhánh delivered thắng `feedback_wait`), bảng orders (rating cell), filter (active/feedback/delivered), KPI (Đã bàn giao/Chưa đánh giá); `shouldShowPreviewAction` trả false khi đã có Final. **(2) Rating bị trùng** (đánh giá rồi bấm lại vẫn mở modal đè được). Fix: `openRatingModal` **guard** — nếu `state.ratings[id]` hoặc `o.rating` đã có → toast "Đã đánh giá" + return; chuyển cập nhật `o.rating`/`__raw.satisfaction_score` ra **ngoài** block `supabaseEnabled` để chống trùng cả khi Supabase tắt. KHÔNG đổi schema. |
| 2026-06-05 | Client Portal + Order Drawer + Content Workbench (Phase 4) | **Client xác nhận Brief Wording (Account → Client → Account/Content)**. Cho phép Account gửi bản wording cho Client xác nhận trong Client Portal; Client **Xác nhận brief** (→ unlock Confirm Brief) hoặc **Yêu cầu chỉnh brief** (→ về Content Workbench chỉnh). **`database-orders.js`**: `buildWordingWorkflow` thêm hàng **"Xác nhận Client"** + nút **"Gửi Client xác nhận Brief"** (`#act-send-wording-client`, enable khi `brief_wording_status='submitted_to_account'` && `wording_brief` không rỗng) → `sendWordingToClient()` set `sent_to_client`+`wording_client_sent_at/by` + `notifyClient` (requester_id/email fallback); note theo status + soft-warning vòng ≥3; wire nút trong openDrawer. **`client-dashboard.js`** + **`client-dashboard.html`**: adapter map field wording (KHÔNG map internal note `wording_account_note`/`wording_production_note`); order drawer thêm section **"Brief đã được chuẩn hóa"** (hiện khi `sent_to_client`/`client_feedback`/`client_approved`/`completed`) show Brief wording/Mục tiêu/Thông điệp/Thông tin bắt buộc/Tone&style/CTA/Vòng + 2 nút **Xác nhận brief** (`approve-wording`→confirm dialog→`client_approved`+`wording_approved_at/by`+notify Account/Admin) / **Yêu cầu chỉnh brief** (`wording-feedback`→modal `#wording-feedback-modal` field Nội dung*/Lý do/Link→`client_feedback`+`brief_wording_round++`+`wording_client_feedback/at`+notify admin/account/content); helper `esc`/`fmtDT`. **`content-workbench.js`**: fix bug `o.client_feedback`→`o.wording_client_feedback`; clientPanel show **Client Feedback Round N** + nội dung + timestamp (+client_approved/sent_to_client states); cho Content **revise từ `client_feedback`** (nút "Chỉnh theo Client" + saveDraft/startWork nhận `client_feedback`→`in_progress`); accountSendToClient lưu `wording_client_sent_at/by`. **Gate Phase 2 giữ nguyên** (Confirm Brief vẫn khóa tới khi `client_approved`/`completed` — Test 4/5 pass sẵn). `styles.css`: `.bw-note.bw-warn`. Migration **`supabase/add-brief-wording-confirmation.sql`** (wording_client_sent_at/by, wording_client_feedback/at, wording_approved_at/by — idempotent). Wording feedback rounds **TÁCH RIÊNG** khỏi production Preview/Final feedback rounds; Client KHÔNG thấy Content Workbench/Task Tracker/internal note; Delivery Log vẫn removed. ⚠️ Cần chạy migration để wording persist cross-user (localStorage cache chỉ same-browser). |
| 2026-06-05 | Content Workbench (Phase 3) | **Build Content Wording Workbench + Drawer thật** (thay placeholder Phase 1). **`content-workbench.html`** rebuild: page head, **7 summary cards** (theo brief_wording_status), filters (search/status/type/priority/dept), bảng order cần wording (`.table-card`/`.data-table`), **Content Wording Drawer** (`#wording-drawer`). **`assets/content-workbench.js`** (mới): guard admin/account/content; load `orders.list()` lọc engaged-wording + overlay **localStorage cache** (`mh-wording-drafts`) để draft persist khi reload kể cả chưa migrate; Drawer gồm Wording Lifecycle (6 bước, Client Confirmation=Phase 4 pending), **Brief gốc read-only**, **Wording Workspace** 7 field (Brief đã wording*, Mục tiêu, Thông điệp chính*, Thông tin bắt buộc, Tone&style, CTA, Ghi chú Production), **Checklist 7 mục**, Account Review panel, Client placeholder, Files/Links (4), Activity. Actions: Content **Lưu nháp / Bắt đầu xử lý / Gửi Account duyệt** (disabled tới khi đủ field* + checklist → `submitted_to_account`, `wording_submitted_by/at`); Account/Admin **Trả Content chỉnh** (bắt buộc note → `account_revision`) / **Gửi Client xác nhận** (`sent_to_client`, UI client để Phase 4). Role matrix: Account read-only workspace, Content/Admin edit. Persist: cache + `orders.update` best-effort (generic PGRST204 retry bỏ cột thiếu). **`database-orders.js`**: block Brief Wording Workflow thêm **summary** (PIC Content/Vòng/Gửi Account lúc/Brief đã wording preview/Ghi chú Account) + nút **"Mở Content Wording"** → `content-workbench.html?id=`. `styles.css`: `.cwb-*`. Migration **`supabase/add-brief-wording-workspace-fields.sql`** (14 cột wording_*, optional — fallback localStorage). KHÔNG build Client confirm (Phase 4) / notifications (Phase 5); production/preview/final/Delivery Log không đổi. |
| 2026-06-05 | Client Orders (Phase 2 Wording Gate) | **Cổng bắt buộc Content Wording trước Confirm Brief & Push Production**. Order phải qua Content Wording (Client xác nhận) trước khi Account Confirm Brief. `database-orders.js`: thêm helper `wordingStatusOf`/`isWordingApproved` + `WORDING_STATUS_LABEL` + `account_status='wording'` (label/lifecycle/next-action). Nút mới **"Chuyển Content Wording"** (`act-wording`, drawer action bar) → `transferToWording()` set `account_status='wording'`, `brief_wording_status='assigned'`, `brief_wording_round=1`, `wording_last_updated_at`. **Gates**: (1) `updateStatus` hard-gate — chặn `confirmed` nếu `brief_wording_status` chưa ∈ {client_approved, completed} (cover cả drawer + kebab); (2) `updateStepperState` ẩn/hiện contextual + **disable Confirm Brief** tới khi wording approved, disable nút Wording sau khi đã chuyển; (3) `pushToProduction` + `buildPushCheck` thêm điều kiện wording approved → không tạo task nếu chưa. **Brief Wording Workflow block** trong drawer body (6 bước done/active/pending + status label). `database-orders.html`: nút act-wording. `styles.css`: `.act-step--wording`, `.bw-*`, `.tb-status.s--wording`. **Migration** `supabase/add-brief-wording-fields.sql` (5 cột brief_wording_* + mở rộng CHECK account_status thêm 'wording' + CHECK brief_wording_status + **grandfather**: order đã confirmed/pushed → `brief_wording_status='completed'` để không chặn flow cũ). Field thiếu → coi như 'none' (mock/localStorage an toàn). KHÔNG build Content drawer / Client confirm UI / notifications; Preview/Final/Task Tracker không đổi; Delivery Log vẫn removed. ⚠️ Phải chạy migration (account_status='wording' bị CHECK cũ chặn nếu chưa chạy). |
| 2026-06-05 | Roles / Auth (Phase 1 Content) | **Thêm role `content` (chỉ nhận diện + routing + nav + access guard — CHƯA làm wording workflow)**. `login.html`: redirect home theo role `roleHome` (client→client-dashboard, **content→content-workbench.html**, còn lại→dashboard). `assets/app.js`: `injectContentNav()` — inject mục **Content Wording** (`data-show-roles="admin,account,content"`) vào sidebar Vận hành mọi trang nội bộ (idempotent, chỉ role nội bộ → client/public bỏ qua) thay vì sửa 10 file HTML. `assets/styles.css`: thêm rule hiển thị `data-show-roles*="content"` cho role content + `.role-badge.r--content` + `.role-tag-badge.rt--content` (tím). **Tạo `content-workbench.html`** — internal shell placeholder ("Coming soon"), guard cho phép admin/account/content (client→client-dashboard, design/editor→dashboard). `user-management.js` ROLE_LABEL + `user-management.html` 2 dropdown role thêm `content`. KHÔNG đổi production/client/notification/Delivery Log. Guard admin-only sẵn có (`settings.js`/`user-management.js` `role!=='admin'→dashboard`) đã chặn content. **DB**: cần thêm user `content@cb.vn` role `content` trên Supabase để đăng nhập. Acceptance: content login→content-workbench + badge "Content"; content bị chặn settings/user-management; client vẫn bị chặn nội bộ; admin/account/design/editor không đổi. |
| 2026-06-05 | Client Orders (Order Drawer) | **Refactor đầu drawer: bỏ status/action trùng, contextual theo lifecycle**. Vấn đề: thanh nút cố định (Kiểm tra brief · Yêu cầu bổ sung · Xác nhận brief · Push → Production) chỉ *disable* chứ không ẩn → order đã `final_sent`/`completed` vẫn hiện nút giai đoạn brief; dòng "Đã push sang Task Tracker. PIC… · Xem task" trùng nghĩa với Lifecycle Timeline + Next Action. **Fix `database-orders.js`**: (1) `updateStepperState` đổi từ disable → **show/hide theo stage**: briefStage (chưa confirm/push) hiện 3 nút brief; pushStage (đã confirm, chưa push) hiện Push; đã push/có Preview/Final/hoàn tất → ẩn hết nút brief+push; ẩn cả `.wf-actions-flow`/`.drawer-actions` khi rỗng. "Hủy đơn" giữ ở `.wf-actions-danger` (Hành động khác), ẩn khi cancelled/completed/rated. Bỏ hẳn dòng `wf-hint` "Đã push…". (2) Thêm `buildLinkedTaskCard(o)` — card gọn "Task Tracker" (task_id · loại · PIC · status · "Xem task"; nhiều task list hết; đã push mà chưa có task local → "mở Task Tracker") thay cho wf-hint. (3) Reorder body: **Timeline → Next Action (ngay dưới Timeline) → Linked Task card → summary grid → …**. (4) Delivery: ẩn block "Gửi Preview" khi đã có Final. KHÔNG động handler Gửi Preview/Final/feedback (guard `if(btn)` sẵn) — Preview/Final/feedback logic không đổi; Delivery Log vẫn removed. Acceptance: order "Đã gửi Final · Chờ Client đánh giá" KHÔNG còn nút brief/push; Timeline + Next Action vẫn rõ; vẫn mở được task. |
| 2026-06-04 | Flow Feedback Vòng 3 → Final | **Chuẩn hóa luồng sau Feedback Vòng 3 (đa file)**. Quyết định nghiệp vụ: Vòng 3 VẪN thuộc order hiện tại — KHÔNG tạo task/order mới ngay. Flow: Client gửi FB Vòng 3 → Account chuyển PIC → PIC cập nhật **Final Link** + gửi duyệt nội bộ → Account/Admin gửi **Final** → Client đánh giá HOẶC **tự tạo Order mới** từ order gốc. Admin/Account KHÔNG tạo order thay client. **client-dashboard.js**: modal FB Vòng 3 đổi title "Feedback Vòng 3 — Final Check", warning vòng cuối, badge "Vòng cuối · 3/3", note dưới textarea, nút "Gửi Feedback Vòng 3 — Final Check"; submit V3 toast + notify Account/Admin (`client_feedback_received`, title/msg riêng V3); sau Final hiện CTA **"Tạo Order mới từ yêu cầu này"** (Action Center + drawer) → `gotoRevisionForm()` mở `request.html?mode=revision&ref_order=ID`; Action Center vẫn ẩn preview khi `feedback_status` ∈ {feedback_received, revision_in_progress, approved, final_sent, exceeded_limit}. **database-orders.js**: Order Drawer khi `revision_round≥3 && feedback_status='feedback_received'` hiện alert "Feedback Vòng 3 — Final Check" + nút **"Gửi feedback vòng 3 cho PIC"** (gửi comment type=feedback vào task + task.status=feedback_fix + notify PIC + order.feedback_status=revision_in_progress; nhiều task → chọn ở "Links from Task Tracker"; không task → cảnh báo kiểm tra Task Tracker). **GỠ "Tạo task mới từ feedback này"** (cả nút lẫn handler redirect production-board). Gửi Final: `feedback_status='final_sent'` + notify client `delivery_final` ("Final đã sẵn sàng" + gợi ý tạo Order mới nếu phát sinh). **production-board.js**: Task Drawer hiện block "Feedback Vòng 3 — Final Check" + chặn chuyển `review` nếu chưa có `final_link` (canTransition/getTransitionError + disable nút "Gửi duyệt nội bộ"); stash `task._refOrderRound/_refOrderLimit` từ order. **order-form.js / request.html**: thêm `mode=revision&ref_order=ID` → title "Tạo yêu cầu chỉnh sửa phát sinh" + banner + checkbox xác nhận `#revision_confirmed` (bắt buộc) + prefill (project/request_type/department) từ order gốc; submit set `parent_order_id`/`order_origin='revision_over_limit'` (best-effort cột + nhúng marker vào content_brief), order mới đi luồng chuẩn brief→confirm→push→preview/final→rating. Migration tùy chọn `supabase/add-revision-link.sql` (parent_order_id + order_origin). Delivery Log vẫn removed; Preview/Final handoff vẫn trong Order Drawer. |
| 2026-06-04 | Client Portal | **Fix Preview feedback Action Center hiện trùng / gửi feedback 2 lần**. Root cause: `renderActionCenter` (và drawer `feedback_wait`) chỉ gate theo `o.status==='feedback_wait'`, không xét `feedback_status` → sau khi client gửi feedback (`feedback_status='feedback_received'`) thẻ "Bản Preview chờ feedback" + nút Duyệt/Gửi feedback vẫn còn → gửi trùng. Fix: thêm helper `shouldShowPreviewAction(o)` (true khi `status==='feedback_wait' && preview_link && (feedback_status rỗng \|\| 'waiting_feedback')`) + `getPreviewRound(o)` (=`min(revision_round+1, revision_limit)`). Action Center push preview qua `shouldShowPreviewAction`; thẻ đổi tiêu đề **"Bản Preview lần N chờ feedback"** + "Feedback Vòng N/limit". Drawer `feedback_wait` tách 3 nhánh: actionable (Xem/Duyệt/Gửi feedback) / approved (read-only "đang chuẩn bị Final") / đã-xử-lý (feedback_received\|revision_in_progress\|exceeded_limit → read-only "đã gửi feedback, chờ team"). Guard `approvePreview` + submit feedback: chặn khi `feedback_status` khác rỗng/`waiting_feedback`. Account gửi Preview mới (`database-orders.js:1104` set `feedback_status='waiting_feedback'`) → thẻ hiện lại với vòng kế tiếp; sau `renderAll()` thẻ biến mất ngay. KHÔNG đổi schema. |
| 2026-06-03 | Client Orders | **Badge "NEW" + fix PIC media không hiển thị + sync progress theo status**. (1) `database-orders.js renderRow`: thêm badge `.order-new-badge` "NEW" (pulse) cạnh order ID khi `account_status='pending'`. (2) **Bug**: order media gán PIC qua `production_pic_video/photo` nhưng bảng/filter đọc `production_pic` đơn → hiện "Chưa gán". Fix: helper `orderHasPic(o)` (media-aware) cho cột P.I.C (hiện "Q: … / C: …"), quick-filter `unassigned` + count-unassigned. (3) Save-internal: thêm map `PROD_PROGRESS` → khi đổi Production Status, progress tự sync (vd received→20%) thay vì kẹt 5%. `styles.css`: badge + pulse animation. LƯU Ý: gán PIC ≠ đổi progress; progress đổi theo production lifecycle (push/status). |
| 2026-06-03 | Master Dashboard | **Đổi "Active Client Orders" → "New Orders" + animation chú ý**. `dashboard.html`: card đổi label "Active Client Orders"→"New Orders", icon inbox, trend "Not cancelled"→"Chờ xác nhận", metric đổi từ active (not cancelled) → đếm **đơn mới `account_status='pending'`**, drilldown `data-drill-key`→`new_requests`. Toggle class `.kpi--attention` khi count>0. `styles.css`: keyframes `kpiAttention` (pulse glow navy + viền + value navy), tôn trọng `prefers-reduced-motion`. Giữ `data-card-key="active_client_orders"` (không phá export/map). |
| 2026-06-03 | Order Form | **Bỏ "Hỗ trợ wording" (toàn bộ) + Video Editing rút gọn Section 4**. Xóa hẳn field `wording_required` + callout khỏi `request.html`; bỏ listener wording-warning + preview Wording row trong `order-form.js`. Video/Motion (sec-d): ẩn Headline/CTA/Thông tin bắt buộc/content_brief/creative_direction (`data-subform-hide-for` thêm `video,motion`), thêm 1 ô gộp **"Nội dung & định hướng sáng tạo"** `#video_brief` (`data-subform-show-for="video,motion"`, bắt buộc) → map vào `content_brief` (+ link kịch bản/source). Preview video hiện ô gộp. Các type khác: bỏ wording, giữ nguyên cấu trúc. Không đổi schema (wording_required/content_brief/creative_direction columns vẫn còn). |
| 2026-06-03 | Order Form (media) | **Media: thêm "Số lượng ảnh" + bỏ mục 4&5, đi thẳng tới Deadline**. `request.html`: thêm `#photo_quantity` (number, disabled) trong media sub-form — `order-form.js syncPhotoQty()` chỉ bật khi tick "Chụp ảnh" (clear khi bỏ tick); gọi init + on change + sau restore. Khi chọn media: ẩn section `sec-d` (Nội dung & định hướng) + `sec-e` (Tài nguyên) + 2 stepper item tương ứng (`data-subform-hide-for="media"`) → flow đi thẳng 3→6 (Deadline). `refreshProgress` đổi sang `visibleSections()` (media tính 5 mục thay vì 7). `renumberSteps()` đánh số liền mạch (1-5) cả stepper sidebar `.step-index` lẫn badge `.section-num` theo section đang hiện (chọn media → 1·2·3·4·5 thay vì 1,2,3,6,7). photo_quantity placeholder "VD: 30 - 50", gói vào `content_brief`. **KHÔNG đổi schema DB** — các cột content_brief/creative_direction… vẫn còn (nullable), media để null/điền từ summary buổi chụp. |
| 2026-06-03 | Database Orders + Task Tracker | **Fix Internal Deadline mất khi mở lại + timestamp hiện ISO thô**. Root cause: input `datetime-local` chỉ `.replace(' ','T')` nên giá trị ISO từ Supabase (`...:00+00:00`, có giây + timezone) không parse được → input rỗng (deadline "mất"); các chỗ hiển thị `created_at`/`last_updated`/comment time đổ thẳng ISO. Fix: thêm helper `toLocalInput(s)` (ISO/bare → `YYYY-MM-DDTHH:MM` giờ local) cho input datetime-local, và `fmtDateTime(s)` (→ `DD/MM/YYYY HH:MM` local, coi bare là UTC vì lưu bằng `toISOString`) cho mọi chỗ hiển thị timestamp. Áp dụng: `database-orders.js` (edit-internal-deadline, activity log, "Tạo lúc", Ngày gửi) + `production-board.js` (edit-deadline, modal prefill, comment/activity time, table last_update). |
| 2026-06-03 | Delivery Log (removed) | **Gỡ trang Delivery Log**. Sau khi bàn giao chuyển vào Order drawer (hướng A), trang Delivery Log (`deliveries` table lệch kiến trúc) thành vestigial → gỡ hẳn: xóa `delivery-log.html` + `assets/delivery-log.js`; gỡ sidebar nav khỏi 10 trang internal; dashboard drilldown `ready_for_delivery`/`average_rating`/`rating_coverage` → `database-orders.html`; `app.js resolveNotifLink` deliveries → `database-orders.html`; chatbot bỏ action "Open Delivery Log" → "Open Client Orders". Còn `16 HTML · 13 JS`. (Bảng `deliveries` trong DB vẫn tồn tại nhưng không còn UI thao tác — flow thật đi qua order.) |
| 2026-06-03 | Database Orders / Delivery flow | **Fix flow bàn giao link không tới client (đứt orders↔deliveries)**. Root cause: Delivery Log lưu preview/final vào bảng `deliveries`, nhưng Client Portal đọc `orders.preview_link`/`orders.final_delivery_link` → client không thấy link; thêm nữa delivery KHÔNG tự tạo cho order thật nên Delivery Log trống. Fix (hướng A): chuyển thao tác bàn giao vào **Order drawer** (`database-orders.js`) — section "Bàn giao cho client" cho admin/account nhập **Preview Link → "Gửi Preview"** (set `order.preview_link` + `delivery_status='client_wait'` + `production_status='feedback_wait'` + `delivery_date` + notify client `delivery_preview`) và **Final Link → "Gửi Final"** (set `final_delivery_link` + `delivery_status='final'` + `production_status='delivered'` + `progress=95` + notify `delivery_final`). Client Portal đọc order → thấy link + rating prompt (status delivered). Dùng cột order có sẵn, không cần bảng deliveries. **Delivery Log cũ giờ vestigial cho flow thật.** |
| 2026-06-02 | Order Form | **Slide / Proposal: bỏ kích thước + đổi Nội dung sang thả Link thô (Google Doc/Slide)**. Slide thường làm từ file Word/PPT thô của TGĐ. `request.html`: thêm `slide` vào `data-subform-hide-for` (ẩn size). Section 4: textarea `content_brief` thêm `data-subform-hide-for="slide"` (ẩn cho slide); thêm field `#slide_source_link` "Link nội dung thô" (`data-conditional-req`, bắt buộc) với **cơ chế mới `data-subform-show-for="slide"`** (hiện field CHỈ cho type liệt kê). `order-form.js`: thêm loop `[data-subform-show-for]` (toggle `.is-cond-shown`); doSubmit map `slide_source_link`→`content_brief`; preview slide hiện "Link nội dung thô" + bỏ size. `styles.css`: `[data-subform-show-for]{display:none}` + `.is-cond-shown{display:block}`. Validate/section-completion tự skip field ẩn (offsetParent null). Slide cũng ẩn Headline/CTA/Thông tin bắt buộc/hỗ trợ wording (`data-subform-hide-for="slide"`); chỉ giữ Link nội dung thô + Phong cách/định hướng + Link tham khảo. Preview ẩn Headline/CTA/Wording cho slide. |
| 2026-06-02 | Order Form | **Video Editing: bỏ kích thước, thêm Link kịch bản (bắt buộc) + Link source**. `request.html`: ẩn 2 trường Kích thước/Tỉ lệ + Kích thước cụ thể cho `video`+`motion` (mở rộng `data-subform-hide-for="media,video,motion"` — 2 type này đã có "Tỉ lệ video" trong sub-form). Video sub-form thêm `#script_link` "Link kịch bản" (`data-conditional-req`, bắt buộc) + đổi "Link footage Drive" → "Link source" (`#footage_link`); bỏ checkbox "Đã có kịch bản" (thừa). `order-form.js`: gói `script_link`+`footage_link` vào `content_brief` (PIC thấy); preview video/motion hiện Tỉ lệ/thời lượng + 2 link thay vì kích thước. (Áp dụng cả Motion vì dùng chung sub-form.) |
| 2026-06-02 | Order Form + Database Orders | **Gộp Quay + Chụp ảnh → 1 type `media` "Quay / Chụp ảnh" + tách 2 task khi Push (Phase 1+2)**. Lý do: sự kiện/ngoại khóa thường order cả quay lẫn chụp cùng 1 buổi. **P1 (form):** `request.html` gộp 2 tile → 1 tile `media`; 2 deliverable group (Quay/Chụp) đổi `data-deliverable-for="media"`; gộp 2 sub-form → 1 "Thông tin buổi Quay / Chụp" có service selector `media_service` (☑Quay ☑Chụp, ≥1) + onsite info dùng chung (ngày/giờ/địa điểm/onsite); ẩn size cho media. `order-form.js`: REQ_TYPE_LABEL+validation (≥1 service) + preview media + map `media_location`→`shoot_location` + gói onsite info vào `content_brief`. TYPE_LABEL `media` ở database-orders/production-board/delivery-log. **P2 (2 task):** `supabase/add-media-pics.sql` — mở rộng CHECK `orders.request_type` thêm `'media'` (BẮT BUỘC) + thêm cột `production_pic_video`/`production_pic_photo`. Order drawer media hiện 2 ô PIC (Quay/Chụp); `pushToProduction` cho media tạo 2 task riêng (task_type `shoot` cho Quay + `photo` cho Chụp) theo **PIC nào được gán** (không dùng deliverable), gán PIC riêng + notify từng PIC. Push readiness/next-action/brief-checklist media-aware (bỏ check deliverable+size, thay bằng dịch vụ+địa điểm). **Media KHÔNG dùng checkbox "Hạng mục cần sản xuất"** (ẩn qua `data-subform-hide-for="media"`) — chỉ cần service selector ☑Quay ☑Chụp + ô "Nội dung cần Quay/Chụp". **Order cũ `shoot`/`photo` giữ nguyên.** ⚠️ Cần chạy `add-media-pics.sql` trong Supabase trước khi dùng (nếu chưa, media order chỉ lưu local do CHECK constraint). |
| 2026-06-02 | Order Form | **Remove field "Ngày sử dụng thực tế" + fix line stepper lệch**. `request.html` xóa field `actual_use_date` (Section 2) + `order-form.js` bỏ dòng preview "Ngày sử dụng" (payload giữ `actual_use_date: null` vô hại). `styles.css` fix `.flow-steps::before` từ `left:18px` → `left:27px` để line dọc canh giữa step-node (node center = a padding 10px + 36/2 = 28px). |
| 2026-06-02 | Order Form | **Tùy biến hạng mục Chụp ảnh (photo)**. `request.html`: đổi checkbox deliverable photo — "Chân dung" (value `Chụp chân dung`) → **"Hoạt động ngoại khóa"**, "Chụp sản phẩm" → **"Chụp Studio/Photoshoot"** (đổi cả text lẫn value). Ẩn 2 trường **Kích thước/Tỉ lệ + Kích thước cụ thể** khi chọn photo (thêm `data-subform-hide-for="photo"` vào `.grid-2`); photo dùng sub-form "Thông tin buổi chụp" có sẵn (`photo_location` = Địa điểm chụp, placeholder đổi sang "Trường TH Trần Hưng Đạo, CB Mekong"). `order-form.js updateConditional` thêm loop `[data-subform-hide-for]` → toggle class `.is-hidden-cond`; `styles.css` thêm `.is-hidden-cond { display:none !important }`. Cơ chế hide-for này tái dùng được cho type khác. |
| 2026-06-02 | Workflow notifications (B+C+D) | **Khép kín flow nội bộ + rating**. (B) `data-store.js findUserIdByName` đổi từ exact-match sang fuzzy: thử exact → prefix `"Duy %"` → suffix `"% Duy"` → contains, để PIC tên ngắn ("Duy") khớp account tên đầy đủ ("Duy Trần") → **designer nhận được notification task_assigned**. (C) `production-board.js` thêm `notifyTaskStatusChange()` gọi trong `updateStatus`: status→`review` báo Admin+Account ("Task chờ duyệt nội bộ"), status→`revision`/`feedback_fix` báo PIC ("Task cần chỉnh sửa"); `delivery-log.js request_rev` cũng notify PIC khi trả task về sửa. (D) `client-dashboard.js` rating-submit giờ **persist Supabase** (`satisfaction_score` + `client_feedback` qua `orders.update`) + notify Admin+Account (`rating_received`) — trước đây chỉ lưu local + toast nên mất khi reload và staff không thấy. Tất cả type (`task_status_changed`, `rating_received`) đã có icon trong `notif-icons.js`. |
| 2026-06-02 | Notifications (refactor) | **Tách `assets/notif-icons.js` shared module — single source of truth cho icon thông báo**. Trước đó icon map lặp ở `app.js` + `client-dashboard.js` (rủi ro lệch). Tạo `assets/notif-icons.js` expose `window.MH.notifIcons` (`PATHS` + `get(type)→{svg,cls}` + `stripEmoji(s)`); `app.js` và `client-dashboard.js` đổi sang dùng module (xóa map cục bộ, chỉ giữ alias). Thêm `<script src="assets/notif-icons.js">` TRƯỚC `app.js` trên 12 page có chuông/panel (public pages không có chuông → không cần). Sửa icon/màu giờ chỉ 1 nơi. |
| 2026-06-02 | Task Tracker | **Fix PIC không thấy task được giao (My Tasks lọc sai tên)**. Root cause: `production-board.js` match user↔task bằng `t.assigned_to === user.name \|\| t.assigned_to === user.name.split(' ').pop()`. Với account design `Duy Trần`, `.split(' ').pop()` lấy CHỮ CUỐI = "Trần", còn PIC lưu tên ngắn "Duy" (chữ đầu) → không bao giờ khớp → Duy không thấy task. Fix: thêm helper `isMyTask(assignedTo)` khớp khi bằng nhau HOẶC full name chứa tên PIC theo word-boundary (`startsWith(a+' ')` / `endsWith(' '+a)` / `includes(' '+a+' ')`, tránh false-positive "Duyên"); thay 6 chỗ match cũ (visibleTasks, quick 'my', quickChip 'mine', renderSummary, renderMyTasks, canEditLinks, attachDrawerEditButton). LƯU Ý workflow: task CHỈ được tạo khi bấm **Push → Production** (assigned_to = order.production_pic); gán PIC trên order mà chưa push thì chưa có task. |
| 2026-06-02 | Client Portal + Notifications | **Fix click notification (client) không mở được khung trạng thái order**. Root cause: notif client có `link='tracking.html?code=…'` (hoặc rỗng → fallback `database-orders.html` mà client bị chặn) nên click bell dropdown không mở được status drawer trong portal. Fix: (1) `app.js resolveNotifLink` role-aware — khi `mh-user.role==='client'` → trả `client-dashboard.html?order=<related_entity_id>` (fallback parse `code=/order=/id=` từ link) thay vì điều hướng trang nội bộ. (2) `client-dashboard.js` thêm `openOrderFromQuery()` đọc `?order=` sau khi `loadClientOrdersFromStore()` xong → `switchTab('orders')` + `openOrderDrawer(id)`. (3) Tab panel: cả `.notif-item` giờ clickable (`data-action="open-order"` + cursor pointer + hover) → click bất kỳ chỗ nào trên thông báo đều mở order drawer + mark read (mọi type, kể cả cancelled). |
| 2026-06-02 | Client Portal | **Đồng bộ icon thông báo client với internal accounts (minimal line, bỏ emoji)**. Gap: `client-dashboard.js renderNotifications` render `n.title` kèm emoji (✅🔎❌) inline, không có icon column → khác giao diện internal bell. Fix: thêm `NOTIF_ICON_PATHS` + `notifIcon(rawType)` + `stripNotifEmoji()` (copy y hệt app.js, key theo `n.raw_type` để cùng icon/màu logic: accent navy cho order_new/task_assigned, red cho order_cancelled/order_needinfo, còn lại muted). Notif item đổi `.notif-dot` → `.notif-ico` (line SVG 18px), title strip emoji. Realtime toast bỏ prefix `🔔` + strip emoji title. `client-dashboard.html` thêm CSS `.notif-ico` (+ is-accent/is-danger). Unread vẫn nhận biết qua bg tint + border (`.notif-item.unread`). |
| 2026-06-02 | Order Form | **Section 3 — chỉnh options Kích thước/Tỉ lệ + repurpose field bên cạnh thành "Kích thước cụ thể"**. `request.html`: option `Banner ngang`→`Banner ngang 3 x 1m`, `Khác — sẽ ghi rõ trong mô tả`→`Khác — điền vào ô bên cạnh`, thêm `Standee 80 x 200cm`. Field `Số lượng phiên bản` (`version_quantity` number) → `Kích thước cụ thể` (`#custom_size` text, disabled mặc định). `order-form.js`: thêm `syncCustomSize()` (enable `custom_size` chỉ khi `size_ratio` bắt đầu bằng "Khác", clear khi đổi; gọi init + change + sau restoreDraft); doSubmit map `custom_size` vào cột `size_ratio` khi chọn Khác; preview đổi nhãn sang "Kích thước cụ thể". `styles.css` thêm style `.input:disabled/.textarea:disabled/.select:disabled`. `version_quantity` bỏ hẳn (trước nay không persist DB). |
| 2026-06-01 | Client Orders + Delivery Log + Orders Dashboard | **Đồng bộ fix Export + remove "Tạo Order" sang các trang còn lại**. Audit: Export wired sẵn ở reports/user-management/settings; HỎNG (không id/handler) ở `database-orders.html` + `delivery-log.html`; "Tạo Order" góc phải còn ở `order-dashboard.html` + `database-orders.html`. Fix: (1) `database-orders.js` thêm handler `#export-orders` xuất CSV danh sách orders **đang lọc** (`sortBy(applyFilters())`, 12 cột) dùng label maps TYPE/PRIORITY/ACCOUNT/PROD. (2) `delivery-log.js` thêm handler `#export-deliveries` xuất CSV deliveries đang lọc (15 cột). (3) Xóa `<a>` "Tạo Order" khỏi header-actions `order-dashboard.html` + `database-orders.html`. Cùng pattern CSV (escape, BOM UTF-8, toast) với Master Dashboard + reports.js. Quick-action tile "Tạo Order mới" trong body dashboard giữ nguyên. |
| 2026-06-01 | Master Dashboard | **Fix Export button + remove "Tạo Order" góc trên phải**. `dashboard.html`: nút Export trước đó không có id/handler (bấm không ra gì) → thêm `id="export-btn"` + `exportMasterCSV()` xuất snapshot CSV (Excel-compatible, BOM UTF-8) đọc trực tiếp từ DOM: filters + Last updated, KPI Client Orders Overview (8) + Internal Tasks Overview (8) qua `[data-card-key]`, và Workload by PIC qua `[data-pic]`. Filename `cb-media-hub-master-dashboard-YYYY-MM-DD.csv`, theo cùng pattern `reports.js exportCSV`. Đồng thời xóa `<a>` "Tạo Order" ở header-actions góc trên phải (quick-action tile "Tạo Order mới" trong body giữ nguyên). |
| 2026-06-01 | Notifications | **Bell attention state: gradient + animation khi có chưa đọc, ẩn dot khi đọc hết**. `assets/app.js refreshBadge()` toggle 1 class `.has-unread` trên `.mh-notif-wrap` theo unread count → điều khiển đồng thời badge đỏ, chuông gradient navy→red (`stroke: url(#mh-bell-grad)` + def SVG inject body), animation rung `mhBellRing`, dot pulse `mhBadgePulse`. Đọc hết (mark-all / click) → bỏ `has-unread` → chuông về normal + ẩn dot. Tôn trọng `prefers-reduced-motion` (tắt animation, giữ gradient+dot). |
| 2026-06-01 | Notifications | **Thay emoji trong notification bell bằng minimal line icon**. `assets/app.js initNotificationBell` thêm `NOTIF_ICON_PATHS` (outline SVG Lucide-style đồng bộ sidebar: `viewBox 0 0 24 24`, `stroke-width 2`, `currentColor`, rounded, 18px), `notifIcon(type)` map 12 type → icon + class màu, và `stripNotifEmoji()` bỏ emoji dẫn đầu (📥🎯🚀✅⚠❌👀📦🔎…) trong title cũ. Dropdown item đổi sang layout `[icon][body]`; màu mặc định `var(--text-muted)` (~#64748B), accent navy `is-accent` cho `order_new`/`task_assigned`, red `is-danger` cho `order_cancelled`/`order_needinfo`. Realtime toast cũng strip emoji cho nhất quán. Producers giữ nguyên (emoji vẫn trong DB, chỉ strip lúc render) nên không cần migration. |
| 2026-05-29 | Database Orders | **Refine Order Workbench thành flow 1 cột + animated next-action banner**. Sau feedback layout 2 cột tạo khoảng trắng chết và che nội dung, `assets/styles.css` đổi `.order-workbench .drawer-body` sang flex column, summary strip giữ ở đầu, `Hành động kế tiếp` thành banner full-width có animation chú ý nhẹ (respect `prefers-reduced-motion`). `assets/database-orders.js` kéo `internal_note` ra thành block riêng **Ghi chú / Comment nội bộ** để Admin/Account thấy rõ vùng trao đổi; vẫn dùng field `internal_note`, không đổi schema/Supabase/action flow. |
| 2026-05-29 | Database Orders | **Update Admin/Account order drawer thành Order Workbench**. Root cause feedback: production `production-board` đã có Task Workbench, nhưng Admin đang mở order mới trong `database-orders` vẫn thấy drawer cũ A/B/C và English section labels. `database-orders.html` thêm class `.order-workbench`; `assets/database-orders.js` thêm summary strip + `orderNextAction()` + Việt hóa block labels; `assets/styles.css` thêm layout 2 cột riêng cho order drawer: nội dung order/brief/internal/related/delivery bên trái, next action + push readiness + activity bên phải. Không đổi action flow Check/Need Info/Confirm/Push/Cancel, save internal, create task, notification hoặc Supabase payload. |
| 2026-05-29 | Data Store / Order Form | **Fix Sync DB lỗi khi DB thiếu optional `shoot_location` column**. Root cause: photo/shoot order gửi `shoot_location`, nhưng Supabase PostgREST schema cache chưa thấy cột này (`PGRST204: Could not find the 'shoot_location' column of 'orders'`). `assets/data-store.js` now retries orders/tasks create/update/upsert without optional `shoot_location` so order vẫn sync DB thay vì chỉ lưu local. Migration chuẩn vẫn là chạy `supabase/add-shoot-location.sql` để lưu được giá trị địa điểm. |
| 2026-05-29 | Master Dashboard | **Fix dashboard không cập nhật order/task mới khi admin đang mở page**. Root cause: nút refresh chỉ spin + đổi `Last updated`, không gọi lại `orders.list()`/`tasks.list()`, và dashboard chỉ load một lần. `dashboard.html` refactor refresh button sang async `loadMasterDashboard()`, cập nhật timestamp sau khi load data thành công, thêm polling fallback mỗi 60s, thêm Supabase Realtime hook cho bảng `orders` + `tasks` nếu publication đã bật. |
| 2026-05-29 | Task Tracker | **Redesign task detail drawer thành Task Workbench**. `production-board.html` thêm class `.task-workbench` cho drawer task. `assets/styles.css` thêm layout workbench 2 cột: detail/main column + right action rail, info tiles 4 mục, production checklist, file workspace zone, comment tabs, people rail, AI hint card, activity timeline; scoped để không ảnh hưởng Database Orders drawer. `assets/production-board.js` bổ sung checklist tự tính từ status/content/links, activity rail từ comments/history, next-action hint; giữ nguyên status transitions, save links, save meta, comment/mention/reply và payload hiện tại. |
| 2026-05-29 | Order Form | **Đổi field campaign sang Mã kế hoạch**. `request.html` Section 2 label "Mã / Tên campaign" đổi thành "Mã kế hoạch", placeholder đổi sang `Tùy chọn — VD: KH07/CBMK`, subtitle đổi "campaign" thành "kế hoạch". `assets/order-form.js` preview brief đổi nhãn "Campaign code" thành "Mã kế hoạch". Giữ `id/name=campaign_code` để không đổi payload/storage. |
| 2026-05-29 | Public Homepage | **Đổi hero audience wording sang Nội bộ CB Centres**. `index.html` hero lead đổi "khách hàng" thành "Nội bộ CB Centres"; workflow preview copy cũng đổi sang "Nội bộ CB Centres" để nhất quán audience nội bộ. Không đổi layout, CTA, routing hoặc logic. |
| 2026-05-29 | Order Form | **Đồng bộ số bước giữa sidebar và section card**. `request.html` đổi header badge từng section từ A-G sang 1-7, helper text nhắc tài nguyên đổi từ "Section E" sang "bước 5". `assets/styles.css` chỉnh `.section-num` thành node tròn đồng bộ visual với smart flow stepper, bỏ alternate A/B màu cũ để tránh lệch nhận thức giữa sidebar và nội dung form. |
| 2026-05-26 | Order Form | **Đổi sidebar A-G sang smart flow stepper 1-7**. `request.html` thêm progress bar + timeline có icon/helper text để diễn đạt bước kế tiếp rõ hơn. `assets/styles.css` thêm light/dark styles và responsive horizontal stepper trên mobile. `assets/order-form.js` sync active step theo scroll/click, done state theo section completion thật; bước 5 `Tài nguyên` optional nên chỉ done khi user thêm file/link/note, không auto xanh lúc mới vào form. |
| 2026-05-26 | Public Homepage | **Refresh hero background theo hướng QuickMagic-inspired nhưng giữ CB brand**. `index.html` thêm decorative `hero-bg` layer aria-hidden; `assets/styles.css` thêm light/dark hero background system: Light mode giữ clean portal với grid/glow navy-red nhẹ, Dark mode cinematic workspace với navy/red depth, **visual-only floating panels không có text để tránh bị hiểu là button**, glass trust strip. Không thêm asset/dependency mới, dùng CSS + hero assets sẵn. Hero content/CTA/flow giữ nguyên; mobile CTA stack full-width để tránh overflow. |
| 2026-05-23 | Shoot location field | **Thêm field "Địa điểm" cho task/order chụp ảnh hoặc quay**. SQL migration `supabase/add-shoot-location.sql` (idempotent, `IF NOT EXISTS`) ADD `shoot_location text` vào cả `orders` và `tasks`. Order Form: sub-form shoot/photo có sẵn `shooting_location`/`photo_location` input → `doSubmit` map qua `shoot_location` khi INSERT. Database Orders drawer: Brief Information thêm row "Địa điểm" khi `request_type IN ('photo','shoot')`. Push to Production: kế thừa `shoot_location` vào task. "Create Task from this Order" deep-link pass qua URL param. Client Portal drawer: hiển thị tương tự. Task Tracker modal "Giao việc nội bộ" thêm row `#tm-location-row` show/hide khi `task_type IN ('photo','shoot')`, save (Create + Edit) include `shoot_location`. Task drawer Brief Information hiển thị. Field **optional** ở tất cả entry — không block submit, UI ẩn row khi type khác. **Cần chạy migration trong Supabase SQL Editor để DB column tồn tại** |
| 2026-05-23 | Header compact redesign (revised) | **Header top-right finalized**: 3 elements 36px — `[Theme circle] [Bell circle] [Profile: name+role text + avatar tròn]`. Theme toggle rewrite từ pill switch 48×26 với thumb translateX → icon-only circular đồng nhất bell + bell có border + hover shadow. Profile chip dùng inline text + avatar KHÔNG border (`flex-direction: row-reverse`, name 13px bold + role badge 10px stacked phải avatar phải). Cả cụm clickable mở dropdown menu cũ. Hover: bg surface-2 + avatar ring shadow. Mobile <560px: ẩn text, chỉ avatar. VN/EN lang toggle scaffold (i18n.js + .lang-pill CSS) **đã removed** sau khi user xác nhận chưa cần (scope dịch toàn bộ 17 page quá lớn để phase này) |
| 2026-05-23 | Notif click drawer fix | **Auto-open drawer retry sau khi Supabase data load**. Gap: 3 module (database-orders.js / production-board.js / delivery-log.js) đọc `?id=` query param ngay sau initial render (mock = empty array vì demo cleared) → focus fail → toast warning. Fix: refactor `tryFocusOrder`/`tryFocusTask`/`tryFocusDelivery` helper với 2-pass — first attempt im lặng với mock, retry sau khi `loadXFromStore()` resolve với Supabase data → mở drawer hoặc toast warning. Áp dụng cho cả 3 module nên Alert Center + dashboard drilldown + notification click đều benefit |
| 2026-05-23 | Brief supplement reverse-flow (Phase 1.5c) | **Wire client → admin/account flow khi client bổ sung brief**. Gap: `client-dashboard.js info-submit` trước chỉ optimistic `o.status='checking'` + toast — KHÔNG persist nội dung bổ sung, KHÔNG đổi `account_status` DB, KHÔNG notify staff. Fix: 1) Persist Supabase qua `MH.store.orders.update(o.id, { account_status:'checking', internal_note: prevNote + '\n\n[Client bổ sung — DD/MM/YYYY HH:MM]\nNội dung: ...\nLink: ...', last_updated })`. Append vào `internal_note` text free (không cần SQL migration, account thấy ngay trong drawer "Ghi chú nội bộ"). Timestamp prefix phân biệt source. 2) Cập nhật `o.__raw.internal_note` + `o.__raw.account_status` để local cache đồng bộ. 3) Bulk notify admin+account active qua `users.in('role',['admin','account']).eq('status','active')` → INSERT notifications `type='order_status_changed'`, title='📥 Client đã bổ sung brief', link=`database-orders.html?id=`. Pattern giống `order-form.js doSubmit`. 4) Error handler toast warning nếu sync fail. Out-of-scope: account-side click "Yêu cầu bổ sung" chưa có modal cho account nhập "cần bổ sung gì cụ thể" (hiện chỉ updateStatus → client thấy default text). Để fix lần refine sau |
| 2026-05-23 | Client notifications (Phase 1.5b) | **Client notifications sync — 5 new producers + Realtime consumer**. Phát hiện gap: client portal notification tab show 5 mock entries (MEDIA-2026-0008/0015/0019/0025/0022 — đã xóa lúc clear demo data). Producers chỉ có 2 spot (order-form submit notify staff, pushToProduction notify PIC) — thiếu hoàn toàn notify client khi status thay đổi. **Fix**: 1) `database-orders.js` thêm `notifyClient(order, payload)` helper (lookup `requester_id` first, fallback `users.id` qua `requester_email`); hook vào `updateStatus()` cho 3 transition (checking → 🔎 Brief đang được kiểm tra type=`order_status_changed`, needinfo → ⚠ Cần bổ sung brief type=`order_needinfo`, confirmed → ✅ Brief đã được xác nhận type=`order_confirmed`); hook vào `pushToProduction()` thêm notify client (🚀 Đã chuyển sang sản xuất type=`order_status_changed`) song song notify PIC. 2) `delivery-log.js` thêm `notifyClientDelivery(delivery, payload)` helper (lookup `users.id` qua `delivery.requester_email`); hook `send_preview` (👀 Đã có bản xem trước type=`delivery_preview` kèm link preview) + `send_final` (📦 Đã bàn giao final type=`delivery_final` kèm link final). 3) `client-dashboard.js` Consumer: bỏ 5 mock NOTIFS hardcoded → `let NOTIFS = []`; thêm `NOTIF_TYPE_UI_MAP` bridge Supabase notification.type → client UI type (needinfo/preview/rating/confirmed/cancelled/system); `mapNotifFromSupabase()` adapter row → mock shape; `loadNotificationsFromStore()` always-swap pattern (listAll 50 → replace NOTIFS + re-seed state.notifRead); `startNotificationsRealtime()` subscribe channel `notif-{user.id}` filter `user_id=eq.{user.id}` → INSERT event → NOTIFS.unshift + renderNotifications + toast 🔔 6s; mark-as-read write-through `MH.store.notifications.markRead()`; cleanup channel beforeunload. Type schema dùng CHECK constraint hiện có từ `add-cancel-fields.sql` (12 types) — không cần SQL migration. Producer/consumer pattern: fire-and-forget INSERT với try/catch + console.warn để không block UI. Acceptance: ✓ Client thấy notification realtime khi staff đổi status, ✓ Toast popup 6s khi có notification mới, ✓ Mark-as-read sync DB, ✓ Mock data đã xóa hoàn toàn |
| 2026-05-22 | Master Dashboard (Module 5 refine) | **Cleanup legacy hidden blocks**. Module 5 đã ship trước đó với 4 sections (Client Orders Overview 8 metrics / Internal Tasks Overview 8 metrics / Alerts / Team Workload) nhưng giữ kèm legacy HTML hidden (~302 lines: old mixed KPI grid + Workflow Health pipeline + Production Status donut + Delivery Funnel + Recent Activity feed). Refine session 2026-05-22: (1) Remove 5 legacy hidden blocks bằng awk skip lines từ `<!-- ============ LEGACY ...` đến `<!-- ============ MODULE 5 ...` next marker. (2) Simplify `loadMasterDashboard()` inline JS: bỏ 18 dbSetKpi/dbSetKpiTrend calls cho legacy keys (total_orders, new_requests, in_production, internal_review, ready_for_delivery, due_soon, on_time_rate, average_rating, rating_coverage) — các keys này không còn DOM target sau khi remove cards. Loader giờ gọi trực tiếp `renderModule5Dashboard(O, T)` rồi compute Workload by PIC. (3) Bỏ `[data-pipe]` selector code (Workflow Health đã removed). dashboard.html từ 1138 → 774 lines (~32% reduction). Drilldown behavior unchanged: `DRILLDOWN_MAP` 26 entries, goDrilldown() reads card data-card-key + optional data-drill-key. Acceptance: ✓ Leader/Admin combined view rõ ràng, ✓ Orders + Tasks visually separated, ✓ drilldown working |
| 2026-05-22 | Master Dashboard (Module 5) | Refined `dashboard.html` as a combined Leader/Admin overview with 4 clear sections: Client Orders Overview (8 metrics), Internal Tasks Overview (8 metrics), Alerts, and Team Workload. Legacy mixed KPI row/workflow row kept hidden for rollback. Added new KPI keys with `data-drill-key` so existing drilldown behavior continues to route to Client Orders or Internal Task Tracker without mixing order/task semantics |
| 2026-05-12 | Public site | Built initial 5 pages and brand setup |
| 2026-05-13 | Public site | Rebrand to navy `#191970` + red `#BA110F`, pill buttons, serif accents |
| 2026-05-13 | Dashboard/Auth | Built dashboard, demo accounts, role-based UI |
| 2026-05-14 | Order Form | Rebuilt to 7-section v2 spec |
| 2026-05-14 | Database Orders | Added full table, drawer, push-to-production flow |
| 2026-05-14 | Production Board | Added table/Kanban/My Tasks, drag-drop, status transitions |
| 2026-05-14 | Delivery Log | Added drawer, checklist, send preview/final flows |
| 2026-05-14 | Reports | Added KPI, custom charts, tables, export |
| 2026-05-14 | User Management | Added user CRUD UI, permissions, validation |
| 2026-05-14 | Settings | Added 12 settings panels, workflow editor, activity log |
| 2026-05-14 | AI Tools | Built module 09 MVP: 12 mini apps, mock generation, usage log |
| 2026-05-14 | Chatbot | Built module 10 MVP: dedicated page, floating widget, lookup, history |
| 2026-05-14 | Auth | Added Client demo account and client-role redirect/guard |
| 2026-05-14 | Tracking | Synced public tracking demo with `MEDIA-*` codes and client scope |
| 2026-05-14 | Auth/Profile | Added editable Profile modal in `app.js`: edit name/initials/title, avatar upload (256px JPEG), phone/department/bio, role select gated to Admin; persists to `mh-user` and refreshes chip live |
| 2026-05-14 | Dashboard | KPI drilldown: 12 cards map to Orders/Production/Delivery via `?dl=<key>`; target applies filter, shows banner, scrolls to table |
| 2026-05-14 | Deploy | Added `package.json` + `railway.json` for Railway deploy via Nixpacks + `serve` |
| 2026-05-14 | Repo layout | Rename folders to remove spaces: `Portal Hub` → `Portal_Hub`, `Brief Wflow` → `Brief_Wflow` |
| 2026-05-14 | Handoff docs | Created and optimized README, `_hot.md`, `STATUS.md` |
| 2026-05-15 | Client Portal | Built module 11: `client-dashboard.html` + `client-dashboard.js` — order status, order detail drawer, notification panel, profile tab; auth guard client-only |
| 2026-05-15 | Order Form | Added auth guard to `request.html`: blocks submit if not logged in, auto-fills requester info, preserves draft on redirect to `login.html?redirect=request.html` |
| 2026-05-15 | Layout/UX | Moved User Profile from sidebar to header right: `#header-profile-chip` (avatar + name + role badge + dropdown) replaces sidebar profile card across all 10 internal pages |
| 2026-05-15 | Layout/UX | Sidebar bottom: replaced profile card with minimal App version block (CB Creative Flow · v1.0) |
| 2026-05-15 | Layout/UX | Removed "+ Tạo Order" button from `dashboard.html` and `database-orders.html` header-actions (button retained in page content) |
| 2026-05-15 | Layout/UX | Reduced role badge font size to 9px on header profile chip |
| 2026-05-15 | Public header | Removed "Gửi yêu cầu" CTA pill; login button restyled to `.btn-login-pill` (red gradient, `border-radius: 9999px`) |
| 2026-05-15 | Shared JS | `app.js` updated: `refreshHeaderChip()`, header chip toggle handler, profile modal close handles both chip selectors |
| 2026-05-15 | Docs | Removed `deliveries.html` from all docs (file never existed in codebase) |
| 2026-05-15 | Handoff docs | Synced README, `_hot.md`, `STATUS.md` to reflect 11/11 modules, current file inventory, Client Portal, new layout conventions |
| 2026-05-15 | Dashboard | Fix Alert Center: 6 "Xem" buttons không click được → đổi thành `<a class="btn">` link tới module phù hợp với `?dl=<key>&id=<order_id>`. Orders/Production/Delivery JS đọc `?id`, auto-open drawer nếu record tồn tại, toast warning nếu placeholder demo không có trong mock |
| 2026-05-15 | Auth/UX | "Cài đặt" trong header profile menu giới hạn admin: `<a href="settings.html" class="hpm-item" data-show-roles="admin">` ở 10 HTML files. Thêm CSS rule `.hpm-item[data-show-roles*="..."]` display: flex (admin/account/design/editor variants) để giữ layout đúng |
| 2026-05-15 | Production Board | Comments: thêm @mention autocomplete (gõ `@` → dropdown 6 team members + current user, filter realtime, ArrowUp/Down/Enter/Tab/Esc nav) và Reply (button per comment → banner "Đang reply @Author" → save với `reply_to` → replies indent dưới parent, click reply-indicator scroll tới parent). Comment object thêm `id`, `mentions[]`, `reply_to`, `reply_to_author`. @Name rendered as `.mention` chip trong text |
| 2026-05-15 | Auth/Order | Restrict Order Form sang admin/account/client only. Thêm `data-show-roles="admin,account"` cho sidebar Order Form `<li>` ở 9 internal pages (client giữ access qua client-dashboard sidebar). Header "+ Tạo Order" button (dashboard, database-orders) cũng gated. `order-form.js` thêm role guard: design/editor → toast warning + redirect dashboard. CSS thêm rule `.btn[data-show-roles*="..."]` display: inline-flex để giữ button layout |
| 2026-05-15 | Auth/Order | Chatbot: ẩn "Open Order Form" cho design/editor. Static link trong `chatbot.html` `data-show-roles="admin,account"`. `chatbot.js` thêm `filterActions(actions)` filter URL `request.html` khỏi message actions khi role design/editor |
| 2026-05-15 | Public/Hero | Rewrite Hero Section visual ở `index.html`: thay khối dashboard mockup cũ (`.hero-mock` + 2 `.hero-floating`) bằng cụm CB character 3D + 3 floating icons (bell, document-pencil, image). Asset đặt tại `assets/hero/` (copy từ `Source/`). Icons có animation translateY 6–10px ease-in-out infinite alternate với delay khác nhau, tôn trọng `prefers-reduced-motion`. Responsive tablet/mobile, icon document-pencil ẩn ở ≤480px |
| 2026-05-15 | Public/Hero | Refinement: tách 3 keyframe riêng (`heroFloatBell`/`heroFloatDoc`/`heroFloatImage`) — biên độ 14-18px + rotate ±3° để chuyển động dễ thấy. Reposition `icon-image` upper-right (top:130, right:28, w:46px sau scale 50%) ngang mặt character thay vì giữa torso, tránh chồng camera baked-in của cutout |
| 2026-05-18 | Nav/UX | Sidebar rename "Production Board" → "Task Tracker" trên 9 internal pages. Thêm 2 nav entries dưới Master Dashboard: `Order Dashboard` (admin/account), `Task Dashboard` (all internal). Rename "Dashboard" → "Master Dashboard" để chuẩn hóa labels |
| 2026-05-18 | Dashboard | Tạo `order-dashboard.html` (KPI cấp Order: funnel theo `account_status`, branch breakdown, type breakdown — drilldown sang `database-orders.html?dl=<key>`) và `task-dashboard.html` (KPI nội bộ: total/in-prod/review/due-soon/overdue/on-time, workload PIC, task-type distribution, Linked vs Standalone — drilldown sang `production-board.html?dl=<key>`). Master Dashboard giữ nguyên drilldown 12 KPI |
| 2026-05-18 | Task Tracker | Production Board được formalize làm Task Tracker. Page H1: "Task Tracker / Production Board". Page head thêm `[+ Tạo Task]` button mở Create Task modal in-page. Quick filter chip row trên toolbar: Tất cả/Due Today/Due This Week/Overdue/Unassigned/My Tasks/Standalone (`state.quickChip`, tách với summary card `state.quick`) |
| 2026-05-18 | Task Tracker | Create/Edit Task modal (`#task-modal`): hỗ trợ `is_standalone` checkbox (ẩn order_id row), prefill từ URL params (`?createTask=1&order_id=...&project_name=...&task_type=...&priority=...&internal_deadline=...&production_pic=...&content=...`), auto-generate `TASK-NNNN` ID, push vào TASKS + persist localStorage `mh-extra-tasks`. Edit mode mở qua dynamic "Sửa Task" button trong drawer head cho admin/account/PIC |
| 2026-05-18 | Task Tracker | Drawer thêm "Linked Order" block: hiển thị order_id + project name + "Mở Order" button → `database-orders.html?id=<order_id>`. Task `is_standalone` hoặc no `order_id` → hiển thị note thay vì link. Cũ "Order ID" line trong Brief Information bỏ |
| 2026-05-18 | Database Orders | Drawer thêm block "Related Tasks (T)": list mọi task gắn order — gộp từ `BUILT_IN_TASKS` snapshot mirror dataset Task Tracker + `mh-extra-tasks`. Mỗi item click → mở Task Tracker với `?id=<task_id>` auto-open task drawer. Button "Create Task from this Order" enable khi `account_status === 'confirmed'`, redirect Task Tracker với prefill query params |
| 2026-05-18 | Architecture | Cross-page task storage `localStorage['mh-extra-tasks']` (max 100 entries): tasks tạo mới từ Task Tracker hoặc "Create Task from Order" persist ở đây, cả `production-board.js` và `database-orders.js` đều đọc để hiển thị lẫn nhau. Mock dataset built-in vẫn giữ in-memory, không bị duplicate |
| 2026-05-18 | Production | **Phase 0 hardening**: Thêm `robots.txt` chặn crawler (demo), `.env.example` scaffold env vars cho Phase 0/1/2, `assets/config.js` runtime config (Sentry DSN + ENV + RELEASE + feature flags), Sentry browser SDK lazy-load qua CDN trong `app.js` (skip nếu DSN trống, tag user role + email). Wire `<script src="assets/config.js">` TRƯỚC `<script src="assets/app.js">` trên 17/17 HTML pages |
| 2026-05-20 | Realtime | **Realtime notification popup**: Thêm `supabase/enable-realtime.sql` ADD TABLE `notifications` vào publication `supabase_realtime`. `order-form.js doSubmit` sau khi INSERT order → query users role IN (admin,account) status=active → bulk INSERT notifications type=`order_new`. `app.js initNotificationBell` thêm `startRealtime()` subscribe channel `notif-{uid}` filter `user_id=eq.me` → INSERT event push qua WebSocket → `showNotifPopup()` toast 8s + click navigate + markRead. Cleanup channel on beforeunload. Poll 60s giữ làm fallback nếu WebSocket fail |
| 2026-05-20 | Workflow UX | **Stepper UI Account workflow + Push tạo task + Notification bell**: 1) `database-orders.js pushToProduction()` async: idempotent check `tasks.list({order_id})`, nếu có → toast warning + KHÔNG tạo mới; nếu chưa → `generateNextTaskId()` query max TASK-NNNN +1, INSERT `public.tasks` với fields auto-fill từ order (assigned_to, internal_deadline, priority, type, content), INSERT `public.notifications` type=`task_assigned` cho PIC. 2) Stepper UI 4 chấm + đường nối trong drawer top, CSS states `is-done/is-current/is-needinfo/is-cancelled` với pulse animation. `updateStepperState(o)` map account_status + production_status → visual + hint text + button enable/disable theo step. 3) `add-notifications.sql` bảng notifications + trigger touch read_at. `data-store.js` namespace `notifications`: listUnread/listAll/create/markRead/markAllRead/findUserIdByName. `app.js` IIFE initNotificationBell tự gắn dropdown vào tất cả `button[aria-label="Thông báo"]`, badge count unread, poll 60s |
| 2026-05-20 | UX | **4-step action button gradient + Cancel red**: 5 button drawer Account workflow đổi từ outline → gradient nền: Step 1 info blue nhạt, Step 2 amber warning, Step 3 navy CB, Step 4 red CB đậm. Cancel button nền red-700 với margin-left:auto. Hover lift + brightness. Dark mode tinh chỉnh contrast |
| 2026-05-20 | Security | **Xóa Demo Accounts section khỏi login.html**: Bỏ block HTML "Demo Accounts / Test mode" 5 tile click-to-login, xóa ACCOUNTS dictionary + DEMO_PWD hardcoded, xóa loginAsDemo() fallback + `[data-demo]` handler. `loginAs()` chỉ Supabase auth, fail trả error message thật. Lý do: Phase 1 đã có DB account thật, demo tiles để public là security risk |
| 2026-05-20 | Test setup | **5 demo + 4 client test accounts**: Thêm 4 user `client1-4@cb.vn` qua Supabase Auth Dashboard. Reset password tất cả 9 user qua SQL UPDATE `crypt('Cbmedia2026'/'client@test', gen_salt('bf'))` (Supabase UI mới không có "Edit password" trực tiếp, free tier rate limit recovery email 3/h). 5 demo cũ dùng `Cbmedia2026`, 4 client mới dùng `client@test` |
| 2026-05-20 | Data flow | **Tracking lookup Supabase + Client dashboard load + Tracking auth modal**: 1) `tracking.html` lookup async — try `MH.store.orders.get(code)` → mapOrderToTracking() shape, fallback localStorage `mh-submitted-orders`, bỏ hardcoded `DB` khi Supabase enabled. STATUS_PUB map 13 trạng thái → 4 statusKey. 2) `client-dashboard.js loadClientOrdersFromStore()` query orders filter `requester_id` fallback `requester_email`, adapter map Supabase shape → mock ORDERS shape (id/name/type/category/date/deadline/status/pic/preview/final/rating). 3) `tracking.html requireLoginModal()`: bỏ auto-redirect login top, thay bằng modal overlay khi user chưa login + click Tra cứu — preserve mã đã nhập qua `?redirect=` 4) Client scope check mở rộng: legacy `client_scope` HOẶC `requester_email`/`requester_id` match từ `__rawOrder` |
| 2026-05-20 | Routing | **Client redirect fix**: dashboard.html line 653 + ai-tools.js + production-board.js đều redirect role=client về `tracking.html` (legacy). Đổi sang `client-dashboard.html` đúng đích Phase 1 |
| 2026-05-20 | Bug fix | **User Management UNDEFINED badge cho design/editor**: ROLE_LABEL trong user-management.js chỉ có 5 key (admin/manager/account/staff/client) — thiếu design/editor. Bổ sung. Edit modal role dropdown thêm 2 option. CSS thêm `rt--design` (teal) + `rt--editor` (cam) cho cả light + dark. `loadUsersFromStore` adapter default đủ field UI cần (user_id, tag, permission_group, data_scope, status, phone, department) theo role |
| 2026-05-20 | Drawer UX | **Cancel-order modal + drawer action refactor (database-orders)**: 1) Remove 4-card stepper UI (`#wf-stepper`) khỏi drawer head — chỉ giữ logic enable/disable button + hint visibility trong `updateStepperState()`. 2) Push status message (`#wf-hint`) giờ chỉ hiện khi `isPushed` với text "✓ Đã push sang Task Tracker. PIC · Xem task →"; hidden khi chưa push. 3) Action button row reorder: `[Hủy đơn]` canh trái (gradient `#E53935 → #BA110F` + `margin-right: auto`) ⟷ 4 step buttons phải. Step 4 (Push → Production) đổi sang gradient green `#22C55E → #16A34A`. 4) Cancel modal `#cancel-modal` (520px overlay): Order ID + Project readonly + select "Nguyên nhân chính" (5 cause keys) + textarea "Lý do hủy đơn" required + checkbox "Gửi thông báo đến client" default ON. `submitCancel()` validate → mutate local + `persistOrder` patch 4 cancel field + notify client qua `notifications` table type=`order_cancelled`. Row kebab "Hủy đơn" cũng mở modal (bỏ `confirm()` cũ). 5) `tracking.html` thêm `#r-cancel-banner` hiển thị reason/cause/cancelled_by/at khi `raw.production_status === 'cancelled'`. 6) Green-circle tick style cho `.checklist li.ok` + `.push-check li.ok` — nền `#16A34A`, check trắng, `border-radius: 9999px`. 7) `supabase/add-cancel-fields.sql` ADD `cancel_reason/cancel_cause/cancelled_by/cancelled_at` + extend `notifications.type` CHECK với `order_cancelled` + `order_new` |
| 2026-05-20 | Drawer UX | **Tách Hủy đơn xuống hàng riêng + divider (cc05311)**: Refactor `.wf-actions` thành flex-column với 2 sub-block: `.wf-actions-flow` (4 step buttons, flex-wrap row) ⟷ `.wf-actions-danger` (border-top dashed + label `.wf-danger-label` "Hành động khác" uppercase muted + `[Hủy đơn]`). Bỏ `margin-right: auto` khỏi `.act-cancel` (không cần nữa vì đã ở row riêng). Destructive action tách hẳn khỏi 4-step flow giúp visual hierarchy rõ ràng hơn |
| 2026-05-20 | AI Tools (no-code) | **TTS engine swap exploration → reverted về Supertonic** (KHÔNG có code change committed). Đánh giá 3 alternative cho AI Voice tool: (1) VietCloneVoice (Windows .exe, không có web port → loại); (2) OmniVoice k2-fsa (PyTorch model, cần GPU backend → loại); (3) sherpa-onnx + Piper VN (có WASM, có 3 Vietnamese model thật từ HF rhasspy/piper-voices: vais1000-medium 63MB / 25hours_single-low 20MB / vivos-x_low 10MB nhưng yêu cầu build emscripten + 75-80MB bundle → phá zero-build, defer Phase 3). Kết luận: giữ Supertonic spec, Web Speech API tiếp tục làm demo runtime. **Lý do tiếng Việt phát sai trên Play button** = OS không có vi-VN voice → `window.speechSynthesis.getVoices().filter(v.lang.startsWith('vi'))` rỗng → fallback English voice → đọc gibberish. Fix tạm: install Vietnamese voice qua Windows Settings, hoặc dùng Edge browser (có Microsoft cloud neural voices `vi-VN-HoaiMyNeural` / `vi-VN-NamMinhNeural` khi online) |
| 2026-05-21 | Task Dashboard (Module 4) | **Refine Task Dashboard cho internal workload & performance**. Restructure 6-card flat grid thành **5 grouped sections** với 17 KPI total + 6-PIC workload + 2 breakdown chart: (1) **Task Volume** (4): Total Internal Tasks / Linked Tasks (order_id != null && !is_standalone) / Standalone Internal Tasks (is_standalone OR no order_id) / New Internal Tasks (status='pending'). (2) **Workload** (1 KPI + chart): Unassigned Tasks (no assigned_to) + Workload by PIC 6-member bar chart (Duy/Vinh/Linh Chi/Hậu/Đức Anh/Mai Phương, max 8 task/member, bar đỏ khi >max). (3) **Deadline** (3): Due Today (deadline today) / Due This Week (deadline ≤7 days) / Overdue Tasks (deadline past). (4) **Production Status** (6): To Do/Pending (status='pending') / In Progress (received|inprogress|revision|feedback_fix) / Internal Review (status='review') / Revision (status='revision') / Completed (status in completed/delivered) / Blocked (status='paused'). (5) **Performance** (3): On-time Rate (completed/(completed+overdue)*100) / Completed This Week (completed AND last_update trong 7 ngày) / Avg Completion Time (avg days between created_at → last_update cho completed tasks). Inline `loadTaskDashboard()` rewrite: helper `diffDaysFromNow(deadline)`, `isOverdueD()`, `isOpenStatus(s)`. **CHỈ load `MH.store.tasks.list()`** — KHÔNG load orders. `production-board.js`: expand `DRILLDOWN_MAP` từ 5 → **15 keys** với label/desc; `state.quick` switch thêm 9 case mới (pending, revision, blocked, unassigned, linked, standalone, due_today, due_week, completed_week). Page subtitle: "Trả lời câu hỏi: Team đang làm gì, ai đang quá tải, việc nào trễ?". Acceptance: ✓ Answer "team đang làm gì", ✓ Linked vs Standalone clearly separated (4-card volume + 2-row bar chart), ✓ Click-through → production-board.html?dl=KEY với filter chính xác |
| 2026-05-21 | Orders Dashboard (Module 3) | **Refine Orders Dashboard cho Client Order lifecycle**. Restructure 6-card KPI grid thành **3 grouped sections** với 13 KPI total: (1) **Order Intake** (5): Total Client Orders / New Orders / Checking Brief / Need More Info / Confirmed Brief. (2) **Production Flow** (3): In Production / Ready for Delivery / Delivered. (3) **Feedback & Completion** (5): Waiting Feedback / Rated Orders / Average Rating / Completed Orders / Cancelled Orders. **6 Breakdowns**: by branch/department + by request type (existing) + by priority + by Account PIC + by production status (9 status keys) + by delivery status (7 keys) (NEW). Inline `loadOrderDashboard()` rewrite: thêm 7 new KPI compute + 4 new breakdown maps; helper `updateBars(attr, countsMap)` consolidate bar update logic. **CHỈ load `MH.store.orders.list()`** — KHÔNG load tasks (internal tasks thuộc Task Dashboard). `database-orders.js`: expand `DRILLDOWN_MAP` từ 4 keys → **12 keys** với label/desc cho mỗi KPI; `matchesView()` switch thêm 7 case (checking/in_production/ready_for_delivery/delivered/waiting_feedback/rated_orders/cancelled). Page subtitle: "Trả lời câu hỏi: Client orders đang ở giai đoạn nào?". Acceptance: ✓ Answers stage question, ✓ no standalone tasks in metrics, ✓ KPI click-through → database-orders.html?dl=KEY với filter chính xác |
| 2026-05-21 | Task Tracker UX (Module 2) | **Refine Internal Task Tracker — radio group "Loại công việc" + Internal Task naming consistency**. production-board.html: (1) Button "+ Tạo Task" → "+ Giao việc nội bộ" (page head + modal title). (2) Modal: thay `<input type="checkbox" id="tm-standalone">` bằng radio group `name="tm-worktype"` 2 option `value="linked"` (Liên kết với Client Order, default) và `value="standalone"` (Công việc nội bộ độc lập). Mỗi option có icon + bold title + helper text dưới. (3) Khi chọn "standalone" → ẩn `#tm-order-row` + hiện `#tm-standalone-hint` với info card "ℹ Công việc này không liên kết với Client Order nào." (4) Validation save: linked option phải có MEDIA-* order_id, fail toast warning với CTA suggest switch sang standalone. (5) Drawer block "Linked Order" rename → "Loại công việc", thêm `.worktype-badge--linked` (navy "Linked to Client Order") hoặc `.worktype-badge--standalone` (red "Standalone Internal Task"). Linked card giữ "Mở Order" button → database-orders.html?id=. (6) CSS `.worktype-group`, `.worktype-option` (pseudo-class `:has(:checked)` cho selected state navy border + light bg) + `.worktype-badge` light/dark variants. (7) Quick filter chip "Standalone" → "Standalone Internal". Page subtitle update reference "Internal Tasks". "Sửa Task" button → "Sửa công việc". task-dashboard.html: H1 thêm "/ Công việc nội bộ", "+ Tạo Task" → "+ Giao việc nội bộ", "Total Tasks" → "Total Internal Tasks", section "Task vs Order link" → "Internal Tasks: Linked vs Standalone", row labels "Linked to Order" → "Linked to Client Order", "Standalone (internal)" → "Standalone Internal Task". database-orders.js drawer button "Create Task from this Order" → "Giao việc nội bộ từ Order này". `is_standalone` field shape KHÔNG đổi — data model preserved, chỉ UI clarification. Client role vẫn không thấy production-board (role guard trong production-board.js line 18). Acceptance: user có thể tạo cả 2 loại task, drawer phân biệt rõ qua badge, client cannot see |
| 2026-05-21 | UI naming (Module 1) | **Clarify Client Orders vs Internal Tasks distinction qua UI labels**. KHÔNG rename files/routes — chỉ update text. Sidebar labels (14 HTML files): "Order Dashboard" → "Orders Dashboard"; "Database Orders" → "Client Orders"; "Task Tracker" → "Internal Task Tracker". Page H1: database-orders.html "Database Orders" → "Client Orders / Database Orders"; production-board.html "Task Tracker / Production Board" → "Internal Task Tracker / Task Tracker · Production Board"; order-dashboard.html "Order Dashboard" → "Orders Dashboard — Client Orders KPI"; task-dashboard.html "Task Dashboard" → "Task Dashboard — Internal Work Dashboard". Browser `<title>` + meta descriptions cũng update tương ứng. JS user-visible strings update: `chatbot.js` (3 chatbot replies + 3 action button labels "Open Production Board" → "Open Internal Task Tracker", "Open Orders" → "Open Client Orders"); `delivery-log.js` (3 toast/modal text "Production Board" → "Internal Task Tracker"); `production-board.js` (1 access-denied toast). chatbot.html "Open Production Board" link; order-dashboard.html "Database Orders" button text; database-orders.html page subtitle. JS internal IDs (DRILLDOWN_MAP keys, data attributes), routes, file paths đều giữ nguyên — anti-break per acceptance criteria |
| 2026-05-21 | Dashboard wiring | **3 Dashboards wire dynamic load từ Supabase** (theo business clarification "2 flow vận hành: Client Orders + Internal Tasks"). Trước: 3 dashboard là static HTML mockup, KPI luôn hardcoded. Sau: **(1) Order Dashboard** (`d028886`): load `MH.store.orders.list()` → compute 6 KPI (Total/Pending/Checking/Need Info/Confirmed/Completed) + 6 Order Funnel + 7 Branch bars + 6 Type bars với % width. **(2) Task Dashboard** (`cbeb3ee`): load `MH.store.tasks.list()` → compute 6 KPI (Total/In Production/Internal Review/Due Soon/Overdue/On-time Rate) + 4 Workload PIC + 7 Task type + Linked-vs-Standalone. **(3) Master Dashboard** (`34a2c13`): parallel fetch orders + tasks → compute 12 KPI (Client Orders side: total_orders/new_requests/brief_need_info/average_rating/rating_coverage; Internal Tasks side: in_production/internal_review/ready_for_delivery/due_soon/overdue/on_time_rate; Combined: completed) + Workflow Health 6-stage pipeline + Workload by PIC 6 rows với segment bars (progress/review/overdue/done). Business model preserved: orders = client requests lifecycle, tasks = internal items (linked-to-order HOẶC standalone). RLS chặn client thấy tasks. Pattern dùng `data-card-key` + `data-pipe` + `data-pic` + `data-branch` + `data-type` + `data-link` attributes để JS dễ target, không động structure DOM khác. Async load fire-and-forget khi Supabase enabled, no-op khi off |
| 2026-05-21 | Cleanup | **Clear toàn bộ fake data ở phần Vận hành** để anh test bằng order/task thật từ client + admin: 1) `database-orders.js` ORDERS array (18 mock orders 80870→62539 bytes) + BUILT_IN_TASKS snapshot (16 mock tasks) → `[]`. 2) `production-board.js` TASKS array (16 mock tasks 79933→70360) → `[]`. 3) `delivery-log.js` DELIVERIES array (10 mock deliveries 57425→42944) → `[]`. 4) `dashboard.html` (Master): KPI 128/12/5/34/7/9/11/4/68 → 0; pipeline counts 6 stage → 0; workload bar widths (24 spans) → 0%; donut center 48 → 0; delivery tiles 32/11/5/4/1/63 → 0; "2 PIC vượt ngưỡng" badge → "Trong ngưỡng"; 6 mock Alert items → empty state "Chưa có cảnh báo"; 6 mock Activity items → empty state "Chưa có hoạt động". 5) `order-dashboard.html` + `task-dashboard.html`: KPI + pipeline + bar charts widths/counts → 0. 6) `reports.html`: KPI trends (+22/+38/+12) → 0; SLA bar width 92% → 0%; avg-completion 2.4 → 0. 7) `tracking.html`: legacy `const DB = {...}` 7 sample MEDIA codes → `{}`; remove "Thử nhanh:" example chips. **KHÔNG đổi**: User Management, AI Tools, Chatbot, Settings, Client Portal (Hệ thống + Client area giữ nguyên). Khi anh tạo order thật qua client@cb.vn → admin/account thấy ngay; dashboards vẫn 0 vì chưa wire dynamic load (cần Phase 3 nếu muốn live KPI). **Tổng giảm**: 8 file, ~64KB size cleanup |
| 2026-05-20 | Deploy ⚠ | **Railway auto-deploy stuck (CHƯA FIX)**: Sau commit `d2ab83d` (May 19), webhook GitHub→Railway không fire trên các push tiếp theo. 5 commit mới (`ba76ff5`, `dd8006c`, `6c3c5b1`, `cc05311`, `ae13cc6`) đã lên GitHub `origin/main` nhưng KHÔNG trigger deploy. Diagnosis hoàn tất: GitHub App "Railway" có repo access ✓, Source repo+branch+root config đúng ✓, Auto-deploy toggle ON ✓, Wait-for-CI OFF ✓. Toggle Auto-deploy Disable→Enable không fix. Manual Redeploy báo "Problem processing request". Empty commit `ae13cc6` push thêm cũng không trigger. **Workaround pending**: anh thử Disconnect Branch `main` trong Railway Settings → Source → Reconnect (an toàn nhất). Hoặc Disconnect Source Repo full + reconnect (rủi ro phải set lại Root Directory `/Portal_Hub/CB_Creative_Flow`). Production site `cbmediahub.up.railway.app` hiện vẫn ở commit `d2ab83d` cũ — thiếu cancel modal flow + tracking banner + tách Hủy đơn UI mới |
| 2026-05-18 | Cleanup | **Clear demo data + always-swap pattern**: Thêm `supabase/clear-demo.sql` xóa toàn bộ orders/tasks/comments/deliveries/ai_usage/chatbot/activity_log/order_drafts seed, giữ users + schema + storage. Update 5 adapter (`loadOrdersFromStore`/`loadTasksFromStore`/`loadDeliveriesFromStore`/`loadUsersFromStore`/`loadClientOrdersFromStore`) — bỏ điều kiện `remote.length > 0`, luôn replace khi Supabase enabled (kể cả empty). DB là source of truth, không còn fallback mock 18 orders khi Supabase rỗng. `tracking.html` lookup: bỏ fallback hardcoded `DB` khi Supabase enabled. Mục tiêu: client tạo order thật → flow clean, không mix mock data |
| 2026-05-18 | Phase 1 + 2 | **Phase 1 close + Phase 2 storage**: Tạo `supabase/rls.sql` idempotent — helper functions `current_user_role()`/`is_staff()`/`is_admin_or_account()`/`is_admin()` SECURITY DEFINER + 11 bảng policies (users self+staff+admin, orders client-scoped + staff-write, tasks staff-only, task_comments cascade, deliveries admin/account write + client read qua order, ai_usage self+admin, chatbot per-user strict, settings admin-only, activity staff-read, order_drafts self). Tạo `supabase/storage.sql` — 3 buckets (`avatars` public + 2MB + image mimes; `brief-files` private + 50MB; `deliverables` private + 500MB) với storage.objects policies (self-folder write avatar, staff read deliverables, requester via order_id qua split_part path). `assets/data-store.js` thêm namespace `files` với upload/getPublicUrl/signedUrl/list/remove (data URL fallback avatar). `assets/app.js` Profile modal save → fetch data URL → blob → upload Supabase Storage → publicUrl + persist `public.users` metadata. `assets/order-form.js` doSubmit (async) → upload brief files lên `brief-files/{order_id}/...` → INSERT `public.orders` qua `MH.store.orders.create()`. localStorage fallback giữ nguyên |
| 2026-05-18 | Full Phase 1 | **Phase 1 module migration (turn 3 — complete)**: Migrate xong các module còn lại theo cùng pattern Auth+Orders. 1) `production-board.js`: expose `MH_MOCK_TASKS`, `loadTasksFromStore` + `persistTask` + `persistTaskComment` cho updateStatus, save links/meta, comment add (kèm @mention + reply), Create/Edit Task modal (insert + update). Re-build `ORDER_INDEX` sau khi swap dataset. 2) `delivery-log.js`: expose `MH_MOCK_DELIVERIES`, `snapshotDelivery()` extract 8 field thuộc schema, `persistCurDelivery(cur)` hook vào 7 mutation sites qua sed. 3) `user-management.js`: expose `MH_MOCK_USERS`, `loadUsersFromStore` adapter map `name → full_name`, `persistUser(id, patch)` qua raw supabase client (data-store chưa có users.update method). Persist status toggle + edit user (chỉ name/phone/department/role/status — các field thuộc public.users). 4) `ai-tools.js`: `addUsage()` + `saveOutput()` write-through qua `MH.store.aiUsage.log()` + `saveOutput()`. 5) `data-store.js`: thêm namespace `chatbot` với `append()`, `history(sessionId, limit)`, `feedback(messageId, val)` — fallback localStorage `mh-chatbot-history` + `mh-chatbot-feedback`. 6) `chatbot.js`: `pushMessage()` + `saveFeedback()` write-through. **Tất cả module giữ optimistic UI + localStorage fallback**, demo flow zero-breakage khi Supabase chưa cấu hình |
| 2026-05-18 | Auth + Orders | **Phase 1 module migration (turn 2)**: 1) `login.html` migrate sang Supabase Auth-first: nếu `window.MH.supabaseEnabled` thì gọi `MH.store.auth.signIn()`, chờ `onAuthStateChange` mirror session → `mh-user`, redirect dashboard/client-dashboard. Lỗi auth hoặc Supabase chưa cấu hình → fallback `loginAsDemo()` giữ nguyên hành vi cũ (demo accounts + password `cb2026`). 2) `database-orders.js` migrate: expose `window.MH_MOCK_ORDERS = ORDERS` cho cross-page reuse. Thêm `loadOrdersFromStore()` fire-and-forget khi init — Supabase enabled = swap dataset từ `MH.store.orders.list()` + re-render + re-open drawer nếu đang mở. Mutations (`updateStatus`, `pushToProduction`, `save-internal` button) gọi `persistOrder()` write-through (optimistic UI + write Supabase nếu enabled, toast warning nếu sync fail). Khi Supabase chưa cấu hình → hành vi y nguyên hiện tại |
| 2026-05-18 | DB Foundation | **Phase 1 foundation**: Thêm `supabase/schema.sql` (12 bảng: users, orders, tasks, task_comments, deliveries, ai_usage_log, ai_saved_outputs, chatbot_messages, settings, activity_log, order_drafts; trigger touch timestamps + auto-create public.users từ auth.users; views tasks_with_order + orders_with_task_count; RLS policies commented sẵn). `supabase/seed.sql` UPDATE role/name cho 5 demo user + seed 5 orders + 4 tasks + 2 comments. `assets/supabase-client.js` dynamic import @supabase/supabase-js@2 từ esm.sh CDN (zero-build maintained), expose window.MH.supabase + mirror session sang `mh-user`. `assets/data-store.js` abstraction layer (`window.MH.store` với 8 namespaces) — Supabase enabled = query thật, else fallback localStorage. Update `assets/config.js` thêm SUPABASE_URL + SUPABASE_ANON_KEY + FEATURES.SUPABASE_DB. Wire `supabase-client.js + data-store.js` TRƯỚC `app.js` trên 17/17 HTML pages. **Static demo flow KHÔNG bị phá** — chưa fill config thì y nguyên hiện tại |
| 2026-05-18 | AI Tools | Thêm tool **AI Voice (Supertonic)** — category `voice` mới. UI clone preset Supertonic M1-M5/F1-F5, 16 ngôn ngữ, expression tags, quality steps. Engine plan: Supertonic ONNX (github.com/supertone-inc/supertonic). Demo runtime: Web Speech API (`window.speechSynthesis`) — on-device, real audio output, no bundling. Voice Player panel có waveform animation, Play/Pause/Stop, export SSML cho production handoff. Generate output trả markdown gồm Script preview · Voice settings · SSML draft · Production handoff note. SSML preserve expression tags để khi swap backend Supertonic giữ nguyên format |

---

## check_update Protocol

When user types `check_update`:

1. Verify root/assets file map and file sizes against File Inventory.
2. Check `localStorage` keys in JS against `_hot.md` Section 8.
3. Check auth guards and role visibility assumptions in page JS files.
4. Confirm no new modules or files exist that aren't documented.
5. Update `Last updated` in `_hot.md` and `STATUS.md`.
6. Report sync summary in 1–2 lines.

---

## sync_task Protocol

When user types `sync_task`:

1. Update `STATUS.md`: Completed Modules (nếu thay đổi feature), File Inventory (KB sizes), Changelog (entry ngày hôm nay).
2. Update `_hot.md`: convention, token, role, file map, known decisions nếu có thay đổi.
3. Update `README.md`: file mới, role mới, deploy/stack thay đổi.
4. Cập nhật `Last updated` trong cả 3 file.
5. Báo tóm tắt 1–2 dòng.

---

*End of STATUS.md*
