# _hot.md — AI Agent Quick-Load Context

> Đọc file này trước khi sửa project. Nó chứa context ngắn để agent/dev mới tiếp quản đúng style, đúng convention.
>
> *Last updated: 2026-07-10 · Project state: Production-ready beta · Supabase Phase 1+2 LIVE · **🔔 ĐỒNG BỘ NOTIF STATUS NỘI BỘ (2026-07-11)** (`production-board.js notifyTaskStatusChange` + `CONTENT_PROD_NOTIFY`) — Feedback #1.3 (bản v2, KHÔNG đụng client): audit ma trận notif status theo role, vá 3 chỗ thủng NỘI BỘ: **`inprogress`** → admin/account ("PIC đã bắt đầu"); **`delivered`/`completed`** → admin/account (link Task Tracker); order NỘI BỘ (Content) delivered/completed → lead_content qua 2 key mới `delivered`/`completed` trong `CONTENT_PROD_NOTIFY` (deep-link content-team). **CỐ Ý bỏ nhánh requester/client** (bản #1.3 v1 rollback vì báo client task nội bộ → nhầm với Final delivery). Notif client (Preview/Final/order_status) do database-orders lo, giữ nguyên. Coverage cũ vẫn: review→account, revision/feedback_fix/ready→PIC, gán→PIC. KHÔNG cần SQL. · **🔐 FIX "TỰ ĐĂNG XUẤT" / BỊ ĐÁ VỀ LOGIN (2026-07-10)** (`supabase-client.js onAuthStateChange`): triệu chứng — sau khi gửi yêu cầu hoặc dùng lâu, tài khoản bị "đăng xuất" / bấm Trang chủ-Quay lại bị đưa về `login.html`. Nguyên nhân KHÔNG phải guard các trang: handler mirror session→`mh-user` chạy MỖI auth event (SIGNED_IN/INITIAL_SESSION/**TOKEN_REFRESHED**/USER_UPDATED/SIGNED_OUT) và (a) ghi đè `mh-user` vô điều kiện — khi query `users` row transient-fail (mạng/RLS) → `res.data=null` → fallback `role='client'` (SSO user KHÔNG có `user_metadata.role`) → admin/account bị **HẠ role** → guard trang (`if(!['admin','account'...].includes(role)) redirect login`) đá về login = cảm giác tự đăng xuất; (b) xóa `mh-user` ngay khi có **SIGNED_OUT** dù token-refresh churn / nhiều tab phát SIGNED_OUT tạm thời. Fix: chỉ ghi khi LẤY ĐƯỢC users row (hoặc lần đầu chưa có mh-user); luôn **MERGE với mh-user cũ** (không bao giờ regress role/name — nếu `!u && prev.role` cùng id thì GIỮ NGUYÊN, return sớm); SIGNED_OUT **double-check `getSession()`** trước khi xóa (còn session → giữ). Guard các trang vẫn đọc `mh-user` sync như cũ, giờ không bị ghi sai. _(Feedback Portal Hub #1.1 logout-sau-submit + #1.2 điều hướng về login. Còn #1.3 notif realtime / #1.4 content tạo task (migration `add-content-self-initiative.sql` + name-match RLS) / #1.5 task gấp (chưa có push ưu tiên) / #1.6 lag (poll 60s + full list()) / #1.7 link — phần lớn migration/design, xem review chat.)_ · **🔒 DESIGN/EDITOR KHÔNG GIAO VIỆC NỘI BỘ (2026-07-08)** (`task-dashboard.html`+`production-board.js`): chỉ admin/account/lead_media được tạo task nội bộ. Kẽ hở cũ: nút **"Giao việc nội bộ"** ở Task Dashboard là `<a href="production-board.html?createTask=1">` KHÔNG gate role → design/editor thấy + bấm được; `handleCreateFromURL` mở modal cho MỌI role trừ client → tạo được dù nút board (`#btn-create-task`) đã ẩn. Fix 3 lớp: (1) task-dashboard nút thêm `id="td-create-task"` + inline script ẩn khi role ∉ {admin,account,lead_media}; (2) `handleCreateFromURL` guard `if(!['admin','account'].includes(user.role)) return` (lead_media alias→account ở ~dòng 45); (3) `openTaskModal` chặn cả CREATE (`!editId && !admin/account` → toast "Không có quyền"), trước chỉ chặn EDIT. Design/Editor vẫn nhận & đổi status task của mình. · **⭐ RATING BẮT BUỘC FEEDBACK (2026-07-08)** (`client-dashboard.js` handler `rating-submit` ~1214 + `client-dashboard.html` modal ~432): client đánh giá order giờ PHẢI để lại nhận xét — chỉ chọn sao mà bỏ trống ô "Nhận xét" → toast `warning` "Cần nhận xét" + focus + return, KHÔNG submit (chặn khi `comment.trim()===''`, không min-length). Label modal đổi "(không bắt buộc)"→dấu `*` đỏ + placeholder "(bắt buộc)". Entry point rating DUY NHẤT là modal này. Nudge drawer "...Không bắt buộc" giữ nguyên (nói về việc CÓ đánh giá hay không, khác "đã rating thì phải kèm feedback"). · **🐛 FIX NOTI "MA" — Content Wording notify tách rời persist (2026-07-08)** (`database-orders.js transferToWording`): triệu chứng Lead nhận noti "Order mới trong Content Inbox" nhưng **Content Inbox rỗng**. Nguyên nhân: order UPDATE và notification INSERT là 2 thao tác ĐỘC LẬP — `notifyContentWording` bắn vô điều kiện, còn order update KHÔNG land thì Inbox (đọc status DB thật) trống. 2 đường update-không-land: (a) lỗi CHECK/thiếu migration (`account_status='wording'` cần `add-brief-wording-fields.sql`, `brief_wording_status='assigned'` cần `add-content-team.sql`) → 23514 throw (persistOrder cũ chỉ `.catch` log/toast nhẹ); (b) **RLS khớp 0 dòng → `orders.update(...).maybeSingle()` trả `null` KHÔNG kèm error → im lặng hoàn toàn**. Fix: `transferToWording` → **async, GHI DB TRƯỚC + await + xác nhận `saved.brief_wording_status==='assigned' && saved.account_status==='wording'` rồi MỚI notify + toast success**; nếu throw hoặc saved null/sai → **toast `danger` nêu rõ nguyên nhân (migration/RLS) + KHÔNG gửi noti** (bỏ optimistic mutate; đồng bộ `Object.assign(o, saved)` từ row DB). Đơn stuck cũ: Account bấm lại "Chuyển Content Wording" (guard `ws==='none'` vẫn cho retry) → giờ hiện lỗi thật thay vì noti ma. · **⏰✅ SLA LEAD DUYỆT + PIC SELF-CHECKLIST (2026-07-08)** (`content-workbench.js`+`content-team.js`+`data-store.js`+**`supabase/add-content-review-sla.sql`**): 2 cột mới `content_tasks.lead_review_due` (timestamptz) + `pic_checklist` (jsonb `[{label,done}]`). **Q2 — SLA duyệt**: khi PIC "Gửi Lead Content duyệt" (`submitTaskToLead`), set `lead_review_due` = ô `#wt-review-due` PIC nhập, **fallback auto = giờ gửi + `REVIEW_SLA_HOURS`(24h)**. content-workbench drawer có block **"Hạn Lead duyệt (SLA)"** (editable khi submittable, read-only + đỏ khi đã gửi/quá hạn — `reviewOverdue(t)`= status submitted_to_lead && due<now). content-team (Lead): `reviewLateCT(t)` → dl "Hạn Lead duyệt" tô `cwb-overdue`, callout đỏ đầu Lead Review Panel, chip đỏ **"quá hạn duyệt"** cạnh status ở bảng Initiatives + task con của Plan. Due chỉ hiện/flag khi `submitted_to_lead` (đổi status → tự ẩn, resubmit set due mới; không cần clear cột). **Q3 — PIC tự add checklist**: drawer PIC content-workbench thêm block **"Checklist của PIC"** (block-letter `P`, sau Quality Checklist) — PIC thêm (`#wt-picchk-add`/Enter) / xóa (`.pic-chk-del`, `preventDefault` vì nút nằm trong `<label>`) / tick, progress bar; lưu `pic_checklist` **NGAY** khi add/remove (`persistTask`), tick lưu khi "Lưu nháp"/submit (`collectPicChk` trong `collectTaskForm`). **KHÔNG bắt buộc** để gửi duyệt (tách khỏi `quality_checklist` 9 mục cứng). Lead xem read-only `pcBlock` (n/total) trong Content Task Drawer. **data-store**: `contentTasks.create/update` nâng **loop-strip** (mirror orders) + allowlist thêm `lead_review_due`/`pic_checklist` → chưa chạy migration vẫn submit được (bỏ cột thiếu, không throw cả patch). **⚠ chạy `add-content-review-sla.sql`** (2 cột + index `submitted_to_lead`; idempotent). · **🔽 PIC CONTENT = DROPDOWN ĐỒNG BỘ (2026-07-08)** (`content-team.js`): mọi ô gán PIC Content đổi từ `<input list=datalist>` (gõ tay) → **`<select>` thật** như PIC Media. Nguyên nhân cũ "không tìm thấy PIC đồng bộ": options lấy từ `CONTENT_USERS` = `users.list({role:'content',status:'active'})` mà `loadContentUsers()` CHỈ chạy khi `supabaseEnabled` → demo/Supabase off/chưa seed content user ⇒ datalist RỖNG. Fix: helper `picSelectOptions(current)` + `contentPicNames()` gộp `CONTENT_USERS` + PIC đã dùng (`CONTENT_TASKS.assigned_pic`/`ORDERS.brief_wording_pic`) + **`FALLBACK_CONTENT_PICS=['Thu Hà','Minh Anh','Bảo Trân']`** (khớp seed demo — LUÔN có option kể cả offline, giống Media hardcode) + luôn giữ giá trị hiện tại, sort vi, option rỗng "— Chọn PIC Content —". Áp **6 surface**: order-wording Lead assign (`ct-pic`) · Content Task drawer reassign (`ctm-t-pic`) · Plan "Thêm task con" (`cct-pic`) · Initiative create (`cim-pic`) · Ads assign (`ads-pic`) · Ads task-tracker (`ads-tt-pic`). Handler đọc `.value` không đổi (select tương thích), guard "Thiếu PIC" vẫn chạy (option rỗng value=""). Gỡ `picDatalistOptions()` (hết caller) + biến `opts` thừa. **KHÔNG cần SQL.** _(Còn Q2 SLA duyệt content + Q3 PIC tự add checklist — chưa làm, chờ tiếp.)_ · **✍️ CONTENT TỰ ĐỀ XUẤT TASK — 1 GATE LEAD DUYỆT NỘI DUNG (2026-07-06)** (`content-workbench.html`+`content-workbench.js`+`content-team.js`+**`supabase/add-content-self-initiative.sql`**): role `content` (và admin) giờ CHỦ ĐỘNG tạo content task ngay tab "Content Tasks của tôi" — nút **"Đề xuất task mới"** (`#cwbt-new-task`, ẩn với role khác) → modal `#cwbt-modal` (Tiêu đề* · Brief · Output types chips default `social_post` · Ưu tiên · Hạn wording · checkbox **"Cần Media thiết kế"** → media type/deadline/note map `request_type`/`media_note`/`production_deadline`) → `contentTasks.create({source:'content_initiated', assigned_pic:CHÍNH MÌNH, status:'in_progress', created_by:user.name})` → mở drawer viết luôn + notify lead_content tức thì (`notifyLeadTask`, link `content-team.html?task=`). **1 GATE duy nhất trước Media = Lead duyệt NỘI DUNG** (chốt với anh Jett — KHÔNG có gate "chấp thuận đề xuất"): flow sau tạo giữ nguyên 100% sẵn có — viết → "Gửi Lead Content duyệt" (`submitted_to_lead`) → Lead duyệt content-team (`ctm-rev-approve` → `lead_approved`) → PIC quay lại workbench mới thấy form **"Gửi sang Media"** (`canSendMediaTask` KHÔNG đổi) → order nội bộ `MEDIA-YYYYMMDD-CTxxx` (`internal_media_request`, `client_visible=false`). Lead side: bảng Initiatives thêm chip **"PIC tự đề xuất"** khi `created_by===assigned_pic`. Chống double-submit `selfModalSaving`. **⚠ chạy `add-content-self-initiative.sql`**: policy INSERT `content_tasks` cho content — WITH CHECK `source='content_initiated'` AND `assigned_pic=(SELECT name FROM users WHERE id=auth.uid())` (không tạo hộ người khác/giả nguồn client_order; `content_task_comments` INSERT content đã có sẵn). Chưa chạy migration → create throw, toast nêu rõ tên file SQL. Verified CDP headless full flow (content tạo → GATE: KHÔNG có nút Media trước duyệt → submit Lead → Lead approve thấy chip tự đề xuất → nút "Gửi sang Media" hiện → order mock đúng kind/ẩn client), 0 JS errors. · **👁 LEAD CONTENT XEM CLIENT ORDERS — READ-ONLY + COMMENT (2026-07-06)** (`database-orders.js`+`app.js`+`content-team.js`+`data-store.js`+`styles.css`+**`supabase/add-lead-content-order-view.sql`**): mở guard database-orders cho `lead_content` — `READONLY = system_supervisor || lead_content` (tái dùng NGUYÊN cơ chế monitor: kebab ẩn, input/select/textarea disabled, mọi button mutation ẩn qua `applyDrawerReadonly`; ngoại lệ `RO_KEEP_IDS` = drawer-close + `lc-comment-input` + `lc-send-comment`), badge **"Chế độ chỉ xem · Lead Content"** (`.do-viewonly-badge`) cạnh H1 (supervisor cũng có badge này giờ). **Comment nội bộ**: section drawer mới **"Comment Lead Content"** (`ow-lc-comments` — mọi role thấy thread khi có nội dung, form gửi chỉ lead_content) → ghi vào **cột MỚI `orders.lead_content_notes`** qua **RPC `append_lead_content_order_note`** (SECURITY DEFINER, role check lead_content/admin, CHỈ append text `[dd/mm/yyyy hh:mm · Tên · Lead Content] …`, không đụng cột nghiệp vụ nào; demo fallback mutate mock cùng format; chống double-click `lcSending`). **⚠ VÌ SAO KHÔNG dùng `internal_note`**: Client Portal HIỂN THỊ internal_note khi `account_status='needinfo'` (client-dashboard.js ~265) → comment lead sẽ LỘ ra client; `lead_content_notes` client không bao giờ đọc. Gửi xong notify admin+account (type `system`, link `database-orders.html?id=`). **Nav**: `app.js revealClientOrdersForLeadContent()` — append `lead_content` vào `data-show-roles` mục Client Orders LÚC RUNTIME (chỉ chạy khi role=lead_content → né substring trap "content"⊂"lead_content", attr tĩnh vẫn "admin,account") + badge `View only`; lead_content KHÔNG có CSS `:has` ẩn database-orders nên reveal ăn ngay. **Content Team**: drawer wording (`buildOrderActions`) + task drawer (Media Order block) hiện link **"Mở Order gốc (chỉ xem)"** cho Lead. **Deep-link**: database-orders nhận cả `?id=` lẫn `?order=` (alias). lead_content SELECT orders ĐÃ CÓ SẴN từ `add-content-team.sql` — không cần policy đọc mới. Role `content` VẪN bị chặn redirect. LEAD KHÔNG ĐƯỢC: confirm/push/PIC/status/deadline/hủy/preview/final/sửa brief — tất cả bị READONLY khóa UI + không có đường ghi DB (RPC chỉ append note). **⚠ chạy `add-lead-content-order-view.sql`** (cột + RPC; chưa chạy → XEM vẫn OK, gửi comment báo lỗi rõ kèm tên file SQL). · **🔔 ADS CLIENT NOTIFY + lead_media order_new (2026-07-06)**: khép kín noti phía client cho Ads flow — `content-team.js` thêm `ADS_CLIENT_NOTIFY`+`notifyAdsClient(o,newStatus,prevStatus)` gọi trong `adsAdvance`: báo client 3 nấc **`running`** (bắt đầu chạy; resume `paused`→`running` KHÔNG re-notify) · **`completed`** · **`cancelled`** — type `order_status_changed` (sẵn trong CHECK constraint), lookup `requester_id` fallback email, link `tracking.html?code=` (bell client tự resolve `client-dashboard.html?order=` qua `related_entity_id`); 3 status này CHỈ set qua `adsAdvance` (workbench sync tối đa tới `creative_ready`) nên 1 chokepoint đủ. `order-form.js` notify `order_new` mở rộng `['admin','account','lead_media']` (lead_media quyền ngang Account). Số đếm build sync lại: **21 HTML (+5 demo `_*.html`) · 21 JS**. · **🛡️ AI BRAND SAFETY CHECKER — module #15 (2026-07-03)**: kiểm duyệt hình ảnh AI theo checklist thương hiệu CB Centres (spec `../Brief_Wflow/CB_Brand_AI_Safety/CB_AI_Brand_Safety_Checker_Planning_Prompt_Claude.md`). Page mới **`brand-check.html`** + **`assets/brand-check.js`** (prefix class/id `bc-*` trong styles.css), nav **"Brand Safety"** inject qua **`app.js injectBrandCheckNav()`** (nhóm Hệ thống, chèn trước AI Tools; mọi role nội bộ kể cả lead_media + system_supervisor). **CLIENT (giáo viên/chi nhánh) ĐÃ MỞ (2026-07-03)**: bỏ guard redirect; brand-check.html thay sidebar nội bộ khi role=client bằng **sidebar ĐỒNG BỘ 1:1 Client Portal (2026-07-04)** — cùng nhóm Yêu cầu (Tổng quan · Yêu cầu của tôi + badge) / Công cụ (Kiểm tra ảnh AI active · AI Tools · CB Assistant · Thông báo + badge · Hỗ trợ), deep-link `client-dashboard.html?tab=overview|orders|notif` (client-dashboard.js thêm hỗ trợ `?tab=`; badge đổ best-effort từ MH.store orders/notifications); lối vào từ `client-dashboard.html` mục "Công cụ"; backend RLS đã sẵn (dùng `auth.uid()` áp mọi user). **Banner kết luận hành động** `actionVerdict()` đầu drawer (`.bc-verdict--pass/rev/fail/review`): map (ai_status × usage_group) → câu rõ ràng cho user — Nhóm 1 PASS "Được dùng nội bộ" · Nhóm 2 PASS "Đủ điều kiện tự đăng" · REQUIRES_MEDIA_REVIEW "Bắt buộc gửi Media duyệt (chưa được tự đăng)" · FAIL/NEEDS_REVISION tương ứng (2 tầng kiểm duyệt: AI tự động cho Nhóm 1&2 đạt → tự dùng; chỉ ca rủi ro + Nhóm 3 về Media). **Form đơn giản hóa (2026-07-04)**: view Kiểm tra mới thành **2 cột** (`.bc-new-col-img` trái = ảnh + tiêu chí/lưu ý · `.bc-new-col-info` phải = thông tin + nhóm + submit); **BỎ ô Chi nhánh/Phòng ban** (auto từ `user.department`/`user.branch_name` trong collectMetadata) · **BỎ Ngày dự kiến** · **BỎ hẳn Checklist khai báo nhanh (7 flag)** → override giờ dựa `usage_group` + **đánh giá trực quan của AI** (Gemini phát hiện logo/mascot/quảng cáo trong ảnh, set requires_media_review; applyOverrideRules chỉ escalate không hạ cấp nên vẫn an toàn). **invokeAnalyze auto-retry 1 lần** khi FunctionsFetchError (cold-start). **⚠ Function transport OK** (probe curl: POST không token vẫn 500 chạy, OPTIONS 200, trailing-slash không sao) — "Failed to send a request" là cold-start/extension, không phải JWT/CORS/URL. **Flow**: upload JPG/PNG/WEBP ≤10MB (dropzone) + metadata (title · chi nhánh/đơn vị · mục đích · kênh · **nhóm nội dung** group_1_internal/group_2_self_check/group_3_media_review (radio cards) · ngày dự kiến · **7 quick flags** has_logo/has_mascot/has_uniform/has_cb_facility/is_admission_or_ads/involves_partner/contains_sensitive_info) → AI Vision chấm **6 tiêu chí /100** (logo_identity 25 · brand_color 15 · text_quality 15 · ai_artifacts 15 · education_suitability 15 · communication_risk 15; 85+=PASS · 70–84=NEEDS_REVISION · <70=FAIL) → **RULE ENGINE v2 (2026-07-04)**: **CHỈ Nhóm 3 ⇒ `REQUIRES_MEDIA_REVIEW`**; Nhóm 1/2 status cuối = `statusFromScore(điểm)` — AI muốn Media xem (requires_media_review/logo fail/`media_review_reasons`) chỉ thành **`ai_warnings[]`** (box vàng khuyến nghị, KHÔNG chặn, KHÔNG notify Media). System prompt tách 2 việc: **điểm = chất lượng ảnh khách quan** (có logo/mascot KHÔNG bị trừ điểm — fix vụ Gemini chấm 30 cho poster đẹp) · **khuyến nghị = cờ riêng** `requires_media_review`+`media_review_reasons` (schema mới, cả RESULT_SCHEMA + GEMINI_SCHEMA). **Tag ĐÃ DUYỆT**: PASS Nhóm 1/2 → auto-duyệt (`approved_at`+`approval_source='auto_ai'`, mã duyệt = chính check_code "BSC-… · ĐÃ DUYỆT" + nút copy trong drawer); Media APPROVED → `'media'`; REJECTED/REVISION_REQUIRED → **thu hồi** (NULL). Verdict banner nói thẳng **ĐẠT/KHÔNG ĐẠT + lý do** (ưu tiên manual_status trước ai_status; KHÔNG ĐẠT kèm 3 detected_issues đầu). History: badge ĐÃ DUYỆT (AI/Media) + filter `bc-filter-approved` + search match check_code; CSV += approved/approved_at/approval_source. data-store `brandChecks.create/update` có **loop-strip** cột thiếu (quên chạy lại migration vẫn lưu được, mất cột approval). ⚠ cột mới `approved_at`/`approval_source` trong `add-brand-check.sql` — **chạy lại file**. **⚠ RULE ENGINE NẰM Ở 2 NƠI**: Edge Function (nguồn chuẩn, server-side) + `brand-check.js applyOverrideRules()` (mirror cho demo/fallback) — **sửa rule phải sửa CẢ HAI**. Lưu qua **`MH.store.brandChecks`** (data-store namespace mới: list/get/create/update/delete/criteriaByCheck/addCriteria; Supabase bảng `brand_checks`+`brand_check_criteria`, fallback localStorage `mh-brand-checks` — ảnh offline chỉ giữ dataURL ≤400KB) → **drawer kết quả** (`bc-drawer`: ảnh signed URL · điểm to · badge ai_status+manual · summary · criteria table · detected_issues/required_actions · override box đỏ · metadata · **manual review panel** admin/account/lead_media: APPROVED/REVISION_REQUIRED/REJECTED/ARCHIVED + note → notify uploader) + tab **History** (filter ai_status/manual/nhóm + search + **CSV export** BOM UTF-8; non-media chỉ thấy lượt của mình — RLS scope server, fallback lọc email) + tab **Dashboard** (`data-show-roles` media+supervisor: KPI + 4 chart CSS bars — phân bố status · top lỗi tiêu chí đọc từ `ai_result_json.criteria` (không query bảng con) · theo chi nhánh · trend 14 ngày) + deep-link `?id=` + realtime `brand_checks` + poll 60s. **AI call**: Supabase ON → upload bucket private **`brand-check-images`** path `{auth.uid}/{key}/{ts}-{file}` (RLS theo folder đầu = uid) → `supabase.functions.invoke('brand-check-analyze')` — **Edge Function** (`supabase/functions/brand-check-analyze/index.ts`) tải ảnh bằng service role, gọi provider theo secret `BRAND_CHECK_PROVIDER` (**gemini mặc định** — model `BRAND_CHECK_MODEL` default `gemini-2.5-flash`, ép JSON qua `responseSchema` (GEMINI_SCHEMA — định dạng type CHỮ HOA khác Anthropic) + safety `BLOCK_ONLY_HIGH` + parse chống ```json fences; anthropic dùng `output_config.format`, openai `response_format` — đều viết sẵn, đổi qua `BRAND_CHECK_PROVIDER`), chạy override server-side, trả JSON final; **function lỗi/chưa deploy → fallback** record `NEEDS_MANUAL_REVIEW` (hoặc `REQUIRES_MEDIA_REVIEW` nếu metadata rủi ro) + toast. Supabase OFF → **DEMO** `simulateAiResult()` deterministic theo title (badge DEMO, confidence low, summary ghi rõ mô phỏng). Notify: REQUIRES_MEDIA_REVIEW/NEEDS_MANUAL_REVIEW → insert notifications **type `system`** (nằm sẵn trong CHECK constraint, `related_entity_type=null`) cho admin+lead_media, link `brand-check.html?id=`; Media hậu kiểm → notify uploader. Status enum: ai_status `PASS/NEEDS_REVISION/FAIL/REQUIRES_MEDIA_REVIEW/NEEDS_MANUAL_REVIEW` · manual_status `PENDING/APPROVED/REVISION_REQUIRED/REJECTED/ARCHIVED`. **⚠ chạy `supabase/add-brand-check.sql`** (2 bảng + RLS owner-insert/select-mình · media select-all+update · supervisor select-all · bucket policy prefix `cbbrand_` · Realtime) **+ deploy Edge Function**: `supabase functions deploy brand-check-analyze` + `supabase secrets set BRAND_CHECK_PROVIDER=anthropic ANTHROPIC_API_KEY=sk-ant-...`. API key KHÔNG bao giờ ở frontend/config.js. **20 HTML · 17 JS**. · **📣 ADS ORDERS — luồng Client → Content Team (2026-07-01)**: luồng thứ 2 độc lập, TÁCH khỏi Media/Production. Client bấm CTA **"Gửi yêu cầu chạy Ads"** (client-dashboard 2 CTA riêng: thiết kế/Media + chạy Ads) → `request.html?type=ads` (form **5 section** tự chứa trong **`assets/ads-order-form.js`** — ẩn form Media `#form-section`; **KHÔNG hỏi Creative** — Content chủ động tạo Internal Media Request nếu cần). **Chọn loại yêu cầu**: request.html có **segmented toggle** (`.rf-type-switch`) `showMedia()`/`showAds()` (đổi form in-place, `history.replaceState` ?type) + **popup chooser** `#rf-chooser` (reuse `.preview-modal`, 2 card Media/Ads kèm **gợi ý hạng mục** `.rf-choice-hint`) hiện khi vào request.html **KHÔNG có `?type`** (từ nav); có `?type=ads`/`?type=media`/`mode=revision` → bỏ qua popup vào thẳng; đóng popup (X/nền/Esc) → mặc định Media. **Form Ads**: layout 3 cột như Media (`.order-form-layout` + `.form-side` stepper 5 bước `data-ads-step` scroll-spy + `.form-tips` mẹo/quy trình), trường **đính kèm File kế hoạch** (`#ads_plan_file` upload `brief-files/{ADS-id}/plan-…` + `plan_file_url` Drive), **Lưu nháp** (`mh-ads-draft`) + **Preview Brief** (`#ads-preview-modal`). **`tracking.html`**: tra cứu được `ADS-*` — map `ads_status`→public status (`ADS_TRACK`, assignee='Content Team', %theo lifecycle); **CHẶN** đơn nội bộ (`client_visible=false`/`internal_*`/`origin content_team|ads_order`) khỏi tra cứu. **⚠ GOTCHA AD-BLOCKER (fix 2026-07-01)**: sau deploy, khu vực fill form Ads biến mất do **ad-blocker (uBlock/AdBlock/Brave) ẩn element có CLASS chứa "ads"** (`.ads-main`/`.ads-file-drop`/`.ads-submit-bar`… → `display:none`), trong khi `.form-side`/`.form-tips` + `#ads-form` (id) sống. **Fix: đổi CLASS `ads-*`→`cmp-*`** + badge `badge-ads-order`→`badge-campaign`, `badge-internal-ads`→`badge-internal-src` (GIỮ id `#ads-form`/`#ads-sec-*`/`#ads_*`/data `request_type='ads'`/`order_kind`/`ads_status`/`?type=ads` — filter chỉ class-based). **QUY TẮC: KHÔNG đặt "ads"/"ad"/"banner"/"sponsor"/"promo" trong class name DOM.** Tile Media cũ **"Ads / Post Basic" đổi thành "Post"**, `request_type` `ads`→`post` (giải phóng `ads` cho campaign). Submit → order với `order_kind='ads_order'`/`request_type='ads'`/`owner_team='content'`/`client_visible=true`/`ads_status='submitted'`/`ads_detail`(jsonb 5 section)/prefix **`ADS-2026-xxxx`**/`account_status='confirmed'`; notify `lead_content` (KHÔNG admin/account). **Lead Content** xử lý ở `content-team.html?tab=ads-orders` (tab **Ads Orders** + drawer `#ctm-ads-drawer`, `content-team.js` module ADS): lifecycle riêng **`ads_status`** (18 trạng thái, TÁCH `brief_wording_status`) qua `adsAdvance` state-machine buttons · gán PIC (`brief_wording_pic`) · **Tách Content Tasks** (`contentTasks.create` source=`ads_order`/`order_id`=ADS-id → PIC làm ở `content-workbench.html` tab "Content Tasks của tôi") · **Tạo Internal Media Request** (`createAdsMediaRequest`: order `order_kind='internal_ads_media_request'`/`origin='ads_order'`/`client_visible=false`/`source_ads_order_id`/prefix **`ADS-MEDIA-2026-xxx`** → Production như order thường; set `ads_status='media_request_created'`+`ads_media_order_id`). Persist ads qua `orders.update` trực tiếp (RLS lead_content UPDATE ads_order). **database-orders** LỌC BỎ `order_kind='ads_order'` khỏi Client Orders (Account KHÔNG bottleneck); `isInternalOrder`+badge `#d-internal-badge` mở rộng nhận `internal_ads_media_request`/`origin=ads_order` → "Internal · From Ads". **production-board `notifyContentOnProdStatus`** + **database-orders `notifyContentRequester`** nhận `source_ads_order_id` → deep-link `content-team.html?tab=ads-orders&id=`. **client-dashboard**: ads order (client_visible=true) hiện với **public status** map client-side (`ADS_PUB` 8 nhãn: Đã nhận yêu cầu Ads→Đang chuẩn bị nội dung→Đang kiểm tra nội dung→Đang chuẩn bị thiết kế/creative→Sẵn sàng chạy Ads→Đang chạy→Hoàn thành→Đã hủy) qua `isAdsOrder`/`adsInfo`/`pubStatus`/`pubProgress`/`adsTlHtml`; drawer CHỈ show campaign summary + status (KHÔNG task nội bộ/PIC/media request); badge `.badge-ads-order`. **data-store** loop-strip allowlist thêm `owner_team`/`ads_status`/`ads_detail`/`source_ads_order_id`. Badges CSS mới: `.badge-ads-order`/`.badge-from-client`/`.badge-need-creative`/`.badge-internal-ads`/`.badge-client-hidden`. reports `ROLE_OF_TASK_TYPE`+`post`→design. **⚠ chạy `supabase/add-ads-orders.sql`** (cột ads + request_type CHECK += `post` + content_tasks.source += `ads_order` + RLS lead_content UPDATE ads_order/INSERT internal_ads_media_request + content INSERT ads-media). Prefix rõ 3 loại: `ADS-`=Ads Order · `MEDIA-`=Media Order · `ADS-MEDIA-`=Internal creative từ Ads. **🔒 KHÉP KÍN ADS FLOW (2026-07-02, 4 fix)**: **(1)** noti gán PIC/trả chỉnh Ads đổi link `content-team?tab=ads-orders`→**`content-workbench.html`** (role content bị guard đá khỏi content-team → deep-link cũ bounce); **(2)** `content-workbench createMediaRequestInline` nhận diện task `source='ads_order'` → tạo order **`internal_ads_media_request`**/`origin='ads_order'`/`source_ads_order_id`/prefix `ADS-MEDIA-` (trước tạo nhầm kind From Content) + Ads drawer `mediaId` fallback tìm theo `source_ads_order_id`; **(3) auto-sync `ads_status` theo Content Task**: workbench `syncAdsOrderFromTask` (PIC start→`writing_ads_content` · submit→`submitted_to_lead` · media request→`media_request_created`+`ads_media_order_id` · completeTask→`creative_ready`) + content-team `syncAdsFromContentTask` (Lead trả chỉnh→`lead_revision` · duyệt→`need_creative`/`lead_approved`) — đều guard `allowedFrom` không kéo lùi trạng thái launch; Lead vẫn bấm tay các nấc launch (ready_to_launch/running/completed). **⚠ RLS mới `orders content update ads`** (PIC sync ads_status — trong `add-ads-orders.sql`, chạy lại); **(4)** content-team Dashboard thêm card **"Ads Orders — theo trạng thái"** (6 bucket bar) + **"Ads cần chú ý"** (`renderAdsDashboard`, submitted/lead_checking/submitted_to_lead/need_creative) + **Calendar event `ads-launch`** "Lịch lên Ads" (`ads_detail.desired_launch_date` fallback `requested_deadline`; ads order KHÔNG rơi vào `order-deadline` chung; role content không thấy; navTarget `content-team?tab=ads-orders&id=`; chip filter mới trong calendar.html). · **🔐 Đăng nhập Google Workspace SSO (@cbcentres.com) (2026-06-19)**: chuyển login từ email+mật khẩu demo (@cb.vn) sang **Google SSO** cho mọi người. `data-store.js auth.signInWithGoogle(redirectTo)` → `signInWithOAuth({provider:'google', queryParams:{hd:'cbcentres.com',prompt:'select_account'}})`. `login.html`: **2 cách đăng nhập hiển thị song song** — (1) email + mật khẩu (Supabase signInWithPassword, nút primary) + (2) nút "Đăng nhập với Gmail công ty" (Google SSO, secondary, dưới divider "hoặc"); `handleOAuthReturn()` chạy khi quay lại (cờ `sessionStorage['mh-oauth-pending']` phân biệt OAuth-return vs phiên cũ) → **domain guard** (email không `@cbcentres.com` → signOut + báo lỗi) → poll `mh-user` → route theo role; `roleHome.lead_content` sửa `supervisor-planning`→**`content-team.html`**. Provision role: `supabase/add-google-sso-cbcentres.sql` — bảng `user_role_map(email,role,name)` (seed thủ công email công ty+role+TÊN khớp assigned_pic) + nâng trigger `handle_new_auth_user` lấy role/name từ map theo email (Google không gửi metadata→mặc định 'client' nếu thiếu map). Data nội bộ keyed theo TÊN nên uuid đổi không phá assignment. ⚠ NGOÀI CODE: bật Google provider (Supabase Auth) + OAuth Client (Google Cloud, redirect `…supabase.co/auth/v1/callback`, app "Internal" cbcentres = enforcement domain thật) + chạy migration + seed user_role_map + mỗi người SSO 1 lần. Guard mirror supabase-client.js CỐ Ý bỏ (tránh chặn admin @cb.vn fallback). · **🎯 PIC Content gửi Internal Media Order + checklist hoàn tất NGAY trong content-workbench (2026-06-19)**: chuyển quyền tạo Internal Media Request từ Lead → **PIC Content**, gói trọn trong Content Task Drawer của `content-workbench.js`. Sau khi Lead duyệt (`lead_approved`+need_media) PIC thấy **form inline "Gửi sang Media"** (prefill từ handoff draft, fields MEDIA_REQ_FIELDS + request_type/deliverable/deadline/priority) → `createMediaRequestInline` tạo order nội bộ (`requester_role='content'`, `MEDIA-…-CT`, loop-strip), task→`media_order_created`, notify admin/account/lead_media (`order_new`) + Lead. **Block "Media Order — theo dõi sản xuất"** (`fillMediaTrack` port, read-only) + **"Checklist hoàn tất task"** (auto-derive: Lead duyệt ✓ · đã gửi Media Request · Media bàn giao Final; progress X/N) trong cùng drawer. Khi order có Final (`final_delivery_link`/`production_status=delivered`) → nút **"Hoàn tất task"** bật → `completeTask` set `status='completed'` + notify Lead. **Lead drawer** (`content-team.js`): **BỎ** nút "Tạo Internal Media Request" (PIC lo), thêm **Checklist hoàn tất read-only** + giữ tracking. ⚠ RLS mới: `orders content insert internal` (`add-content-to-media-order.sql` — **chạy lại**); SELECT order đã có sẵn qua `orders content read` (không scope). 1 media order / content task. · **🏷️ Badge NEW cho order NỘI BỘ (2026-06-19)**: order nội bộ tạo với `account_status='confirmed'` (Option A) nên trước đó KHÔNG có badge "NEW" (logic cũ chỉ `account_status==='pending'`). Thêm `isNewOrder(o)` ở `database-orders.js`: client = pending; **nội bộ = chưa push Production** (`production_status` unassigned/null & ≠rejected). Áp cho badge dòng (line 468) + filter view `pending` + `count-pending` + `nav-pending`. `dashboard.html` card "New Orders" (`renderModule5Dashboard`) thêm `_isNewO` inline cùng logic để KHỚP drilldown. · **🔁 KHÉP KÍN loop noti Content↔Production cho order NỘI BỘ (2026-06-19)**: trước đó order nội bộ từ Content (`order_kind=internal_media_request`/`origin=content_team`/`source_content_task_id`) chỉ thông chiều Content→Media (notify admin/account/lead_media lúc tạo); chiều Media→Content HỞ (status SX không báo; bàn giao "ăn may" qua `notifyClient` vì requester_email=email Lead → message văn phong client + link Drive thô). Fix: **(A)** `production-board.js` thêm `notifyContentOnProdStatus(task,newStatus)` gọi trong `updateStatus` — khi order nội bộ đổi `inprogress`/`ready` → insert noti cho **Lead Content (role)** (chỉ Lead — sở hữu handoff Media + có block tracking; PIC content bị guard đá khỏi content-team), deep-link `content-team.html?task=<ctId>`. **(B)** `database-orders.js`: helper `isInternalOrder(o)` + `notifyClient` **early-return nếu internal** (order nội bộ KHÔNG có client thật → hết bắn noti văn phong client); `sendDelivery` gọi `notifyContentRequester(order,isFinal,link)` báo Lead Content khi Preview/Final (type `delivery_preview`/`delivery_final`, deep-link content task), toast đổi "Đã báo Lead Content". Content thấy link Preview/Final sống qua block tracking `fillMediaTrack` (đọc order). **CHỈ insert notifications** (quyền staff) — KHÔNG ghi `content_tasks` (RLS chặn Media/Account update). `related_entity_type='orders'` (CHECK constraint chỉ tasks|orders|deliveries|comments|NULL). Không cần migration mới. · **🎬 Content Team Deep Workflow HOÀN TẤT 6/6 PHASE — Phase 5 Content→Media + Phase 6 Dashboard/Calendar DONE (2026-06-18)**: **P5 Internal Media Request** — `content-team.js buildTaskActions` thêm CTA **Tạo Internal Media Request** khi task `status=lead_approved`+`need_media_production`+`!media_request_created`; `openMediaRequestModal`/`createMediaRequest` (MEDIA_REQ_FIELDS prefill từ handoff/draft, validate min: final_body_or_script·visual_direction·format_size·channel·production_deadline·request_type·deliverable_type) → `MH.store.orders.create` order nội bộ `origin=content_team`/`order_kind=internal_media_request`/`client_visible=false`/`source_content_task_id`/`source_content_plan_id`/`account_status=confirmed`/`brief_wording_status=completed`, map handoff→order (final_headline→project_name·body+cta+mandatory→content_brief·visual→creative_direction·format→size_ratio·asset→source_link·media_note→internal_note·deadline→requested_deadline), gen `MEDIA-YYYYMMDD-CTxxx`; task→`media_order_created`+flags; notify admin/account/lead_media (type `order_new`); Option A (Lead Media/Account push Production sau). `data-store.js orders.create` nâng **loop-strip 12 cột thiếu** (như update) + allowlist cột internal → migration chưa chạy vẫn tạo order base. `database-orders.js` badge `#d-internal-badge` "Internal · From Content". `client-dashboard.js loadClientOrdersFromStore` lọc bỏ `client_visible===false`/`order_kind==='internal_media_request'`/`origin==='content_team'`. ⚠ `supabase/add-content-to-media-order.sql` (cột orders internal + RLS lead_content INSERT/UPDATE order nội bộ). **P6** — content-team Dashboard view (`renderContentDashboard`): KPI Content Tasks/Plans + breakdown Status/Source/Output (`barRows`) + Plans progress + High-revision; `calendar.js` event `ct-deadline`/`cplan-deadline` role-filtered (load `contentTasks`/`contentPlans`, navTarget `content-workbench.html?task=`/`content-team.html?plan=`, dotClass→wording/order). CSS `.ctm-internal-badge`. · **🛂 Content Team Deep Workflow — Phase 4 Lead Review & Internal Revision DONE (2026-06-18)**: `content-team.js` nâng **Content Task Drawer** (buildTaskBody/buildTaskActions) thành cổng duyệt Lead↔PIC. Block read-only mới: **Bản thảo Content** (`WS_ORDER`/`WS_LABEL` lọc cột non-empty) · **Quality Checklist n/9** (`CTCHK`, đọc `quality_checklist` object-or-string-safe qua `parseChecklist`) · **Production Handoff Draft** (`HANDOFF_LABEL`, chỉ need_media) · **Revision History** (`revision_history` + internal_revision_count). **Lead Review Panel** chỉ khi `status==='submitted_to_lead'`: select `REVISION_REASONS` (14) + textarea note. Actions: `leadReturnTask` (→`lead_revision`, `internal_revision_count++` ĐẾM-không-chặn, append `revision_history` {round,reason,note,by,at}, lưu `last_revision_reason`/`lead_review_note`, notify PIC link `content-workbench.html?task=`) · `leadApproveTask` (→`lead_approved`+`lead_approved_at/by`; route: need_media→`lead_approved`(chờ P5) · `client_order`→`submitted_to_account`+notify Account/Admin · else→`completed`). Mỗi action gọi `syncPlanStatus` (rollup plan cha, `CT_DONE` gồm lead_approved). PIC side (content-workbench Phase 3) đã hiển thị callout lead_revision + revision_history + sửa/gửi lại không giới hạn. CSS mới `.ctm-draft-field`/`.ctm-chk-readonly`/`.ctm-review`. · **✍️ Content Team Deep Workflow — Phase 3 PIC Workbench DONE (2026-06-18)**: `content-workbench.html`+`content-workbench.js` thêm tab **"Content Tasks của tôi"** (model `content_tasks`), TÁCH BIỆT bảng order-wording cũ (giữ nguyên). `cwbView` toggle 2 section `cwb-view-orders`/`cwb-view-tasks` (default `tasks` cho content, `orders` cho admin/account). PIC thấy task `isMine(assigned_pic)`; admin/lead/account/supervisor xem tất cả (read-only). Sub-tabs Tất cả/Đang viết/Cần chỉnh/Đã gửi Lead/Đã duyệt + stats + filter output + search. **Content Task Drawer** (`ctask-drawer`): Summary+Brief(+key msg/đối tượng từ Plan) · Missing Info/Assumptions/Questions/Risk(không block) · **Writing Workspace multi-output** (`workspaceFields()` union field theo `output_types` qua `OUTPUT_FIELDS`→`FIELD_META`, map cột discrete content_tasks) · **Production Handoff Draft** (chỉ need_media) · **Quality Checklist 9 mục** (`CT_CHECKLIST`→`quality_checklist` jsonb) · Files/Links(asset_links) · Activity/Revision(`revision_history`+cache `task:<id>`). Handlers: `startTask`(→in_progress)/`saveTaskDraft`/`submitTaskToLead`(→submitted_to_lead, chỉ pic_assigned/in_progress/lead_revision, validate checklist+≥1 ô; `notifyLeadTask`→lead_content link `content-team.html?task=`). `canEditTask` = (content&isMine)||admin && status∈editable. **KHÔNG bypass Lead** (no gửi Account/Client/Media). Persist `MH.store.contentTasks.update` (content RLS update own assigned). Deep-link `?task=`. CSS tái dụng (`.ctm-drawer`/`.cwb-stat`/`.saved-views`...) — KHÔNG thêm class mới. · **🧭 Content Team Deep Workflow — Phase 2 Lead Workspace DONE (2026-06-18)**: `content-team.html`+`content-team.js`+`styles.css` thêm 2 tab **Content Plans** + **Content Initiatives** (role-gated Lead/Admin tạo; Account/Supervisor xem read-only), TÁCH BIỆT hoàn toàn flow Inbox/orders cũ. **Content Plans**: bảng plan cha + roll-up progress/status từ task con (`rollup()` spec §6) + **Plan Drawer** (info/file/objective/channels/key-msg/CTA/deadline + Progress summary 6 ô + bảng task con + "Thêm task con" modal + activity). **Content Initiatives**: bảng `content_tasks` `source=content_initiated` + form tạo (output checkbox-chips, PIC datalist, hạn, ưu tiên, cần-Media→media type/deadline/note). **Content Task Drawer** (Lead reassign PIC+hạn+ưu tiên+cần-Media · Missing Info/Assumptions/Questions/Risk — **Lead KHÔNG trả brief về Account**, chỉ ghi chú · activity). Modal generic `openModal()`+`modalSaveHandler`. Cảnh báo hạn task con vượt deadline plan (non-block). Deep-link `?plan=`/`?task=`. Notify PIC khi gán (type `task_assigned` base-safe). Dùng `MH.store.contentPlans`/`contentTasks` (Phase 1); seed demo có sẵn. CSS mới `.ctm-section-head`/`.ctm-progress`/`.ctm-plan-badge.ps--*`/`.ctm-sum-grid`/`.ctm-chk-chip`(`:has`)/`.task-modal`(định nghĩa global, trước chỉ inline ở production-board) — z-index drawers 120/modal 160 dưới toast 1000. ⚠ vẫn cần `add-content-initiatives.sql` để persist Supabase (chưa chạy → list trả `[]`, create/update throw). · **🧱 Content Team Deep Workflow — Phase 1 Foundation DONE (2026-06-18)**: nền data model cho nâng cấp sâu Content Team (xử lý task Client Order + Content Initiative chủ động + Content Plan cha nhiều task con + revision no-limit + Production Handoff). Thêm 3 namespace data-store `contentPlans`/`contentTasks`/`contentTaskComments` + `contentEnums` (xem mục `data-store.js` bên dưới), fallback localStorage `mh-content-*` cross-tab + seed demo 1-lần (`mh-content-seed-v1`), migration `supabase/add-content-initiatives.sql` (3 bảng `content_plans`/`content_tasks`/`content_task_comments` + RLS Content-Team-only KHÔNG đụng `is_staff()` → content/lead_content vẫn KHÔNG thấy Production · client KHÔNG thấy + index + trigger + Realtime). **CHỈ nền data + compat — UI CHƯA đụng**, content-team/content-workbench mở không crash. Phase 2–6 (Lead Workspace · PIC Workbench multi-output · Lead Review no-limit · Content→Media Internal Order · Dashboard/Reports/Calendar/Notif) chưa làm. · **🗂️ Supervisor Planning DONE (2026-06-17)** — module mới `supervisor-planning.html`+`supervisor-planning.js`: System Supervisor/Admin lập + giao **kế hoạch nội bộ** cho Lead Media / Lead Content / **cả 2 (`assigned_lead='both'`)**, TÁCH BIỆT `tasks` production + wording. Bảng mới `lead_tasks` (data-store namespace `leadTasks`, fallback `MH_MOCK_LEAD_TASKS`/`mh-lead-tasks`) + **đính kèm file PDF (Storage bucket `plan-files`, cột attachment_path/name) hoặc link (attachment_url)**. **Role mới `lead_media`** = Trưởng nhóm Production (quyền NGANG Account: Client Orders/Task Tracker/Dashboards/Reports/Order Form qua alias→account; KHÔNG Content Team/Settings; login→Master Dashboard; ⚠ `add-media-lead-production.sql`). UI: stats+form tạo+filters+**List/Board kanban**+bảng (Media teal/Content purple/Cả 2 gradient + per-lead chips + mini-progress). **Tiến độ RIÊNG từng Lead** (`lead_status` jsonb; `status`=derived). **Drawer workspace cộng tác**: lane card từng Lead + comment 2 chiều + activity timeline (bảng `lead_task_comments`) + checklist/progress. **Cổng duyệt per-lane**: Lead "Nộp duyệt" (Kế hoạch/Sản phẩm + link/file + note → lane `submitted`, lưu `lead_submissions` jsonb) → Supervisor Duyệt (`completed`)/Trả chỉnh (`revision`+lý do→nộp lại); Lead KHÔNG tự `completed`. `displayOverall` badge Chờ duyệt/Cần chỉnh; Board cột "Chờ duyệt". **Lead chủ động đề xuất** (`origin=lead`/`approval=proposed`): Lead "Đề xuất kế hoạch" bucket mình + mention Lead kia (`informed_leads`, read-only) → Supervisor Duyệt(`approved`)/Từ chối(`declined`); RLS lead INSERT + informed SELECT; `canParticipate` gate ẩn comment/control cho informed Lead. **Notify 2 chiều** (giao→Lead, đổi status/nộp→Supervisor, duyệt/trả chỉnh→Lead, comment→chiều kia) + deep-link `?id=` + realtime. Sidebar **"Strategy Board"** inject `app.js injectPlanningNav()` (reveal lead_media + override ẩn content do "lead_content"⊃"content"). ⚠ chạy `supabase/add-supervisor-planning.sql` (users_role_check += lead_media + bảng + bucket `plan-files` + RLS supervisor/admin ALL · lead SELECT/UPDATE/DELETE bucket mình incl `both` KHÔNG INSERT). **⚠ supervisor vốn read-only — module này CỐ Ý cho supervisor GHI trên lead_tasks cô lập.** **19 HTML · 16 JS** · **🛡️ Role `system_supervisor` (Giám sát hệ thống) — monitor READ-ONLY toàn hệ thống DONE** (redirect dashboard; thấy mọi màn vận hành + Reports/export; mọi mutation khóa; ẩn Order Form/User Mgmt/Settings; ⚠ chạy `add-system-supervisor.sql`) · **🧑‍🤝‍🧑 Content Team Workspace DONE** (`content-team.html`+`content-team.js`: team Content TÁCH BIỆT Production — role mới **`lead_content`**; Dashboard/Inbox/Board 7 cột/Danh sách/My Tasks + drawer (Lifecycle 7 bước · Assign panel · Workspace · Quality Checklist · Lead Review Panel · Account&Client status · Files/Links · Activity); flow `assigned`→`pic_assigned`→`in_progress`→`submitted_to_lead`→(`lead_revision`↺)→`submitted_to_account`→flow Account/Client cũ; Lead gán PIC + hạn wording + duyệt/trả chỉnh; notify: transfer→lead_content (fallback content), gán PIC→PIC, submit→lead, duyệt→admin+account; cache CHUNG `mh-wording-drafts`; **sidebar nhóm "Content Team" 2 sub**: Content Workspace (lead) + Content Wording (content) — content làm & "Gửi Lead Content duyệt" ngay tại content-workbench; guard chặn production-board/database-orders/task-dashboard redirect theo role; ⚠ chạy `supabase/add-content-team.sql`) · **📅 Calendar / Lịch DONE** (`calendar.html`+`calendar.js`: **Month/Week/Day**, role-filtered — admin/account full, design/editor chỉ task PIC, content chỉ order wording; 4 event: deadline task/order + lịch quay/chụp + bàn giao; **Day view ref-style** mini-calendar + danh sách task của ngày — "+N khác"/click số ngày để mở; click→popover→drawer; nav inject qua `app.js injectCalendarNav()`; ⚠ chạy `add-shoot-date.sql` cho cột shoot_date/time) · **18 HTML · 15 JS** · **Brief Wording flow Phase 1→4 DONE** (role content → mandatory gate → Content Workbench → Client confirm trong Portal; wording rounds tách khỏi preview/final; ⚠ chạy `add-brief-wording-fields`+`-workspace-fields`+`-confirmation`.sql) · **Workflow notifications khép kín** (order→PIC→duyệt→bàn giao→rating, fuzzy PIC name match) · **Bàn giao Preview/Final trong Order drawer** (Delivery Log ĐÃ GỠ — đọc `orders.preview_link`/`final_delivery_link`) · **Order `media` "Quay/Chụp" gộp → Push tách 2 task** (production_pic_video/photo; `add-media-pics.sql` đã chạy production 2026-06-03) · Order Form theo type (media skip mục 4,5 + renumber 1-5; video gộp 1 ô; slide thả link; **bỏ wording**) · Master "New Orders" card + NEW badge · `notif-icons.js` shared module · fmtDateTime/toLocalInput fix timezone · **Order drawer Điều phối**: Push → Production giữ SÁNG sau confirm brief (chỉ mờ khi đã push) + nút "Xác nhận & Chuyển Production" (lưu+push 1 chạm) + khóa PIC select sau push (`isOrderPushed()`, đổi PIC ở Task Tracker) · **⚡ Auto-sync Production Status task→order** (ô order read-only; bottleneck rule; ⚠ cần chạy `supabase/sync-order-status-from-tasks.sql` — CHƯA chạy production) · **⚡ Preview → 3 vòng Feedback → Final** (bàn giao trong Order drawer; design tối đa 3 vòng, vòng 4 = task/order mới; ⚠ chạy `supabase/add-revision-rounds.sql`) · **⚡ Approve Preview + Link sync Task↔Order** (client "Duyệt Preview"; Order drawer "Gửi feedback cho PIC" + "Links from Task Tracker" Dùng làm Preview/Final; ⚠ chạy `supabase/add-preview-approval.sql`) · **📊 Reports wired LIVE (2026-06-12)** (`reports.js`: load orders+tasks+users qua `MH.store`, aggregate client-side 12 KPI/6 charts/funnel/rating/3 bảng; filters period/role/PIC/branch/type recompute từ RAW snapshot không refetch; empty-state mọi chart/table khi 0 data — fix crash `FUNNEL[0]` cũ; poll 60s + reload visibilitychange) · Railway LIVE ✓*

---

## 1. TL;DR

**CB Media Hub / CB Creative Flow** là Creative Service Portal cho **CB Centres**.

- Static multi-page site: 21 HTML pages (+5 trang demo/preview `_*.html` không tính), 21 JS files, 1 shared CSS, zero build. (Delivery Log page gỡ 2026-06-03 — bàn giao trong Order drawer.)
- **2 khu vực riêng biệt**: Internal Dashboard (admin/account/design/editor) và Client Portal (client).
- Workflow chính: Order Form → Client Orders (database-orders) → Internal Task Tracker → **bàn giao trong Order drawer** → Reports.
- Brand: navy `#191970` + red `#BA110F`, Inter / Plus Jakarta Sans + Playfair italic accent.
- Done: 5 public pages + 1 client portal + 15 internal modules (xem Quick View trong STATUS.md), gồm AI Tools MVP và Chatbot MVP.
- Data/auth/upload/API đều là demo/mock, chưa production-ready.

---

## 2. Brand Tokens

```css
--brand-600: #191970;
--red-600: #BA110F;

--grad-navy:  linear-gradient(135deg, #191970 0%, #3849b3 100%);
--grad-red:   linear-gradient(135deg, #d62a28 0%, #BA110F 100%);
--grad-brand: linear-gradient(135deg, #191970 0%, #4338ca 45%, #BA110F 100%);

--success: #16a34a;
--warning: #f59e0b;
--danger:  #dc2626;
--info:    #0ea5e9;
```

Typography:

- Sans/display: Inter + Plus Jakarta Sans.
- Accent: Playfair Display italic via `.serif-italic` và `.serif-italic--red`.
- Mono: JetBrains Mono.

Interaction/style invariants:

- Buttons dùng pill radius (`--radius-pill`).
- Touch target tối thiểu 44×44px.
- Giữ focus visible, reduced motion, dark mode fallback.
- Không dùng emoji làm structural icon trong UI; dùng inline SVG Lucide-style.
- Content Vietnamese-first.
- Avatar luôn hình tròn: `.avatar { border-radius: 9999px !important; overflow: hidden; }` — global trong `styles.css`.
- Theme toggle là pill switch (`[data-theme-toggle].theme-toggle-switch`), CSS-driven qua `[data-theme="dark"]` selector. Không dùng inline style cho icons.

---

## 3. File Map

### Public Pages

| File | Purpose | JS |
|---|---|---|
| `index.html` | Homepage + hero + quick actions | inline |
| `login.html` | Login (email + password, Supabase auth; tiles demo/password cứng đã gỡ) | inline |
| `request.html` | Order Form Media 7 sections + **Ads 6 sections** (`?type=ads`, auth-gated) | `order-form.js` + `ads-order-form.js` |
| `tracking.html` | Client tracking by `MEDIA-*` code | inline |
| `help.html` | FAQ + search + accordion | inline |

> `deliveries.html` không tồn tại trong codebase — đã remove khỏi docs.

### Client Portal

| File | Roles | JS |
|---|---|---|
| `client-dashboard.html` | client only | `client-dashboard.js` |

Client Portal gồm: xem orders của mình, order status tracking, tạo yêu cầu mới (link sang `request.html`), profile. Client bị redirect về đây sau login và bị block khỏi Internal Dashboard.

### Internal Pages

| File | Roles | JS |
|---|---|---|
| `dashboard.html` | admin, account, design, editor | inline (Master Dashboard) |
| `order-dashboard.html` | admin, account | inline (Order-level KPI) |
| `task-dashboard.html` | admin, account, design, editor | inline (Task-level / Production KPI) |
| `database-orders.html` | admin, account | `database-orders.js` |
| `production-board.html` | admin, account, design, editor | `production-board.js` — Task Tracker / Production Board |
| `calendar.html` | admin, account, design, editor, content, lead_content | `calendar.js` — Lịch / Calendar (Month/Week/Day, role-filtered) |
| `content-workbench.html` | admin, account, content, lead_content | `content-workbench.js` — Content Wording (legacy solo flow; status Content Team = read-only) |
| `content-team.html` | admin, account, lead_content, content | `content-team.js` — **Content Team Workspace** (Dashboard/Inbox/Board/List/My Tasks + drawer + Lead Review) |
| `supervisor-planning.html` | admin, system_supervisor, lead_media, lead_content | `supervisor-planning.js` — **Supervisor Planning** (sidebar "Strategy Board"; bảng `lead_tasks`; giao Media/Content/**cả 2**; đính kèm PDF Storage `plan-files`/link; supervisor/admin tạo+sửa+xóa, Lead xem bucket mình + tải file + đổi status) |
| `reports.html` | admin, account | `reports.js` |
| `brand-check.html` | **mọi role đăng nhập** (client/giáo viên tự kiểm — sidebar tối giản về Client Portal; media = admin/account/lead_media hậu kiểm + dashboard; system_supervisor read-only) | `brand-check.js` — **AI Brand Safety Checker** (kiểm duyệt hình ảnh AI theo brand CB; Edge Function `brand-check-analyze`; ⚠ `add-brand-check.sql`) |
| `ai-tools.html` | admin, account, design, editor | `ai-tools.js` |
| `chatbot.html` | admin, account, design, editor | `chatbot.js` |
| `user-management.html` | admin | `user-management.js` |
| `settings.html` | admin | `settings.js` |

### Shared Assets

- `assets/styles.css` — Design tokens, components, page styles. Gồm `.header-profile-chip`, `.theme-toggle-switch`, `.sidebar-version-block`, `.btn-login-pill`, `.auth-gate-bar`.
- `assets/notif-icons.js` — **Shared notification icon module** (2026-06-02). Expose `window.MH.notifIcons` (`PATHS` + `get(type)→{svg,cls}` + `stripEmoji(s)`). Single source of truth cho icon thông báo, dùng bởi `app.js` (bell) + `client-dashboard.js` (panel). Load TRƯỚC `app.js` trên 12 page có chuông/panel.
- `assets/config.js` — **Runtime config**, load TRƯỚC `app.js` trên 17/17 page. Expose `window.MH_CONFIG` với `SENTRY_DSN`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SENTRY_ENV` (auto-detect localhost vs production), `SENTRY_RELEASE`, `APP_VERSION`, `FEATURES` flags (gồm `SUPABASE_DB`). Để trống các key = disable feature tương ứng. File commit kèm repo, fill tay khi sẵn sàng bật.
- `assets/supabase-client.js` — **Supabase SDK loader (Phase 1)**, dynamic import @supabase/supabase-js@2.45.4 từ `esm.sh` CDN (giữ zero-build). Expose `window.MH.supabase`, `window.MH.supabaseReady` (Promise), `window.MH.supabaseEnabled`. Auth state change handler mirror Supabase session sang `localStorage['mh-user']` cho compat code hiện tại. Khi config trống → stub null, không network call.
- `assets/data-store.js` — **Data abstraction (Phase 1+2)**, expose `window.MH.store` với các namespaces: `users`, `orders`, `tasks`, `taskComments`, `deliveries`, `aiUsage`, `chatbot`, `files`, **`notifications`**, `auth`, `activity`, `leadTasks`, `leadTaskComments`, **`contentPlans`/`contentTasks`/`contentTaskComments`** (Content Team Deep Workflow Phase 1, 2026-06-18) + `contentEnums` + **`brandChecks`** (AI Brand Safety Checker 2026-07-03: list/get/create/update/delete + criteriaByCheck/addCriteria; Supabase `brand_checks`/`brand_check_criteria` qua `add-brand-check.sql`, fallback localStorage `mh-brand-checks` — criteria inline field `criteria` khi offline). **Content Team namespaces** (pattern y hệt `leadTasks`: localStorage `mh-content-plans`/`mh-content-tasks`/`mh-content-task-comments` cross-tab, Supabase → bảng `content_plans`/`content_tasks`/`content_task_comments` qua `add-content-initiatives.sql`): `contentPlans` list/get/create/update/delete · `contentTasks` list/get/byPlan/byOrder/byAssignedPic/create/update/delete · `contentTaskComments` byTask/add. `source` của task: `client_order`/`content_initiated`(supported) + `strategy_board`/`campaign_package`(chuẩn bị). **Seed demo 1-lần** `seedContentDemo()` (flag `mh-content-seed-v1` → user xóa KHÔNG seed lại; chỉ ghi localStorage nên Supabase-on bỏ qua): 1 Plan "Khai trương Q3" + 5 task con + 1 Initiative độc lập + 1 task từ Client Order. ⚠ chưa chạy migration thì list() bảng chưa tồn tại trả `[]` (KHÔNG crash). Mọi method trả Promise — nếu Supabase enabled → query qua SDK, else fallback `window.MH_MOCK_*` arrays + localStorage keys cũ. Consumer code dùng `await MH.store.tasks.get(id)` thay vì `TASKS.find(...)` — không branch theo backend. **Always-swap pattern (5/2026)**: bỏ điều kiện `remote.length > 0`, khi Supabase enabled luôn replace local array (kể cả empty → UI hiển thị empty state thay vì fallback mock). ⚠ **`orders`/`tasks` có cả `update(id, patch)` (UPDATE .eq) lẫn `upsert(fullRow)`: PATCH → `update`, TẠO MỚI → `upsert` full row. KHÔNG upsert partial vào bảng có cột NOT NULL** (project_name/task_type) — vi phạm NOT NULL trước ON CONFLICT, fail âm thầm (xem fix 2026-06-03).

**Module migration progress (Phase 1):**
- ✅ `login.html` — Supabase Auth-first via `MH.store.auth.signIn()`, fallback `loginAsDemo()`.
- ✅ `database-orders.js` — `MH_MOCK_ORDERS`, async `loadOrdersFromStore`, mutations via `persistOrder` (status, push-to-prod, save-internal).
- ✅ `production-board.js` — `MH_MOCK_TASKS`, `loadTasksFromStore`, `persistTask` (UPDATE patch) + `persistNewTask` (upsert tạo mới) + `persistTaskComment` cho status transitions, save links/meta, comment add, Create/Edit task modal.
- ~~`delivery-log.js`~~ — **ĐÃ GỠ 2026-06-03** (bàn giao Preview/Final chuyển vào Order drawer `database-orders.js` section "Bàn giao cho client" → set `orders.preview_link`/`final_delivery_link`). `deliveries` namespace trong data-store giữ lại nhưng không còn UI.
- ✅ `user-management.js` — `MH_MOCK_USERS`, `loadUsersFromStore` (adapter Supabase row → mock shape), `persistUser` cho status toggle + edit user (chỉ field nằm trong public.users schema).
- ✅ `ai-tools.js` — `addUsage()` + `saveOutput()` write-through via `MH.store.aiUsage.log()` + `saveOutput()`.
- ✅ `chatbot.js` — `pushMessage()` + `saveFeedback()` write-through via `MH.store.chatbot.append()` + `feedback()`. Cần thêm namespace `chatbot` vào data-store.js.
- ✅ `reports.js` — **wired LIVE 2026-06-12** (trước đó là module DUY NHẤT chưa nối Supabase — chỉ vỏ UI + mảng rỗng). `loadReportsFromStore()` load `orders.list()`+`tasks.list()`+`users.list()` song song → snapshot `RAW` → `applyFilters()` (period/role/PIC/branch/type, recompute client-side KHÔNG refetch) → `computeReportData()` aggregate 12 KPI + TREND 14 ngày + donut task_type + role bars (`ROLE_OF_TASK_TYPE`: design/digital/ads/slide→design, video/motion→editor, shoot/photo→shoot) + PIC stacked + heatmap thứ-trong-tuần + Delivery Funnel 7 bước (Tiếp nhận→Đánh giá) + rating dist + 3 bảng (PIC KPI composite 60% on-time + 25% rating + 15% ít revision · Overdue ≤2 ngày/trễ · Feedback top 6). Read-only consumer — không producer/notify. Empty-state mọi chart/table khi 0 data (fix crash sẵn có `FUNNEL[0].count` trên mảng rỗng làm chết init từ renderFunnel trở xuống). Poll 60s + visibilitychange. `#filter-pic` options rebuild từ data thật. `revision_round`/`production_pic_video/photo` đọc an toàn khi migration chưa chạy (undefined→0/skip). Match tên PIC↔users word-boundary (**"Du" KHÔNG khớp "Duy"**).
- ✅ Enable RLS policies — `supabase/rls.sql` idempotent (DROP + CREATE), helper functions `current_user_role()` / `is_staff()` / `is_admin_or_account()` / `is_admin()`, policies cho 11 bảng. Run trong Supabase SQL Editor sau khi verify migration.

**Phase 2 — File Storage:**
- ✅ `supabase/storage.sql` — 3 buckets (`avatars` public, `brief-files` + `deliverables` private) với mime/size limits + storage.objects policies dùng helper functions từ rls.sql.
- ✅ `assets/data-store.js` thêm namespace `files`: `upload(bucket, path, file, opts)`, `getPublicUrl()`, `signedUrl(bucket, path, expiresIn)`, `list()`, `remove()`. Fallback avatar → data URL inline; bucket private throw nếu chưa cấu hình.
- ✅ `assets/app.js` Profile modal save: nếu Supabase Storage enabled VÀ pendingAvatar là data URL → fetch → blob → upload `avatars/{user.id}/avatar-{ts}.{ext}` → publicUrl thay thế data URL. Persist profile metadata (name/initials/title/phone/department/bio/avatar_url/role) sang `public.users` qua raw client.
- ✅ `assets/order-form.js` doSubmit (async): trước khi tạo order, upload mỗi file trong `files` Map lên `brief-files/{order_id}/brief-{ts}-{filename}` với contentType. Lưu array `briefFiles` vào payload, INSERT row vào `public.orders` qua `MH.store.orders.create()`. localStorage fallback giữ nguyên.
- ⬜ Delivery preview/final file upload — chưa migrate (current dùng Drive URL inputs, không có upload native).

**Phase 1.5 — Realtime Notifications (5/2026):**
- ✅ `supabase/add-notifications.sql` — bảng `notifications` (user_id, type, title, message, link, related_entity_*, is_read) + 3 index + trigger `touch_notif_read_at`.
- ✅ `supabase/enable-realtime.sql` — `ALTER PUBLICATION supabase_realtime ADD TABLE notifications` để WebSocket push từ Postgres.
- ✅ `data-store.js notifications` namespace — listUnread/listAll/create/markRead/markAllRead/findUserIdByName.
- ✅ `app.js initNotificationBell` IIFE — auto-wire dropdown vào tất cả `button[aria-label="Thông báo"]`, badge unread, click bell mở dropdown 20 mới nhất. **Supabase Realtime subscribe channel `notif-{uid}` filter `user_id=eq.me`** → INSERT event → `showNotifPopup()` toast 8s + click→markRead+navigate. Poll 60s backup, cleanup channel on `beforeunload`.
- **Producers** đã ship: `order-form.js` (notify admin+account khi client submit, type=`order_new`), `database-orders.js pushToProduction` (notify PIC khi push to prod, type=`task_assigned`).
- **Bell icon = minimal line SVG theo `notification.type`** (2026-06-01, refactor 06-02): icon lấy từ shared module `window.MH.notifIcons` (xem mục dưới), `app.js` chỉ giữ alias `notifIcon=(t)=>MH.notifIcons.get(t)` + `stripNotifEmoji=(s)=>MH.notifIcons.stripEmoji(s)`. Dropdown item layout `[span.mh-notif-item-ico][div.mh-notif-item-body]`. Màu mặc định `var(--text-muted)`, `is-accent`=navy, `is-danger`=red. **KHÔNG thêm emoji mới vào title producer** — icon do consumer render theo type; cần icon mới chỉ thêm key vào `assets/notif-icons.js` PATHS.
- **Notification click → order drawer cho client (2026-06-02)**: `app.js resolveNotifLink` role-aware — `mh-user.role==='client'` → `client-dashboard.html?order=<id>` (client không vào được database-orders/production-board/delivery-log). `client-dashboard.js openOrderFromQuery()` đọc `?order=` sau khi orders load → `switchTab('orders')` + `openOrderDrawer(id)`. Tab panel `.notif-item` cũng clickable toàn bộ (`data-action="open-order"`). **notifyClient producer vẫn để link=`tracking.html?code=…`** (cho public tracking page) — client override sang in-portal drawer ở consumer, không cần đổi producer.
- **Notification flow end-to-end (hoàn thiện 2026-06-02 B+C+D)** — ma trận producer→consumer:
  - Client submit order → `order_new` → Admin+Account.
  - Account check/needinfo/confirm → `order_status_changed`/`order_needinfo`/`order_confirmed` → Client.
  - Account **Push → Production** → tạo task + `task_assigned` → **PIC** (lookup qua `findUserIdByName` fuzzy: exact→prefix→suffix→contains, tên ngắn khớp tên đầy đủ) + `order_status_changed` → Client.
  - PIC gửi duyệt (status→`review`) → `task_status_changed` "Task chờ duyệt nội bộ" → **Admin+Account** (`production-board.js notifyTaskStatusChange`).
  - Account trả sửa (status→`revision`/`feedback_fix` ở Task Tracker, HOẶC `request_rev` ở Delivery Log) → `task_status_changed` "Task cần chỉnh sửa" → **PIC**.
  - **Account bàn giao trong Order drawer** (`database-orders.js` section "Bàn giao cho client", 2026-06-03): nhập Preview Link → "Gửi Preview" (set `order.preview_link`+`delivery_status=client_wait`+`production_status=feedback_wait`); Final Link → "Gửi Final" (set `final_delivery_link`+`delivery_status=final`+`production_status=delivered`) → notify `delivery_preview`/`delivery_final` → Client. **Client Portal đọc `orders.preview_link`/`orders.final_delivery_link`** (KHÔNG đọc bảng `deliveries`). ⚠️ **Delivery Log (`delivery-log.js`) thao tác trên bảng `deliveries` riêng, KHÔNG sync sang order → vestigial cho flow thật**; đừng dùng Delivery Log để bàn giao cho client.
  - Client rating → `orders.update(satisfaction_score, client_feedback)` + `rating_received` → **Admin+Account** (`client-dashboard.js`, KHÔNG còn local-only).
  - LƯU Ý: gán PIC trên order ≠ tạo task; phải Push. Mọi cross-user INSERT notification được RLS cho phép (producer chạy dưới user bất kỳ).
- **Icon thông báo = SHARED MODULE `assets/notif-icons.js` (2026-06-02 refactor)**: SINGLE SOURCE OF TRUTH. Expose `window.MH.notifIcons` = `{ PATHS, get(type)→{svg,cls}, stripEmoji(s) }`. Cả `app.js` (bell dropdown, internal+client) lẫn `client-dashboard.js` (panel "Thông báo") đều consume `window.MH.notifIcons` — KHÔNG còn map riêng. **Sửa icon/màu chỉ ở `notif-icons.js` là mọi surface (bell, panel, toast) tự đồng bộ.** Load `<script src="assets/notif-icons.js">` TRƯỚC `app.js` trên 12 page có chuông/panel (11 internal + client-dashboard; public pages không có chuông nên `initNotificationBell` early-return, không cần). `cls` (is-accent/is-danger) do từng surface tự style màu. Client panel item dùng `.notif-ico` (CSS inline `client-dashboard.html`).
- **Bell attention state (2026-06-01)**: `refreshBadge()` toggle 1 class `.has-unread` trên `.mh-notif-wrap` theo `notifications.listUnread()` count > 0 — điều khiển ĐỒNG THỜI: badge đỏ (dot), **chuông gradient navy→red** (`stroke: url(#mh-bell-grad)`, def SVG inject vào body) + **animation rung `mhBellRing`**, và **dot pulse `mhBadgePulse`**. Hết chưa đọc (mark-all / đọc hết) → bỏ `has-unread` → chuông về `currentColor`, dừng animation, ẩn dot. Animation tắt khi `prefers-reduced-motion` (gradient + dot vẫn giữ). Mọi path đều gọi `refreshBadge`: start, poll 60s, realtime INSERT, mark-all, click item.
- **🐛 FIX dot kẹt đỏ khi đã đọc hết — mọi role (2026-06-12)**: Client Portal có 2 indicator chồng trên cùng chuông `#notif-btn`: `.mh-notif-badge` (app.js, đọc Supabase `listUnread`) + `#notif-dot` (span tĩnh `client-dashboard.html`, do `client-dashboard.js renderNotifications` điều khiển theo `state.notifRead`). Hệ quả: bấm "Đánh dấu đã đọc" ở **dropdown app.js** clear badge nhưng `#notif-dot` kẹt đỏ; ngược lại **mark-all ở tab client** (`mark-all-read-btn`) trước đây CHỈ set local `state.notifRead`, KHÔNG persist Supabase → reload dot quay lại + badge app.js không clear. **Fix**: (1) `app.js refreshBadge` giờ đồng bộ luôn `#notif-dot` (`getElementById('notif-dot')`) theo cùng count Supabase → 1 nguồn sự thật, no-op trên trang nội bộ (không có dot tĩnh); (2) `client-dashboard.js mark-all-read-btn` gọi thêm `store.notifications.markAllRead()` (fire-and-forget) → persist is_read. Hội tụ 2 chiều: đọc hết ở bất kỳ surface nào → cả badge lẫn dot tĩnh tắt, và sau reload vẫn tắt. Internal role không có `#notif-dot` nên hành vi cũ giữ nguyên (đã đúng sẵn).
- **🐛 FIX toolbar "Yêu cầu của tôi" desync — Client Portal (2026-06-15)**: list filter/sort lệch với badge trạng thái thật (badge render qua `pubEffStatus` = 14 nhãn granular, nhưng filter cũ chỉ 6 bucket thô + so **raw `o.status`**). Sửa trong `client-dashboard.js getFilteredOrders` + `client-dashboard.html`: (1) **Status filter** đổi sang `STATUS_FILTER_GROUPS` lọc bằng `pubEffStatus(o)` (submitted/needinfo/producing/review/feedback/completed/cancelled) — option mới khớp badge; giữ `active`/`delivered`(=`isFinalDelivered`)/`no_rating` cho **KPI drilldown** đúng count. (2) **KPI click handler** set `state.ordersFilterStatus` trực tiếp (gồm `active`/`no_rating` không có trong dropdown), `sel.value` chỉ phản chiếu khi có option. (3) **Sort "Mới nhất/Cũ nhất"** TRƯỚC sort theo Order ID random (`b.id.localeCompare`) → giờ theo `orderTime()` (cột `created_raw` thêm vào adapter; fallback parse `DD/MM/YYYY`); thêm handler **`deadline_asc`** (trước no-op) qua `orderDeadlineTime()`; sort `status` dùng `STATUS_SORT_PRIO` đầy đủ + `pubEffStatus`. (4) **Type filter** option dùng đúng label localized (`Digital`/`Quay / Chụp ảnh`/`Chụp ảnh`/`Slide`/`Ads`; bỏ `Photo` chết) + **`TYPE_LABEL` thêm key `media`='Quay / Chụp ảnh'** (trước media hiện chữ "media" lowercase). _Quirk:_ filter `delivered` = `isFinalDelivered` (gồm đơn đã rating) để khớp KPI "Đã bàn giao"; đơn rated vừa khớp `delivered` vừa khớp `completed` (overlap có chủ đích, không phải miss). Admin list `database-orders.js` sort generic theo key thật → KHÔNG dính.
- **🔔 Bù noti thiếu — flow Content-only (KHÔNG dùng lead_content) (2026-06-12)**: role `content` login → `content-workbench.html`. Trước đây nút submit là "Gửi Lead Content duyệt" → set `submitted_to_lead` + chỉ notify `lead_content` → **không có lead = kẹt + Account không nhận noti**. Sửa trong `content-workbench.js`: (1) `submitToLead`→**`submitToAccount`**: status thẳng **`submitted_to_account`** (RPC `update_brief_wording` whitelist cho phép — cả v1 lẫn v2), set `wording_submitted_at/by`, **notify `admin`+`account`** (link `database-orders.html?id=`), nút đổi nhãn "Gửi Account duyệt"; (2) `accountSendToClient` → **notify Client** (`notifyClientWb`, requester_id→email); (3) `accountReturnRevision` → **notify PIC Content** (`notifyPicWb`, findUserIdByName). Thêm helper `notifyRolesWb`/`notifyPicWb`/`notifyClientWb`. **Type base-CHECK-safe** (`task_status_changed`/`order_status_changed`) → chạy KHÔNG phụ thuộc migration pending. Cũng đổi `database-orders.js sendWordingToClient` type `wording_sent_to_client`→`order_status_changed` (cùng lý do). `notifyLeads` giữ lại (dead code, forward-compat khi bật lead). **⚠ Nếu muốn dùng type wording_* / client_* chuẩn thì chạy `add-brief-wording-confirmation.sql` (hoặc `add-revision-rounds.sql`) để CHECK constraint không từ chối.**
- **🔔 Bù noti Task Tracker — role-to-role (2026-06-12)**: `production-board.js` thêm 2 helper + 1 nhánh, tất cả type **base-CHECK-safe** (`task_assigned`/`task_comment`/`task_status_changed` — có trong CHECK gốc `add-notifications.sql`, chạy ngay không cần migration): (**#5**) tạo task mới qua modal có gán PIC → `notifyTaskAssign(newTask)` báo PIC; (**#4**) Edit task đổi `assigned_to` (so `prevPic`) → báo **PIC mới**; (**#6**) comment có `@mention` / reply → `notifyTaskMentions` báo người được tag + người được reply (bỏ qua tự-báo-mình, dùng `findUserIdByName`); (**#7**) status `review→ready` (Account/Admin duyệt nội bộ — PIC không set được `ready`) → thêm nhánh trong `notifyTaskStatusChange` báo PIC "Task đã được duyệt". Kết hợp producer cũ (review→staff, revision/feedback_fix→PIC) → handoff Account↔PIC giờ phủ đủ.
- **🔁 Brief Wording rounds (Client yêu cầu chỉnh brief vòng 2,3…) (2026-06-12)**: KHÁC Preview feedback (design cap 3 vòng) — **wording KHÔNG giới hạn vòng**. Client gửi chỉnh (`client-dashboard.js wordingFeedback`) → `brief_wording_status='client_feedback'`, `brief_wording_round++`, note `wording_client_feedback='[Wording Vòng N] …'`. **Content** (content-workbench) thấy callout đỏ "Client yêu cầu chỉnh — Vòng N" + full text → "Chỉnh theo Client" → "Gửi Account duyệt" (notify account). **Account** (database-orders Brief Wording Workflow) thấy "Vòng N" + `wording_client_feedback` ("Client yêu cầu chỉnh"); nút "Gửi Client" DISABLED tới khi content resubmit (`submitted_to_account`). Client duyệt (`approveWording`) → `client_approved`. **FIX cùng đợt**: 2 producer wording client (`wording_client_feedback` + `wording_client_approved`) trước dùng type migration-gated → fail thầm; đổi cả 2 sang `order_status_changed` (base-safe) → account/content nhận chắc. Sửa copy "gửi lại qua Lead Content"→"gửi lại Account" + link admin/account → `database-orders.html?id=` (trước trỏ content-team của lead).
- **🚦 Wording CAP 2 vòng (2026-06-12)**: brief wording giờ **tối đa 2 vòng chỉnh từ Client** (`client-dashboard.js WORDING_ROUND_LIMIT=2` + `wordingAtLimit(o)` = `brief_wording_round>=2`). Khi client SẮP gửi vòng cuối (đã dùng 1 vòng → `wround===1`): hiện cảnh báo `WORDING_LAST_ROUND_MSG` ("vòng chỉnh wording cuối 2/2") ở **action card** + **modal** (`wfb-round-warn`) + **toast** sau khi gửi. Khi đã dùng hết 2 vòng (`wround>=2`): **ẩn nút "Yêu cầu chỉnh brief"** + hiện `WORDING_LIMIT_MSG`, chỉ còn "Xác nhận brief"; chặn cứng ở `openWordingFeedbackModal` + submit handler (defense-in-depth). Account: `database-orders.js soft3` ngưỡng `round>=3`→**`round>=2`** + đổi message "đã đạt tối đa 2 vòng chỉnh". (KHÁC Preview design vẫn 3 vòng — `REVISION_ROUNDS`/`revision_limit` không đổi.)

**Phase 1.5b — Client notifications sync (2026-05-23):**
- ✅ `database-orders.js notifyClient()` helper — lookup `order.requester_id` first, fallback `users.id` qua `requester_email`. INSERT notification row fire-and-forget.
- ✅ `database-orders.js updateStatus()` — hooked 3 status transitions: `checking` (🔎 Brief đang được kiểm tra, type=`order_status_changed`), `needinfo` (⚠ Cần bổ sung brief, type=`order_needinfo`), `confirmed` (✅ Brief đã được xác nhận, type=`order_confirmed`).
- ✅ `database-orders.js pushToProduction()` — thêm notification cho client (🚀 Đã chuyển sang sản xuất, type=`order_status_changed`) song song với notify PIC.
- ✅ `delivery-log.js notifyClientDelivery()` helper — lookup `users.id` qua `delivery.requester_email` (delivery rows không có requester_id field). Hooks: `send_preview` (👀 Đã có bản xem trước, type=`delivery_preview`, kèm link preview file) + `send_final` (📦 Đã bàn giao final, type=`delivery_final`, kèm link final file).
- ✅ `client-dashboard.js` Consumer wired:
  - Replaced 5 mock NOTIFS entries → `let NOTIFS = []`.
  - `NOTIF_TYPE_UI_MAP` bridge Supabase notification.type → client UI type (needinfo/preview/rating/confirmed/cancelled/system).
  - `mapNotifFromSupabase(n)` adapter: row → mock NOTIFS shape `{id, type, raw_type, order_id, title, message, link, time, read}`.
  - `formatNotifTime(s)` ISO timestamptz → "DD/MM/YYYY HH:MM".
  - `loadNotificationsFromStore()` async fetch `MH.store.notifications.listAll(50)` → replace NOTIFS + re-seed `state.notifRead`. Always-swap pattern.
  - `startNotificationsRealtime()` subscribe `notif-{user.id}` channel filter `user_id=eq.{user.id}` → INSERT event → `NOTIFS.unshift(mapped)` + `renderNotifications()` + toast popup 🔔 6s. Cleanup channel on `beforeunload`.
  - Mark-as-read click handler now calls `MH.store.notifications.markRead(notifId)` write-through (fire-and-forget).
- **Producer/consumer pattern**: producers (database-orders.js, delivery-log.js, order-form.js) fire-and-forget INSERT vào notifications table với try/catch + console.warn; consumer (client-dashboard.js, app.js initNotificationBell) subscribe Realtime channel filter theo user_id để push UI.
- Type schema: `notifications.type` CHECK constraint (từ `add-cancel-fields.sql`) cover: task_assigned, task_status_changed, task_comment, order_new, order_status_changed, order_confirmed, order_needinfo, order_cancelled, delivery_preview, delivery_final, rating_received, system.
- **🐛🔔 GOTCHA `related_entity_type` CHECK (fix 2026-06-19)**: cột `notifications.related_entity_type` cũng có CHECK `IN ('tasks','orders','deliveries','comments') OR NULL` (add-notifications.sql) — **KHÔNG chỉ `type` mới bị CHECK**. Producer gửi giá trị ngoài set (vd `'lead_task'`, `'content_tasks'`, `'content_plans'`) → INSERT **fail âm thầm** (fire-and-forget try/catch nuốt lỗi) → người nhận KHÔNG thấy noti. **Hậu quả đã gặp**: TOÀN BỘ noti `supervisor-planning.js` (builder `planNotif` dùng `'lead_task'`) chưa từng gửi được kể từ 2026-06-17; noti Content Team Deep Workflow (gán PIC / submit / trả chỉnh / duyệt, `'content_tasks'`) cũng câm. **Fix**: đổi `related_entity_type` → `null` ở các producer này (link đã lo điều hướng; `related_entity_id` giữ nguyên text tự do, KHÔNG có CHECK). **Quy tắc**: producer mới phải dùng `related_entity_type` ∈ set hợp lệ HOẶC `null` — base-CHECK-safe như đã làm với `type`. (Noti `order_new`/order/task của Content→Media + flow cũ dùng `'orders'`/`'tasks'` hợp lệ → vẫn chạy bình thường.)

**Phase 1.5c — Reverse-direction brief supplement (2026-05-23):**
- ✅ `client-dashboard.js info-submit` handler (Bổ sung brief modal): trước chỉ optimistic local `o.status='checking'` + toast → giờ đầy đủ flow:
  - Optimistic UI: `o.status='checking'`, clear `o.need_info`, close modal, re-render.
  - Persist Supabase: `MH.store.orders.update(orderId, { account_status:'checking', internal_note: prevNote + '\n\n[Client bổ sung — DD/MM/YYYY HH:MM]\nNội dung: ...\nLink: ...', last_updated })`. Append vào `internal_note` (text free) để **account thấy ngay trong drawer "Ghi chú nội bộ"** mà không cần SQL migration. Timestamp prefix giúp phân biệt source.
  - Notify admin + account active: query `users.in('role',['admin','account']).eq('status','active')` → bulk INSERT notifications `type='order_status_changed'`, title='📥 Client đã bổ sung brief', link=`database-orders.html?id=<order_id>`. Pattern y hệt `order-form.js doSubmit` lúc client tạo order mới.
  - Cập nhật `o.__raw.internal_note` + `o.__raw.account_status` để local cache đồng nhất sau refresh.
- Gap còn lại (out-of-scope): account-side click "Yêu cầu bổ sung" hiện chỉ `updateStatus('needinfo')` → KHÔNG có modal cho account nhập "cần bổ sung gì cụ thể" → client thấy default text "Vui lòng bổ sung brief — liên hệ Account team." Cần thêm modal account-side trong lần refine sau.

**Shoot location field (2026-05-23):**
- ✅ SQL migration `supabase/add-shoot-location.sql` — ADD `shoot_location text` cho cả `orders` và `tasks`. Idempotent (`IF NOT EXISTS`). **Cần chạy migration này trong Supabase SQL Editor để field hoạt động trên Railway DB.**
- ✅ Order Form (`request.html` + `order-form.js`): các sub-form `shoot` và `photo` đã có sẵn input `shooting_location`/`photo_location` từ trước → `doSubmit` giờ map qua `shoot_location` khi INSERT row Supabase. Field optional, không validate ràng buộc theo type (UI conditional show là đủ).
- ✅ Database Orders drawer: Brief Information section thêm dòng `<dt>Địa điểm</dt>` khi `request_type IN ('photo','shoot')`.
- ✅ Push to Production: `taskPayload.shoot_location` kế thừa từ `order.shoot_location` khi `request_type IN ('photo','shoot')` (else null). "Create Task from this Order" deep-link cũng pass qua URL param `shoot_location`.
- ✅ Client Portal drawer: hiển thị "Địa điểm" trong section detail-row khi `request_type IN ('photo','shoot')` và `shoot_location` có value.
- ✅ Task Tracker (`production-board.html` + `production-board.js`): modal "Giao việc nội bộ" thêm row `#tm-location-row` show/hide khi `tmType.value IN ('photo','shoot')`. Save (cả Create lẫn Edit) include `shoot_location` vào persistTask payload. Drawer Brief Information section hiển thị `<dt>Địa điểm</dt>` khi task_type photo/shoot.
- Field optional ở tất cả entry point — không block submit; UI ẩn row khi type không phải photo/shoot để tránh nhiễu.
- ✅ 2026-05-29 resilience: `assets/data-store.js` catches Supabase `PGRST204` schema-cache errors for optional `shoot_location`, then retries orders/tasks write without that field. This prevents photo/shoot orders from getting stuck as local-only while the DB migration/cache is not updated. Still run `supabase/add-shoot-location.sql` to persist location values.

**Header compact redesign (2026-05-23):**
- ✅ Header top-right: **[Theme circle 36px] [Bell circle 36px] [Profile: text + avatar 36px]**. Theme toggle rewrite từ pill switch 48×26 với thumb translateX → icon-only circular đồng nhất bell.
- ✅ Profile chip giờ là **inline text + avatar tròn KHÔNG border** (chốt 2026-05-23): `flex-direction: row-reverse` để name+role text bên trái, avatar bên phải. Text 2 dòng (name 13px bold + role badge 10px). Cả cụm clickable mở dropdown menu cũ. Hover: background `var(--surface-2)` + avatar ring shadow. Mobile (`max-width: 560px`): ẩn text, chỉ avatar tròn (avoid layout vỡ).
- ✅ **VN/EN lang toggle ĐÃ REMOVED** (tested chưa cần, scope quá lớn nếu dịch full 17 page). `assets/i18n.js` đã xóa, CSS `.lang-pill` đã xóa, `data-i18n` attrs trên 3 profile menu items + 2 aria labels còn lại trong HTML nhưng KHÔNG có consumer — không gây lỗi, nhưng nếu cần clean có thể strip sau.

**Homepage hero background refresh (2026-05-26):**
- ✅ `index.html` hero thêm `hero-bg` decorative layer (`aria-hidden`) với orb/line/floating panels, inspired by QuickMagic mood nhưng dùng CB brand navy `#191970` + red `#BA110F` only.
- ✅ `assets/styles.css` hỗ trợ 2 mode: Light mode = clean bright portal có grid/glow nhẹ; Dark mode = cinematic workspace navy/red, glass trust strip, floating panels rõ hơn. Floating panels là **visual-only, không có text** để tránh người xem hiểu nhầm là button/chip có thể click. Không thêm ảnh nền bitmap/dependency.
- ✅ Hero content, CTA, character asset và flow không đổi. Mobile/tablet nhỏ stack CTA full-width, hide bớt decorative panels để tránh overflow.
- ✅ 2026-05-29 copy update: Homepage hero chuyển wording từ "khách hàng" sang "Nội bộ CB Centres" để đúng audience nội bộ.
- ✅ 2026-06-12 **Support strip = block nội bộ** (không còn CTA ngoài): `index.html` (+ scratch `_index-gsap-demo.html`) đổi `.support-strip` từ "Cần tư vấn dự án lớn / booking dài hạn" → eyebrow "QUY TRÌNH NỘI BỘ" + h2 "Yêu cầu hợp lệ sẽ được tiếp nhận và phân công theo quy trình" + nút "Xem quy trình tiếp nhận →" link `help.html#requesting` (section "Cách viết brief tốt" — intake; KHÔNG dùng `/huong-dan` vì static site `.html` → sẽ 404). Chỉ đổi text+href; layout/gradient/radius/responsive (`@media 720px` stack) giữ nguyên. KHÔNG đụng section "4 tác vụ chính" (quick-cards) phía trên hay footer phía dưới.
- ✅ 2026-06-12 **Contact info chuẩn hóa toàn site** (audit cross-surface): email `hub@creative.vn`→`marketing@cbcentres.com`, phone placeholder `+84 000 000 000`→hotline `1900 866 855` (`tel:1900866855`), giờ làm việc → `T2–T6 7:30–11:30 · 13:30–17:00 / T7 7:30–11:30`. Sửa ở: `index.html` footer · `help.html` footer + contact card (nút "Gọi điện · 1900 866 855") · `settings.html` Support Phone default · `_index-gsap-demo.html`. (settings Support Email vốn đã `support@cbcentres.com` — giữ.)

**🎬 Homepage GSAP motion (2026-06-11):**
- ✅ `assets/home-motion.js` (MỚI) + nạp **GSAP 3.12.5** (gsap + ScrollTrigger, CDN cdnjs) ở cuối `index.html`. Thêm: count-up số liệu hero (`[data-countup]` trên `.hero-meta strong`), scroll-reveal `.quick-card`/`.wf-step`/`.lookup-card`/`.faq-item`/`.support-strip`, parallax `.hero-bg`/`.hero-graphic-wrap`, thanh tiến trình cuộn `#gsap-progress`, header `.is-scrolled` đổ bóng, hover nâng thẻ + icon pop, magnetic `.btn-hero`. **KHÔNG đụng hero entrance `.fade-up` (CSS) — GSAP chỉ bổ sung, tránh chạy 2 lần.** Bọc trong `gsap.matchMedia()` → `prefers-reduced-motion` hiện full trạng thái cuối; guard `if(!window.gsap)return` nên CDN fail thì trang vẫn hiện đủ nội dung (opacity:1). CSS helper (#gsap-progress + .site-header.is-scrolled + .btn-hero will-change) inline trong `<head>` index.html (scoped riêng trang chủ, KHÔNG đụng `styles.css`). **Flow trang chủ/đăng nhập GIỮ NGUYÊN gốc** (nút "Đăng nhập" như cũ; KHÔNG session-aware — đã thử & rollback theo yêu cầu).
- 📄 Bản thử nghiệm độc lập (KHÔNG ảnh hưởng production, prefix `_`): `_gsap-demo.html` (dashboard demo) + `_index-gsap-demo.html` (nháp trang chủ trước khi áp). Có thể xóa bất cứ lúc nào.

**Order Form smart flow stepper (2026-05-26):**
- ✅ `request.html` sidebar stepper A-G đổi sang timeline 1-7 có icon, mô tả ngắn từng bước, progress bar và trạng thái active/done để thể hiện flow kế tiếp rõ hơn.
- ✅ `assets/order-form.js` sync active step theo scroll/click bằng `IntersectionObserver`; done state vẫn dựa vào completion thật của từng section.
- ✅ Bước 5 `Tài nguyên` là optional nên không auto-complete khi chưa có file/link/note; chỉ chuyển done khi user thêm tài nguyên thật. Giữ selector/form submit flow hiện tại.
- ✅ 2026-05-29 sync UI: section header badge trong từng card đổi từ A-G sang 1-7, cùng ngôn ngữ với sidebar flow. Badge dùng node tròn thống nhất, không alternate chữ/màu gây lệch nhận thức.
- ✅ 2026-05-29 copy update: Section 2 field hiển thị `Mã kế hoạch` với placeholder `KH07/CBMK`; giữ id/name `campaign_code` để không ảnh hưởng payload/storage.
- ✅ 2026-06-02 **GỘP Quay + Chụp → 1 type `media` "Quay / Chụp ảnh"** (Phase 1+2). 1 tile `media` (bỏ tile shoot/photo riêng); sub-form "Thông tin buổi Quay / Chụp" có `media_service` (☑Quay ☑Chụp ≥1) + onsite dùng chung; `media_location`→`shoot_location`, onsite info gói vào `content_brief`. **Media KHÔNG dùng checkbox "Hạng mục cần sản xuất"** (ẩn `data-subform-hide-for="media"`) — chỉ service selector ☑Quay ☑Chụp + ô Nội dung. **Push order media tạo 2 task theo PIC được gán** (không theo deliverable): task_type `shoot` (Quay, assigned=production_pic_video) + `photo` (Chụp, assigned=production_pic_photo). Order drawer media hiện **2 ô PIC** (Quay/Chụp). Push/readiness/brief-checklist media-aware (bỏ check deliverable+size). **SQL `supabase/add-media-pics.sql` BẮT BUỘC**: mở rộng CHECK `orders.request_type` thêm `'media'` + 2 cột PIC (tasks.task_type giữ nguyên vì media split thành shoot/photo). request_type cũ `shoot`/`photo` vẫn hợp lệ cho order cũ. TYPE_LABEL `media`='Quay / Chụp ảnh' ở 3 module + order-form REQ_TYPE_LABEL.
- ✅ 2026-06-02 (slide) ẩn Kích thước/Tỉ lệ (thêm `slide` vào hide list); Section 4 ẩn textarea `content_brief` (`data-subform-hide-for="slide"`) + thêm `#slide_source_link` "Link nội dung thô" (Google Doc/Slide, bắt buộc) qua **cơ chế mới `data-subform-show-for`** (hiện field CHỈ cho type liệt kê; CSS `[data-subform-show-for]{display:none}`+`.is-cond-shown`). doSubmit map link→`content_brief`.
- ✅ 2026-06-02 (video/motion) ẩn Kích thước/Tỉ lệ (mở rộng `data-subform-hide-for="media,video,motion"` — đã có "Tỉ lệ video"). Video sub-form thêm `#script_link` "Link kịch bản" (bắt buộc) + đổi `#footage_link`→"Link source"; bỏ checkbox "Đã có kịch bản". Links gói vào `content_brief`.
- ✅ 2026-06-02 (photo) Hạng mục Chụp ảnh: deliverable đổi "Chân dung"→`Hoạt động ngoại khóa`, "Chụp sản phẩm"→`Chụp Studio/Photoshoot` (đổi cả value). 2 trường Kích thước/Tỉ lệ + Kích thước cụ thể ẩn khi chọn photo qua **`data-subform-hide-for="photo"`** (cơ chế mới: `updateConditional` toggle `.is-hidden-cond { display:none!important }`, dùng được cho mọi type). Photo dùng sub-form "Thông tin buổi chụp" sẵn có (`photo_location`→ map vào cột `shoot_location`).
- ✅ 2026-06-02 Section 3 `Kích thước / Tỉ lệ` (`#size_ratio`, sub-form design): options đổi `Banner ngang`→`Banner ngang 3 x 1m`, `Khác — sẽ ghi rõ trong mô tả`→`Khác — điền vào ô bên cạnh`, thêm `Standee 80 x 200cm`. Field bên cạnh ĐỔI từ `Số lượng phiên bản` (`version_quantity`, number) → **`Kích thước cụ thể`** (`#custom_size`, text, **disabled mặc định**). `order-form.js syncCustomSize()` chỉ enable `custom_size` khi `size_ratio` bắt đầu bằng `Khác` (clear value khi đổi sang preset khác); gọi lúc init + on change + sau restoreDraft. **`version_quantity` đã bị bỏ hoàn toàn** (không persist DB trước giờ). doSubmit: khi `size_ratio` = `Khác…` + có `custom_size` → lưu `custom_size` vào cột `size_ratio` (DB chỉ có cột này). Preview đổi nhãn `Số phiên bản`→`Kích thước cụ thể`. CSS thêm `.input:disabled/.select:disabled/.textarea:disabled`.

**Task Workbench drawer (2026-05-29):**
- ✅ `production-board.html` task drawer thêm class `.task-workbench`; `assets/styles.css` mở rộng riêng drawer task lên workbench 2 cột: main detail + right action rail. Không ảnh hưởng drawer Database Orders vì CSS scoped theo `.task-workbench`.
- ✅ `assets/production-board.js` giữ logic status/link/meta/comment cũ, bổ sung production checklist tự tính từ status/content/link, action rail gồm Next Actions, Người liên quan, AI hint, Activity Log.
- ✅ Pattern UI: task detail là nơi xử lý task cụ thể, không chỉ xem thông tin. Giữ `TASKS` data shape và Supabase payload hiện tại.

**Order Workbench drawer (2026-05-29):**
- ✅ `database-orders.html` order drawer thêm class `.order-workbench`; `assets/styles.css` mở rộng drawer Admin/Account order thành **single-column workbench flow**: summary strip → animated next-action banner → requester → brief → điều phối → ghi chú/comment nội bộ → điều kiện tạo task → related tasks → delivery → activity.
- ✅ `assets/database-orders.js` Việt hóa các block drawer cũ (Requester/Brief/Internal/Related/Delivery/Push/Activity), thêm `orderNextAction()` để Account/Admin nhìn thấy bước kế tiếp ngay khi mở order mới.
- ✅ `internal_note` được kéo ra thành block riêng **Ghi chú / Comment nội bộ**. Đây chưa phải threaded comments như Task Tracker; cố ý giữ field/schema hiện tại để không cần migration, vẫn persist qua `persistOrder(... internal_note ...)`.
- ✅ Animation banner `Hành động kế tiếp`: CSS `orderNextAttention` nhẹ bằng lift + navy glow, bọc trong `prefers-reduced-motion` để không gây khó chịu.
- ✅ Logic cũ giữ nguyên: action buttons Check/Need Info/Confirm/Push/Cancel, save internal fields, create task from order, notification và Supabase write-through không đổi.

**Master Dashboard live refresh (2026-05-29):**
- ✅ `dashboard.html` fix refresh button: trước chỉ đổi timestamp/toast, giờ gọi lại `loadMasterDashboard()` để fetch `orders.list()` + `tasks.list()` thật.
- ✅ Thêm polling fallback 60s và Supabase Realtime hook cho `orders`/`tasks` nếu publication đã bật. Timestamp `Last updated` chỉ đổi sau khi load data thành công.

**Other 2026-05-20 work:**
- **Drawer action area refactor (cancel modal)**: bỏ stepper UI 4 chấm khỏi `database-orders.html`. `wf-hint` giờ chỉ hiện khi `isPushed` với message "✓ Đã push sang Task Tracker · PIC · Xem task →"; ẩn khi chưa push. Action button row: `[Hủy đơn]` canh trái (gradient `#E53935 → #BA110F` + `margin-right: auto`) ⟷ `[Kiểm tra brief] [Yêu cầu bổ sung] [Xác nhận brief] [Push → Production]` (Push đổi sang gradient green `#22C55E → #16A34A`). `updateStepperState()` giờ chỉ enable/disable button + toggle hint visibility, không còn DOM ops cho stepper.
- **Cancel modal**: `#cancel-modal` overlay với Order ID + Project name readonly + select "Nguyên nhân chính" (5 cause keys: brief_insufficient / no_longer_needed / deadline_mismatch / duplicate_request / other) + textarea "Lý do hủy đơn" required + checkbox "Gửi thông báo đến client" default checked. Submit → `submitCancel()` validate reason → mutate local order → `persistOrder({ account_status:'rejected', production_status:'cancelled', cancel_reason, cancel_cause, cancelled_by, cancelled_at })` → nếu notify checked + Supabase enabled → lookup client `users.id` qua `requester_id` hoặc `requester_email` → INSERT notification type=`order_cancelled` link `tracking.html?code=...`. Row kebab "Hủy đơn" cũng mở modal (bỏ `confirm()` cũ).
- **Tracking cancel banner**: `tracking.html` thêm `#r-cancel-banner` hiển thị "Yêu cầu này đã bị hủy" + cause + reason + cancelled_by/at khi `raw.account_status === 'rejected'` || `raw.production_status === 'cancelled'`. Đọc từ `data.__rawOrder` (Supabase shape).
- **Green-circle tick style (yêu cầu #5)**: `.checklist li.ok::before` + `.push-check li.ok::before` đổi sang nền `#16A34A`, check trắng, `border-radius: 9999px`, `width/height 16-18px`. Áp dụng cho Brief checklist + Push-to-Production pre-check.
- **SQL migration** `supabase/add-cancel-fields.sql`: ADD `cancel_reason text` + `cancel_cause text` (CHECK 5 keys) + `cancelled_by uuid REFERENCES users(id)` + `cancelled_at timestamptz` vào `orders`. Đồng thời extend `notifications.type` CHECK constraint với `'order_cancelled'` + `'order_new'`.
- 4-step Account workflow buttons gradient nền nhạt→đậm + Cancel red-700 (bỏ outline). _(Step 4 đã đổi từ red sang green 5/2026.)_
- Stepper UI 4 chấm + đường nối + hint text — _(đã remove 5/2026, xem mục Drawer action area refactor.)_
- `pushToProduction` (async): idempotent check (đã có task → toast warning + skip), INSERT task auto-fill từ order, INSERT notification cho PIC, UPDATE order.production_status.
- **Order drawer Điều phối — push UX (2026-06-03)**:
  - `updateStepperState`: sau khi **Xác nhận brief** (`isConfirmed && !isPushed`) → **Push → Production giữ SÁNG** làm CTA (KHÔNG mờ vì thiếu PIC/deadline — bỏ gate `readyToPush`); `pushToProduction` tự validate khi bấm + toast nếu thiếu. Push chỉ **mờ (disabled)** khi đã push (`isPushed`). _(Trước: confirm brief làm Push mờ ngay vì chưa gán PIC → gây hiểu nhầm.)_
  - Nút mới **"Xác nhận & Chuyển Production"** (`#confirm-coordination`, class `.btn-push-coord` green) trong section Điều phối/Ghi chú: gọi `applyCoordination()` (lưu PIC/deadline/status) **rồi** `pushToProduction()` → tránh lỗi bấm Push top mà chưa Lưu form. Đã push → đổi thành "✓ Đã chuyển Production" (disabled). Chưa confirm brief → disabled (title nhắc).
  - `applyCoordination()` tách từ handler `#save-internal` (dùng chung cho Lưu + Push); đọc PIC select có guard `!el.disabled` để không ghi đè khi select bị khóa.
  - `isOrderPushed(o)` helper: order đã có task → **khóa (disabled) PIC select** trong drawer (single + media Quay/Chụp) + note "đổi PIC tại Task Tracker". Lý do: đổi PIC ở order KHÔNG reassign task đã tạo → desync. Reassign đúng cách = `production-board.js` task drawer `#edit-pic`. Hint media-aware: "Quay: X · Chụp: Y" (trước đọc `production_pic` null → "—").
- **Order list sort + drilldown banner (2026-06-03)**: default list `created_at desc` (mới nhất trên cùng). Drilldown `new_requests` (New Orders) đổi `asc → desc` để cũng mới-nhất-trước. `render()` cập nhật `#dl-count` mỗi lần (trước kẹt "0 kết quả" vì tính lúc ORDERS rỗng async). Chip saved-view click gọi `removeDrilldownBanner()` (xóa banner + dọn `?dl=`) để banner drilldown cũ không kẹt đè lên view khác; sort cột KHÔNG xóa banner (re-sort trong cùng drilldown vẫn đúng).
- **⚡ Auto-sync Production Status: task → order (2026-06-03)** — order.production_status + progress TỰ đồng bộ theo task ở Task Tracker (trước: push set 'received' 1 lần rồi MANUAL). Quy tắc **bottleneck** = task tiến độ thấp nhất → order chỉ "Hoàn thành" khi MỌI task xong (media 2 task Quay+Chụp).
  - **Nguồn chính = DB trigger** `supabase/sync-order-status-from-tasks.sql` (⚠ **CHƯA CHẠY production** — cần chạy 1 lần): trigger `trg_sync_order_status` AFTER INSERT/UPDATE/DELETE ON tasks → recompute order, `SECURITY DEFINER` để bỏ qua RLS (RLS dòng 89 chỉ cho admin/account UPDATE orders; design/editor bị chặn). Helper `task_status_rank()` (= STATUS_PROGRESS) + `task_to_order_status()` (= TASK_TO_ORDER_STATUS map: pending→received, feedback_wait→delivered, feedback_fix→revision, paused→inprogress).
  - **Client-side** (`production-board.js syncOrderStatusFromTasks`): chỉ chạy cho **admin/account** (instant feedback, né RLS-denied của design/editor); query `tasks.list({order_id})` lấy task anh em + overlay status mới của task vừa đổi → `orders.update`. Design/editor dựa hoàn toàn vào trigger.
  - **Order drawer** (`database-orders.js`): ô **Production Status giờ DISABLED (read-only)** + note "Tự đồng bộ theo Task Tracker". `applyCoordination()` KHÔNG đọc/ghi đè `production_status` từ form (guard `!elProdStatus.disabled`) — tránh clobber giá trị đã sync. Account không chỉnh status tay nữa.
- **⚡ Preview → 3 vòng Feedback → Final (revision rounds, 2026-06-03)** — bàn giao VẪN trong Order drawer (KHÔNG khôi phục Delivery Log, KHÔNG dùng bảng `deliveries` cho luồng client thật). Client Portal đọc `orders.preview_link` / `orders.final_delivery_link`.
  - **Bàn giao đầu tiên LUÔN là "Preview"** (yêu cầu nghiệp vụ — không gọi Final/Draft/Demo). `sendDelivery('preview')`: set `preview_link` + `delivery_status=client_wait` + `production_status=feedback_wait` + `feedback_status=waiting_feedback`; notify client `delivery_preview` title="Sản phẩm Preview đã sẵn sàng". Final: `final_delivery_link` + `delivery_status=final` + `production_status=delivered` + `feedback_status=approved`; notify `delivery_final` → client rate.
  - **Quy tắc 3 vòng CHỈ áp dụng order design** (`appliesRevisionRule` = request_type ∈ design/digital). Type khác (video/media/slide/motion/other) → linh hoạt, KHÔNG chặn vòng 4.
  - **Fields mới trên orders** (migration `supabase/add-revision-rounds.sql` — ⚠ CHƯA chạy production): `revision_round`(0), `revision_limit`(3), `feedback_status`(waiting_feedback/feedback_received/revision_in_progress/approved/exceeded_limit), `last_feedback_at`, `last_feedback_by`, `latest_feedback_note`. Migration cũng thêm notif type `client_feedback_received` vào CHECK.
  - **Client Portal** (`client-dashboard.js`): modal feedback hiện "Bản Preview" + badge "Feedback Vòng N/3" + helper text theo vòng (REVISION_ROUNDS 1/2/3) + policy block. Submit → `revision_round++`, `feedback_status=feedback_received`, lưu `latest_feedback_note`/`last_feedback_at`/`last_feedback_by`, notify admin/account (`client_feedback_received`). **Vòng 4** (design, round≥3): KHÔNG lưu feedback thường → hiện block + CTA "Gửi yêu cầu mới" (→ request.html).
  - **Order drawer** (`database-orders.js buildRevisionPanel`): hiện vòng X/3 + feedback_status + latest feedback + time + by; round≥limit → badge "Đã đạt giới hạn chỉnh sửa" + alert + nút "Tạo task mới từ feedback này" (→ production-board createTask prefill content=feedback).
  - **notif-icons.js**: thêm icon `client_feedback_received` (messages-square, cls is-accent). Notif liên quan: `delivery_preview` · `client_feedback_received` · `delivery_final` · `rating_received`.
- **⚡ Approve Preview + Link sync Task↔Order (2026-06-04)** — tiếp nối luồng trên. Migration `supabase/add-preview-approval.sql` (⚠ CHƯA chạy): cột `approved_at`/`approved_by` + notif type `client_preview_approved`.
  - **Client "Duyệt Preview"** (`client-dashboard.js approvePreview`): nút cạnh "Gửi feedback" ở action card + order detail (state feedback_wait, chưa approved). Set `feedback_status=approved` + `approved_at/by`, **KHÔNG đổi production_status** (task-driven); notify admin/account `client_preview_approved` ("Client đã duyệt Preview … chuẩn bị gửi Final"). Sau approve hiện "Bản Preview đã được duyệt…". Icon `client_preview_approved` (badge-check) trong notif-icons.
  - **Order drawer "Gửi feedback cho PIC"** (`database-orders.js sendFeedbackToTask`): nút ở revision panel (1 task → gửi luôn; nhiều → chọn ở mục Links) + nút per-task. Append comment `type=feedback` + task `status=feedback_fix` (`store.tasks.update`) + notify PIC (`task_status_changed`) + order `feedback_status=revision_in_progress`. Không có task → CTA "Tạo task mới".
  - **Order drawer "Links from Task Tracker"** (`loadTaskLinksIntoDrawer`, async `tasks.list({order_id})`): mỗi task hiện `link_drive`/`preview_link`/`final_link` (group theo task_id + assigned_to) + nút **Copy** · **Dùng làm Preview** (điền `#dlv-preview-link`) · **Dùng làm Final** (điền `#dlv-final-link`). ⚠ Chỉ ĐIỀN ô — KHÔNG tự gửi client; client chỉ nhận khi Account bấm "Gửi Preview/Final".
  - **Task drawer feedback block** (`production-board.js loadOrderFeedbackIntoTaskDrawer`, async `orders.get`): nếu order liên kết có `revision_round>0` → hiện "Client Feedback Round N/3" + `latest_feedback_note` + feedback_status; `task.status==='feedback_fix'` → badge "Đang chỉnh Feedback Round N" (giữ key `feedback_fix`).
  - **Gatekeeper**: PIC sửa link ở Task Tracker; CHỈ Account/Admin gửi Preview/Final cho client từ Order drawer (drawer này vốn chỉ admin/account).
- **⚡ Order Lifecycle Timeline (display-only, 2026-06-04)** — `database-orders.js computeLifecycle(o)` + `buildLifecycleTimeline(o)` render ở ĐẦU drawer body (trên summary grid). 4 nhóm **Brief → Production → Preview & Feedback → Final & Rating**, mỗi nhóm state done(✓ xanh)/active(dot pulse)/pending(mờ) + status ngắn; dưới có dòng summary (vd "Đã gửi Preview · Chờ Client phản hồi", "Client đã gửi Feedback Round N · Đang chờ gửi cho PIC", "Client đã duyệt Preview · Chờ gửi Final"). **KHÔNG dùng action buttons làm timeline** — timeline chỉ hiển thị, buttons (Kiểm tra/Push/Gửi Preview/Final/Hủy đơn) là Quick Actions. `orderNextAction(o)` viết lại để **suy ra từ lifecycle stage** (12 stage: brief/brief_confirmed/production/preview_sent/feedback_received/revising/exceeded/preview_approved/final_sent/rated/completed/cancelled). CSS `.order-lifecycle`/`.lc-*`.
- **📅 Calendar / Lịch (2026-06-08)** — trang mới `calendar.html` + `assets/calendar.js` (clone cấu trúc `task-dashboard.html`). **Month / Week / Day**, read-only. **Day view (ref ClickUp "Date", thêm 2026-06-08)**: bấm **"+N khác"** hoặc **click số ngày** ở Month → mở Day view = mini-calendar trái (`miniCursor` state, `‹ ›` đổi tháng, chấm `has-evt`, `is-today`/`is-selected`) + panel phải danh sách ĐẦY ĐỦ task của ngày (`selectedDay` state), mỗi dòng có tên + avatar PIC. Toolbar `‹ ›` ở Day view dịch ±1 ngày; click dòng → popover sẵn có. `renderDay`/`miniCalHtml`/`dayListHtml` + `gotoDay(d)` (đồng bộ selectedDay/miniCursor/cursor). (Agenda cũ đã bỏ — Day thay thế.) **4 loại event chấm theo ngày**: `task-deadline` (navy, `tasks.internal_deadline`) · `order-deadline` (amber, `orders.requested_deadline`→fallback `internal_deadline`) · `shoot` (đỏ, lịch quay/chụp) · `delivery` (green, `orders.delivery_date`/final). **Role filter (mấu chốt)**: admin/account = FULL (mọi task+order); design/editor = CHỈ task `isMyTask(assigned_to)` (copy helper từ production-board, không xem order-level); content = order đang wording active (`brief_wording_status` ∉ none/completed/client_approved). Click event → popover → "Mở chi tiết" navigate `production-board.html?id=` (task) / `database-orders.html?id=` (order, admin/account) / `content-workbench.html?order=` (content). **"Hôm nay" = `new Date()` NGÀY THẬT** (KHÔNG hardcode demo anchor — sửa 2026-06-08 theo yêu cầu; mặc định mở tháng hiện tại). **Đồng bộ realtime**: `startRealtime()` = Supabase Realtime channel trên `orders`/`tasks` (best-effort, chỉ fire nếu 2 bảng đã ADD vào publication — hiện CHƯA bật → cần `enable-realtime` cho orders/tasks) + poll 60s + reload khi `visibilitychange`; mọi reload silent (giữ tháng/view/filter). **Shoot date**: thêm cột `orders/tasks.shoot_date` + `shoot_time` (migration `add-shoot-date.sql` ⚠ CHƯA chạy production); `order-form.js` doSubmit lưu `media_date`→`shoot_date` (vẫn giữ trong content_brief để hiển thị); `database-orders.js pushToProduction` kế thừa shoot_date/time sang task quay/chụp (cạnh shoot_location). calendar.js có fallback regex bóc `Ngày: YYYY-MM-DD` từ content_brief khi chưa migrate. **Nav inject 1 chỗ** qua `app.js injectCalendarNav()` (pattern injectContentNav, chèn sau Internal Task Tracker, `data-show-roles="admin,account,design,editor,content"`) → mọi trang nội bộ thấy mà không sửa 11 HTML. CSS `.cal-*` cuối `styles.css` (token-driven → tự dark mode; `.cal-chip[data-show-roles]` có rule hiển thị riêng vì là button không phải .btn). ⚠ Demo không bật Supabase: `store.tasks/orders.list()` fallback localStorage (mh-extra-tasks/mh-submitted-orders) — built-in mock của board/orders KHÔNG load trên trang này; bản Railway bật Supabase nên đủ data.
- **⏰ Hạn hoàn thành wording (2026-06-08)** — trước đây Account "Chuyển Content Wording" KHÔNG kèm deadline; cột "Deadline" ở Content Wording chỉ là `requested_deadline` (deadline KHÁCH). Thêm cột `orders.wording_deadline` (migration `add-wording-deadline.sql` ⚠ pending). **Account đặt ở Order drawer** (`database-orders.js buildWordingWorkflow`): ô `#ow-wording-deadline` (datetime-local) + nút "Lưu hạn" (`act-save-wording-deadline`→`saveWordingDeadline`, persistOrder trực tiếp). `transferToWording` đọc ô này khi Chuyển → hạn đi kèm. **Content Wording chỉ HIỂN THỊ** (không sửa — vì `content-workbench.js persist()` đi qua RPC `update_brief_wording` whitelist; đặt hạn là việc Account → ghi thẳng `orders.update`, KHÔNG đụng RPC). Bảng Content Wording thêm cột **"Hạn wording"** (cạnh "Deadline khách") + drawer row; **tô đỏ `.cwb-overdue`** khi `isWordingOverdue()` (có hạn, chưa approved/completed, chưa hủy, đã quá giờ). Helper `isWordingOverdue` ở cả database-orders.js + content-workbench.js. **(2026-06-08 +)** **Notify Content khi chuyển wording**: `transferToWording` gọi `notifyContentWording(o)` — bulk INSERT notifications cho mọi user `role='content'` active (type `task_assigned`, link `content-workbench.html?id=`, kèm hạn nếu có) → content nhận chuông realtime. **Calendar role Content** giờ CHỈ hiển thị **"Hạn Content Wording"** (event type `wording-deadline`, màu tím `#6B21A8`, từ `orders.wording_deadline` của order đang wording active) — ẩn hẳn deadline order/shoot; chip chỉ còn "Hạn Wording" (`calendar.html` chip `data-show-roles="content"` + CSS ẩn chip task/shoot cho content). navTarget content → `content-workbench.html?id=` (khớp param content-workbench đọc).
- **🐛 FIX desync lifecycle Content↔Account (2026-06-08)**: `content-workbench.js loadOrders` trước `Object.assign({}, dbOrder, cache[id])` → cache localStorage (`mh-wording-drafts`) ĐÈ LÊN DB. Cache giữ `brief_wording_status` cũ (vd `submitted_to_account` lúc content gửi), trong khi Account/Client đã tiến order ở DB (→ `client_approved`) mà KHÔNG ghi vào cache content → Content drawer kẹt stage cũ (cũng làm `account_revision`/`client_feedback` không hiện cho content). **Fix**: khi `supabaseEnabled`, danh sách `DB_AUTH` (brief_wording_status/round + các mốc wording + account/production_status + wording_deadline) **luôn lấy từ DB** sau merge — cache CHỈ còn preserve bản nháp text/checklist/links/activity của content. DB là nguồn thật cho lifecycle.
- **🐛 FIX CRITICAL — task status/link không lưu (2026-06-03)**: `persistTask()` (production-board.js) trước dùng `tasks.upsert(partial {task_id,status})`. Bảng `tasks` có `project_name`/`task_type` **NOT NULL** → upsert partial dựng INSERT-candidate thiếu cột → **vi phạm NOT NULL TRƯỚC ON CONFLICT** → mọi update task (status, 3 link, meta) **fail âm thầm** (chỉ comment persist vì dùng INSERT riêng). Triệu chứng: đổi status/dán link → reload mất hết. **Fix**: thêm `store.tasks.update(taskId, patch)` (UPDATE .eq, giống `orders.update`) → `persistTask` dùng update; tạo task mới dùng `persistNewTask` (upsert full row). ⚠ **QUY TẮC: KHÔNG bao giờ upsert partial vào bảng có cột NOT NULL — patch thì UPDATE, tạo mới thì upsert full row.** Đây cũng là điều kiện để DB trigger auto-sync thật sự chạy (tasks UPDATE phải thành công trigger mới fire).
- ROLE_LABEL trong `user-management.js` mở rộng `design`/`editor` (trước thiếu → UI hiển thị UNDEFINED). CSS `rt--design` teal + `rt--editor` cam.
- **🐛 Task Tracker fix Design/Editor (2026-06-12)**: (1) **Ẩn nút "Giao việc nội bộ"** (`#btn-create-task`) cho design/editor (chỉ admin/account tạo task; role này chỉ nhận & thực hiện) + guard click. (2) **`TODAY` hardcode `new Date('2026-05-13')` → ngày THẬT** (`new Date()` set 00:00) ở CẢ `production-board.js` LẪN `database-orders.js` (count "Còn Nd"/overdue lệch ~30 ngày). `diffDays` đổi sang so **NGÀY lịch** (date-only) + `Math.round` (trước `Math.ceil` trên timestamp → lệch giờ; giờ deadline hôm nay = "Hôm nay", mai = "Còn 1d"). (3) **My Tasks block grouping** (`renderMyTasks`): `ready` (đã duyệt nội bộ, đang pha Preview/bàn giao) + `feedback_wait` (đã gửi Preview chờ client) chuyển từ done → **`progress` (Đang thực hiện)**; `done` (Hoàn thành) giờ CHỈ `delivered`(final)+`completed`(đóng). Trước: account duyệt review→`ready` làm task nhảy "Hoàn thành" sớm dù mới gửi Preview. Giờ task ở "Đang thực hiện" suốt pha Preview→Feedback, chỉ "Hoàn thành" khi đóng/bàn giao final.
- **🐛🔴 FIX CRITICAL — Client không nhận Final (2026-06-12)**: `database-orders.js sendDelivery('final')` set `feedback_status='final_sent'` — value này **KHÔNG có trong CHECK** `orders_feedback_status_check` (chỉ có waiting_feedback/feedback_received/revision_in_progress/approved/exceeded_limit). → Khi cột feedback_status đã tồn tại (migration đã chạy), UPDATE final **vi phạm CHECK (lỗi 23514)** → fail CẢ cục → `final_delivery_link`+`production_status='delivered'` KHÔNG lưu → **client không thấy Final** (preview OK vì `waiting_feedback` hợp lệ). `stripMissingOptionalColumn` của data-store CHỈ bắt PGRST204 (cột thiếu), KHÔNG bắt 23514 nên không cứu. **Fix**: đổi `final_sent`→**`approved`** (hợp lệ CHECK; comment migration ghi 'approved' = "client duyệt / đã gửi Final"). Lifecycle/`isFinalDelivered` nhận diện Final qua `final_delivery_link`+`production_status='delivered'` nên không cần value riêng. Stage label 'final_sent' (string nội bộ) giữ nguyên, vô hại. **QUY TẮC: chỉ set feedback_status/delivery_status/production_status bằng value NẰM TRONG CHECK** — value sai = fail im cả update (không phải PGRST204 nên không auto-strip).
- **🔗 Account NHẬN BIẾT order chỉnh sửa tiếp (2026-06-13)**: order revision (client tạo sau khi quá 3 vòng) trước chỉ nhúng marker chìm trong content_brief → account khó biết. Thêm `database-orders.js getRevisionRef(o)` (ưu tiên cột `parent_order_id`, fallback regex bóc `MEDIA-...` từ content_brief — KHÔNG cần migration). Hiển thị 3 chỗ: **badge `↩ Tiếp`** ở bảng (cạnh NEW, CSS `.rev-cont-badge`) · **banner đỏ đầu Order drawer** ("Order chỉnh sửa tiếp — từ Order gốc MEDIA-XXX, link mở order gốc") · **notification gửi account đổi tiêu đề** "↩ Order chỉnh sửa tiếp: …" + message "tiếp nối từ MEDIA-XXX (sau 3 vòng feedback)" (`order-form.js`). order-form vốn đã nhúng ref vào content_brief + set parent_order_id best-effort.
- **🔁 Refill SÂU order revision + Rating optional/encouraged (2026-06-13)**: **(V1)** `order-form.js prefillFromOrder(o)` — revision mode (`?mode=revision&ref_order=`) giờ điền ĐẦY ĐỦ brief từ order gốc (project_purpose, target_audience, usage_channels, deliverable_type, size_ratio→custom_size, content_brief, main_headline, cta, mandatory_info, creative_direction, reference_link, campaign_code) — trước chỉ project/department/type → client chỉ bổ sung nội dung mới. Reuse `syncChips`/`syncCustomSize`/`updateConditional`. **(V2 — phương án 1)** Final = hoàn tất bàn giao, **rating OPTIONAL không gate hoàn thành**: `database-orders.js` thêm nút **"Đóng đơn — Hoàn thành"** (`closeOrderCompleted`, hiện khi đã gửi Final & chưa completed → set production_status+delivery_status='completed') = lưới an toàn khi client KHÔNG đánh giá (trước phải vòng qua Task Tracker đóng task). **Tăng tỉ lệ rating**: `client-dashboard.js` rating nudge nổi bật (`.cl-rate-nudge` + nút `.cl-rate-btn` pulse, 5 sao bounce, copy khích lệ) ở Final drawer block + Action Center card (glow `.ac--rating`), + `maybeRemindRating()` toast nhắc 1 lần/phiên nếu có sản phẩm chưa rating. CSS animation trong `client-dashboard.html` (`@keyframes clRatePulse/clStarBounce/clCardGlow`, tắt khi reduced-motion). **KHÔNG thêm nút "đã nhận" cạnh client** (tránh cạnh tranh rating). `pubEffStatus`: rating → 'completed' (display) vẫn giữ; backend completed do nút Đóng đơn / task close.
- **🐛 FIX gửi Final nhiều lần (2026-06-12)**: nút "Gửi Final → Client" trước LUÔN hiện/enable cho admin/account → bấm lại nhiều lần = re-persist + re-notify client. **Fix 2 lớp** (`database-orders.js`): (1) UI — khi `o.final_delivery_link` đã có: input final `readonly` + nút **disabled** đổi nhãn "✓ Đã gửi Final" + helper "chỉ gửi MỘT lần"; (2) `sendDelivery` guard đầu hàm: `isFinal && currentOrder.final_delivery_link` → toast "Đã gửi Final" + return (chặn double-click trước khi drawer re-render). **Preview KHÔNG khóa** (cố ý — vòng feedback cần gửi Preview mới nhiều lần). Section Preview vốn đã ẩn khi đã có final.
- **👔 Content submit định tuyến ĐỘNG Lead/Account (2026-06-12)**: bật lại role **Lead Content** (`lead_content`). `content-workbench.js` thêm flag `HAS_LEAD` + `detectLeads()` (query `users` role=lead_content active, gọi ở init). `submitWording()` (đổi tên từ `submitToAccount`): **có Lead** → `submitted_to_lead` + notify `lead_content` (link content-team.html), nút "Gửi Lead Content duyệt"; **chưa có Lead** → fallback `submitted_to_account` + notify admin/account (như trước). Tự khớp migration: Lead user chỉ tồn tại sau khi chạy `add-content-team.sql` (users_role_check + RPC v2 cho phép submitted_to_lead) → HAS_LEAD true ⟺ RPC v2 sẵn sàng, không lo RPC reject. content-team.js (Lead Workspace) đã có sẵn: Lead duyệt→admin/account, gán PIC→PIC, trả chỉnh→content (đều type base-safe). User Management đã có option role "Lead Content" + badge `rt--lead_content`. **Còn lại (việc anh làm trên Supabase)**: chạy `add-content-team.sql` + tạo user role `lead_content` trong Supabase Auth (metadata {name, role}).
- **🎨 Content Wording badge DONE = xanh + check (2026-06-12)**: `content-workbench.js` trước render MỌI status WORDING bằng `tb-status s--wording` (tím) → khó nhận diện order đã chốt. Thêm helper `wsIsDone/wsCls/wsMark` (`WS_DONE=['client_approved','completed']`): done → class **`s--completed`** (xanh lá, đã có sẵn styles.css) + **icon check** thay dot; còn lại giữ tím. Áp ở cả badge bảng (renderTable) lẫn header drawer (`#cwb-d-status`). (content-team.js của lead chưa đụng — role content dùng content-workbench.)
- Tracking auth flow: chưa login + click Tra cứu → `requireLoginModal()` overlay với CTA preserve `?code=`. Scope check: legacy `client_scope` OR `requester_email`/`requester_id` match.
- Client redirect fix: dashboard.html/ai-tools.js/production-board.js role=client → `client-dashboard.html` (trước redirect `tracking.html` legacy).
- Demo Accounts section + ACCOUNTS map + DEMO_PWD hardcoded XÓA khỏi login.html (security). loginAs() chỉ Supabase auth.
- Cross-page tasks/orders: clear demo seed qua `supabase/clear-demo.sql`, đồng bộ 9 user password (5 demo `Cbmedia2026`, 4 client test `client@test`) qua SQL `UPDATE auth.users SET encrypted_password = crypt(..., gen_salt('bf'))`.

**Migration pattern thống nhất:**
```text
1. Expose mock: window.MH_MOCK_X = X_ARRAY
2. Async swap: loadXFromStore(localArr) — Promise<number|null>, swap content + re-render
3. Optimistic mutation: mutate local trước, persistX(id, patch) fire-and-forget Supabase
4. Khi supabaseEnabled === false → tất cả persist là no-op, demo flow KHÔNG đổi
```
- `assets/app.js` — **Sentry lazy-loader top-of-IIFE** (load CDN bundle nếu DSN có, init kèm beforeSend tag user role/email từ `mh-user`, skip nếu DSN trống → 0 network call). Theme toggle (CSS-driven pill switch), mobile nav, toast, copy helpers, Profile editor modal, **header profile chip** (`#header-profile-chip`) toggle + populate. Functions: `refreshProfileChip(user)`, `refreshHeaderChip(user)`, `syncChipFromUser()`, `openProfileModal()`.
- `assets/logo.png` — Resized brand logo, 256×256, ~13 KB.

---

## 4. Auth & Roles

Session key: `localStorage['mh-user']`.

```json
{
  "role": "admin",
  "name": "Mai Phương",
  "email": "admin@cb.vn",
  "initials": "MP",
  "title": "Admin · Account Lead",
  "avatar": "",
  "phone": "",
  "department": "",
  "bio": ""
}
```

Profile fields (set via Profile modal từ header profile chip → Hồ sơ cá nhân):

- `avatar`: data URL JPEG ≤ 256px, render qua `.avatar.has-img > img`. Trống = fallback initials.
- `phone`, `department`, `bio`: free text (department có datalist 7 chi nhánh).
- Role chỉ Admin được đổi (select); các role khác readonly badge.

Demo accounts — **login thủ công** (nhập email + password; tiles demo + password cứng đã GỠ khỏi `login.html` vì lý do bảo mật, auth qua Supabase). Password demo: **`Cbmedia2026`** (5 demo) · client test riêng `client@test`:

| Email | Role | Redirect sau login |
|---|---|---|
| `admin@cb.vn` | admin | `dashboard.html` |
| `account@cb.vn` | account | `dashboard.html` |
| `design@cb.vn` | design | `dashboard.html` |
| `editor@cb.vn` | editor | `dashboard.html` |
| `client@cb.vn` | client | `client-dashboard.html` |

Important nuances:

- design/editor lưu là `role: "design"` và `role: "editor"`. Dùng `design,editor` không dùng `staff` trong filter.
- **Role `content` — backend enable**: để tạo user content THẬT cần chạy `supabase/add-content-role.sql` (idempotent, SAU 3 migration wording): (1) `users_role_check` thêm `content` — KHÔNG có thì trigger `handle_new_auth_user` fail → không tạo được user; (2) policy `orders content read` (content CHỈ SELECT orders); (3) **RPC `update_brief_wording(p_order_id, p_patch jsonb)`** = đường GHI orders DUY NHẤT cho content/client (whitelist cột wording, validate status; KHÔNG cấp UPDATE orders trực tiếp). Frontend: mọi ghi wording (`content-workbench.js persist()`, `client-dashboard.js approveWording`/wording-feedback) đi qua `store.orders.updateWording()` → `rpc`. **Tạo user**: Supabase Auth → Add user + metadata `{name, role:'content'}` + password (UI "Tạo user" trong app là MOCK, không tạo user thật).
- **Role `content` (Phase 1)**: internal role mới (tương lai phụ trách wording brief). Login redirect → `content-workbench.html`. Sidebar có mục **Content Wording** (`data-show-roles="admin,account,content"`) — inject bằng `app.js injectContentNav()` cho mọi trang nội bộ (không sửa từng HTML). CSS: `data-show-roles*="content"` rules + `.role-badge.r--content`/`.role-tag-badge.rt--content`. Content KHÔNG vào được settings/user-management (guard `role!=='admin'`). `content-workbench.html` guard: admin/account/content; client→client-dashboard, design/editor→dashboard. Cần tạo user `content@cb.vn` role `content` trên Supabase. **(2026-06-08) Content = team TÁCH BIỆT, không xem KPI Production**: tạm ẩn nav **Master Dashboard + Task Dashboard + Internal Task Tracker** cho content qua CSS `body[data-user-role="content"] .dash-nav li:has(> a[href="dashboard.html"|"task-dashboard.html"|"production-board.html"])` (3 mục này không có data-show-roles nên ẩn bằng `:has()` — 1 chỗ, mọi trang). Content còn: Content Wording + Calendar + AI Tools + Chatbot. Tổng hợp/nhắc nhở của content nằm ở Content Wording (bảng order cần wording: status/round/deadline/PIC). ⚠ Mới ẩn NAV (CSS) — chưa chặn truy cập URL trực tiếp 3 trang đó; thêm guard redirect khi build workflow riêng cho team Content.
- **Role `lead_content` + Content Team Workspace (2026-06-11)**: team Content có 2 role — `lead_content` (gán PIC, đặt hạn wording, duyệt/trả chỉnh) + `content` (làm wording được gán). Trang theo vai (sidebar nhóm **"Content Team"** 2 sub, 2026-06-11): **lead_content → `content-team.html` "Content Workspace"** · **content → `content-workbench.html` "Content Wording"** — login redirect theo role; nhóm sidebar inject qua `app.js injectContentTeamGroup()` (THAY injectContentNav+injectContentTeamNav cũ; 2 trang content hardcode cùng cấu trúc h6+ul). Status flow MỞ RỘNG `brief_wording_status`: `assigned` (Inbox Lead) → `pic_assigned` → `in_progress` → `submitted_to_lead` → (`lead_revision`↺) → `submitted_to_account` → flow Account/Client giữ nguyên. Cột mới: `wording_lead_note`/`wording_lead_reviewed_at`/`wording_lead_reviewed_by`/`wording_submitted_to_lead_at`. ⚠ **Chạy `supabase/add-content-team.sql`** (role check + status CHECK + cột + RLS lead_content SELECT orders/users + RPC v2 — RPC thêm `wording_deadline` cho Lead đặt hạn). **Notify matrix Content Team**: Account transfer → `lead_content` active (fallback `content` nếu chưa có lead, title khác); Lead gán PIC → PIC (`findUserIdByName`); Content submit → mọi `lead_content`; Lead duyệt → admin+account; Client wording feedback → admin+account+lead_content+content (**link per-role**: content→`content-workbench.html?id=`, còn lại→`content-team.html?id=`; PIC được gán/trả chỉnh cũng nhận link Wording). **Guard 2 chiều**: design/editor/client KHÔNG vào content-team (redirect); role content vào content-team → redirect `content-workbench.html` (GIỮ `?id=`); content/lead_content KHÔNG vào production-board + database-orders + task-dashboard (redirect THEO ROLE: lead→content-team, content→content-workbench). **CSS substring trap**: `[data-show-roles*="content"]` match cả value chứa `lead_content` → KHÔNG dùng data-show-roles cho UI chỉ-Lead (gate bằng JS); rules `body[data-user-role="lead_content"]` riêng trong styles.css (kèm ẩn nav Master/Task Dashboard/Task Tracker như content). **Cache CHUNG `mh-wording-drafts`** giữa content-team + content-workbench (draft/activity share; DB_AUTH của content-team chỉ override khi DB có giá trị — tránh mất data khi cột chưa migrate). **content-workbench = trang LÀM VIỆC của content (2026-06-11, hết legacy)**: WS_CONTENT_EDITABLE thêm `pic_assigned`/`lead_revision`; nút submit đổi **"Gửi Lead Content duyệt"** (`submitToLead` → `submitted_to_lead` + `wording_submitted_to_lead_at` + `notifyLeads()`) — KHÔNG còn đường gửi thẳng Account (hết bypass); LIFE bước 3 = "Lead Review"; account panel ở đây vẫn giữ "Trả Content chỉnh"/"Gửi Client xác nhận" cho `submitted_to_account`. Order drawer (database-orders) có 2 nút: "Mở Content Workspace" + "Mở Content Wording"; WORDING_STATUS_LABEL + reachedMap đã cover status mới. Tạo user lead_content: Supabase Auth → Add user + metadata `{name, role:'lead_content'}` (SAU khi chạy migration, không thì trigger fail).
- **Brief Wording flow (Phase 1→4 DONE)**: bắt buộc Content Wording + Client xác nhận trước Confirm Brief & Push Production. Status `brief_wording_status`: none→assigned→in_progress→submitted_to_account→(account_revision)→sent_to_client→(client_feedback↺)→client_approved/completed. **Account** chuyển wording (`transferToWording`) + duyệt + **gửi Client** (`sendWordingToClient`, nút `#act-send-wording-client` trong Brief Wording Workflow block) → **Client Portal** section "Brief đã được chuẩn hóa" (Xác nhận brief `approve-wording` / Yêu cầu chỉnh brief `wording-feedback` modal `#wording-feedback-modal`) → nếu feedback về **Content Workbench** (`brief_wording_round++`, Content "Chỉnh theo Client"). Gate: `isWordingApproved(o)` (client_approved/completed) khóa Confirm Brief. **Wording rounds TÁCH RIÊNG khỏi production Preview/Final rounds.** Client KHÔNG thấy Content Workbench/Task Tracker/internal note (`wording_account_note`/`wording_production_note` không map sang client adapter). ⚠ chạy 3 migration: `add-brief-wording-fields.sql` (P2) + `add-brief-wording-workspace-fields.sql` (P3) + `add-brief-wording-confirmation.sql` (P4).
- **Role `system_supervisor` ("Giám sát hệ thống") — monitor READ-ONLY cấp hệ thống (2026-06-14)**: redirect `dashboard.html`. **Thấy** Master/Orders/Task Dashboard + Client Orders + Internal Task Tracker + Calendar + Content Workspace + Content Wording + Reports(+export) + AI Tools + Chatbot. **Ẩn** Order Form + User Management + Settings. **MỌI mutation khóa**. Cơ chế: (1) guards mở allow-list (`database-orders.js`/`reports.js`/`content-team.js`/`content-workbench.js` thêm vào list; `calendar.js IS_FULL`; production-board vốn không chặn). (2) read-only sâu bằng cờ `const READONLY = user.role==='system_supervisor'`: `database-orders.js` (kebab chỉ "View Detail" + `applyDrawerReadonly()` disable mọi input + ẩn nút drawer trừ close + `updateStepperState` early-return ẩn Quick Actions/Hủy đơn) · `production-board.js` (`buildStatusActions`→`[]`, `updateStatus` guard, composer textarea/select `disabled` + nút Gửi ẩn; drag/create/edit task đã loại sẵn admin/account). (3) các module content/calendar/reports KHÔNG cần cờ — action tự ẩn vì isLead/isContent/isAccountAdmin/canEdit đều false. **Nav reveal**: app.js inject Content Team group + Calendar có data-show-roles += `system_supervisor`; 3 nav hardcode `admin,account` (Orders Dashboard/Client Orders/Reports) reveal qua CSS `:has` theo href (KHÔNG sửa 16 sidebar; Order Form CỐ Ý không reveal). **CSS** badge `.role-badge.r--system_supervisor`/`.rt--` (slate + dark) + block reveal `[data-show-roles*="system_supervisor"]`. ⚠ Substring-safe: "system_supervisor" KHÔNG chứa admin/account/design/editor/content/client → không kích nhầm rule `*=` role khác. **DB** `add-system-supervisor.sql` (⚠ pending): users_role_check += role + helper `is_system_supervisor()` + SELECT-only RLS orders/tasks/task_comments/users/deliveries/activity_log. **KHÔNG thêm vào `is_staff()`** (is_staff gate cả write → sẽ lộ quyền ghi). roleLabel/ROLES (app.js) + ROLE_LABEL/defaultPermByRole (user-management.js, report_viewer + scope all) + 2 dropdown role đã thêm. Tạo user: Supabase Auth metadata `{name, role:'system_supervisor'}`.
- **Role `lead_media` ("Lead Media") = TRƯỞNG NHÓM PRODUCTION (cập nhật 2026-06-18)**: quyền vận hành **NGANG Account** — Client Orders / Internal Task Tracker / 3 Dashboards / Reports / Order Form. **KHÔNG** Content Team (Workspace+Wording), **KHÔNG** Settings/User Mgmt (admin). Login → **Master Dashboard** (`login.html roleHome`). Cũng dùng Strategy Board (Supervisor Planning). **Cơ chế quyền**: frontend **alias `lead_media`→`account`** trong `production-board.js` + `database-orders.js` (đặt SAU `data-user-role`+chip → mọi check `['admin','account']` pass); `reports.js`+`database-orders.js` allow-list += lead_media; bỏ redirect cũ ở production-board. **CSS** (`data-user-role='lead_media'` giữ thật): reveal nav admin/account-only (Orders Dashboard/Client Orders/Reports/Order Form) theo `:has` + reveal `.dash-section/.kpi/.qa-btn/.dash-card[data-show-roles]` để dashboard không trắng; **KHÔNG** reveal `.dash-nav li` chung → group Content Team vẫn ẩn (data-show-roles không chứa lead_media). **RLS**: `add-media-lead-production.sql` thêm lead_media vào `is_staff()`+`is_admin_or_account()`. Badge teal; roleLabel/ROLES; user-mgmt defaultPermByRole(manager·order_mgmt·all). Trên `lead_tasks` (Strategy Board): vẫn SELECT/UPDATE/DELETE bucket mình + đề xuất (RLS planning riêng). ⚠ chạy `add-supervisor-planning.sql` + `add-media-lead-production.sql` rồi tạo user metadata `{name, role:'lead_media'}`. **CSS substring-safe**: "lead_media" KHÔNG chứa admin/account/design/editor/content/client; nhưng data-show-roles của nav "Kế hoạch" chứa "lead_content"⊃"content" → role content reveal nhầm → có override `:has(a[href="supervisor-planning.html"])` ẩn cho content.
- **Module `supervisor-planning` — quy tắc (2026-06-17)**: TÁCH BIỆT hoàn toàn `tasks`/wording — KHÔNG tạo production task, KHÔNG đụng flow Preview/Final. Ghi qua `MH.store.leadTasks` (Supabase `lead_tasks` / fallback `MH_MOCK_LEAD_TASKS`+localStorage `mh-lead-tasks`). `upsert`: không id → INSERT (DB tự gen uuid); có id → UPSERT onConflict id. `update(id,patch)` cho PATCH (status/field). **`assigned_lead='both'`** = giao đồng thời 2 Lead → cả 2 Lead thấy & cập nhật chung 1 plan (`leadCanSee` = `assigned===lead||assigned==='both'`); notify create gửi cả lead_media+lead_content; RLS lead dùng `IN ('lead_x','both')`. **Đính kèm**: file PDF upload bucket Storage `plan-files` (`{plan_id}/{ts}-{file}`) → cột `attachment_path`/`attachment_name`, tải bằng `MH.store.files.signedUrl`; hoặc link ngoài `attachment_url`. Demo (Supabase tắt) chỉ link (upload throw → toast, bỏ qua). **Tiến độ per-lead**: `lead_status` jsonb {lead_media,lead_content} với states `pending`/`in_progress`/`submitted`/`revision`/`completed`; `status` cột (CHECK 4) = `deriveOverall` (submitted/revision→in_progress, all completed→completed, archived sticky) — KHÔNG set `status` tay trừ archived; badge/board dùng `displayOverall` (Chờ duyệt/Cần chỉnh). Plan `both` = 1 row, 2 nhánh độc lập; Lead chỉ sửa nhánh mình. **Cổng duyệt**: Lead `LEAD_MANUAL`=pending/in_progress + "Nộp duyệt"→`submitted` (hồ sơ `lead_submissions` jsonb per-lane: kind/link/file_path/note/round); Supervisor `approveLane`→`completed` / `rejectLane`→`revision`+review_note. **Lead KHÔNG tự completed.** File nộp dùng chung bucket `plan-files`. **Đề xuất bottom-up**: Lead tạo plan `origin=lead`+`approval=proposed` cho bucket mình (RLS lead INSERT chỉ proposed+bucket mình); `informed_leads` jsonb = Lead được mention nắm thông tin (RLS informed SELECT read-only; `leadCanSee` gồm informed; `canParticipate`=canManage||isResponsible → informed Lead ẩn comment/control). Supervisor `approveProposal`(→approved)/`declineProposal`(→declined). `displayOverall` ưu tiên approval (proposed/declined) trước status; board bỏ qua proposed/declined. **Comment/activity** qua `MH.store.leadTaskComments` (bảng `lead_task_comments`, kind comment/status/system) — `logEvent` ghi status/system, `sendComment` ghi comment + notify chiều còn lại. **Checklist** jsonb trên row (`persistChecklist`→`leadTasks.update`). **Notify type base-safe** (task_assigned/task_status_changed/task_comment) — chạy không cần migration notif. **system_supervisor CỐ Ý có quyền GHI ở module NÀY** (bảng cô lập) — KHÁC với mọi module khác nơi supervisor read-only; đừng nhân rộng quyền ghi supervisor sang module khác.
- Client bị block Internal Dashboard → redirect `client-dashboard.html`.
- `login.html` hỗ trợ `?redirect=<page>` param: sau login sẽ redirect về trang gốc (dùng bởi `order-form.js` để bảo toàn draft).
- Unknown valid email = admin để demo nhanh.

Auth guard pattern (internal pages):

```js
let user;
try { user = JSON.parse(localStorage.getItem('mh-user') || 'null'); } catch (e) { user = null; }
if (!user || !user.role) { location.replace('login.html'); return; }
document.body.setAttribute('data-user', user.email);
document.body.setAttribute('data-user-role', user.role);
```

Auth guard pattern (`request.html` — order form):

```js
const AUTH_USER = (() => { try { return JSON.parse(localStorage.getItem('mh-user') || 'null'); } catch(e) { return null; } })();
// validate() → blocks submit if !AUTH_USER
// doSubmit() → injects requester_id/email/name/department vào order payload
// redirect to login.html?redirect=request.html nếu chưa login + bảo toàn draft
```

---

## 5. Navigation

### Internal Header Layout

```text
[Hamburger] [Logo/Brand]    ←→    [Theme Toggle][Notification Bell][User Profile ▾]
```

- **Theme toggle**: pill switch `(.theme-toggle-switch[data-theme-toggle])`, CSS-driven.
- **User Profile**: `#header-profile-chip` — avatar + name + role badge. Click → dropdown gồm: Hồ sơ cá nhân (→ openProfile modal), Cài đặt (→ `settings.html`), Đăng xuất.
- **No "Tạo Order" button** trong header — đã remove. Button này chỉ còn trong page content.

### Internal Sidebar

```text
Vận hành
├── Master Dashboard
├── Orders Dashboard       admin/account  (was "Order Dashboard")
├── Task Dashboard         (page subtitle: "Internal Work Dashboard")
├── Order Form             admin/account (design/editor blocked)
├── Client Orders          admin/account  (was "Database Orders"; file: database-orders.html)
├── Internal Task Tracker  (was "Task Tracker"; file: production-board.html; page H1: "Internal Task Tracker / Task Tracker · Production Board")
└── Reports                admin/account
    (Delivery Log đã GỠ 2026-06-03 — bàn giao Preview/Final link nằm trong Order drawer của Client Orders)

Hệ thống
├── AI Tools            all internal roles
├── Chatbot             all internal roles
├── User Management     admin
└── Settings            admin

[bottom]
CB Creative Flow
v1.0
```

Sidebar bottom: App version block (thay thế profile card cũ).

### Public Header Layout

```text
[Logo/Brand]    [nav links]    [Theme Toggle][Đăng nhập (red pill)]
```

- Không còn "Gửi yêu cầu" CTA pill trong header public.
- Login button: `.btn-login-pill` — gradient đỏ `#BA110F → #E53935`, `border-radius: 9999px`.

Role visibility trong internal sidebar dùng `data-show-roles`:

```css
body [data-show-roles] { display: none; }
body[data-user-role="admin"] [data-show-roles*="admin"] { display: revert; }
```

---

## 6. Data Conventions

IDs:

- Orders: `MEDIA-YYYY-NNNN`
- Tasks: `TASK-NNNN`
- Deliveries: `DLV-YYYY-NNNN`
- Users: `USR-NNNN`

Status keys:

- Account: `pending`, `checking`, `needinfo`, `confirmed`, `rejected`
- Production: `pending`, `received`, `inprogress`, `review`, `revision`, `feedback_wait`, `feedback_fix`, `ready`, `delivered`, `completed`, `paused`, `cancelled`
- Delivery: `waiting`, `need_rev`, `ready`, `preview`, `client_wait`, `client_rev`, `final`, `rated`, `completed`, `reopened`, `cancelled`

Production progress:

```text
pending 20% → received 30% → inprogress 50% → review 65%
→ revision 75% → feedback_wait 80% → feedback_fix 85%
→ ready 90% → delivered 95% → completed 100%
paused/cancelled = 0%
```

Demo date anchor:

- `database-orders.js`, `production-board.js`, `delivery-log.js`: `new Date('2026-05-13')`
- `reports.js`: `today = new Date('2026-05-13')`

People/departments reused across modules:

- PIC/account names: Duy, Vinh, Linh Chi, Hậu, Mai Phương, Đức Anh.
  - ⚠ `tasks.assigned_to` lưu **tên ngắn** ("Duy") nhưng `mh-user.name` của account là **tên đầy đủ** ("Duy Trần"). `production-board.js isMyTask(assignedTo)` (2026-06-02) match user↔task: bằng nhau HOẶC full name chứa tên PIC theo word-boundary. **Đừng dùng `=== user.name` hay `.split(' ').pop()`** (lấy nhầm chữ cuối → design/editor không thấy task của mình). Task chỉ tạo khi **Push → Production** (assigned_to = order.production_pic).
- Departments: HO Marketing, Academic, Sales, CB Mekong, CB Hưng Phú, CB Cần Thơ, CB Tiên Thủy.

---

## 7. Common UI Patterns

Toast:

```js
window.MH.toast({
  type: 'success' | 'error' | 'info' | 'warning',
  title: 'Title',
  message: 'Body',
  duration: 4000
});
```

Profile modal:

```js
window.MH.openProfile(); // mở profile editor modal
```

Header Profile Chip (DOM):

```html
<div class="header-profile-chip" id="header-profile-chip" tabindex="0">
  <span class="avatar" id="hpc-avatar">MP</span>
  <div class="header-pc-info">
    <span class="header-pc-name" id="hpc-name">Mai Phương</span>
    <span class="role-badge r--admin header-pc-role" id="hpc-role-badge">Admin</span>
  </div>
  <svg class="header-pc-chevron">...</svg>
  <div class="header-profile-menu" id="header-profile-menu">
    <a href="#" class="hpm-item" id="hpm-profile">Hồ sơ cá nhân</a>
    <a href="settings.html" class="hpm-item" data-show-roles="admin">Cài đặt</a>
    <div class="hpm-divider"></div>
    <button class="hpm-item hpm-danger" id="logout-btn">Đăng xuất</button>
  </div>
</div>
```

`app.js` handles: toggle (`is-open` class), populate (`refreshHeaderChip(user)`), Hồ sơ link → `openProfileModal()`.

Drawer pattern:

```html
<div class="drawer-backdrop" id="drawer-backdrop"></div>
<aside class="drawer" id="xxx-drawer" aria-hidden="true">
  <div class="drawer-head"></div>
  <div class="drawer-actions"></div>
  <div class="drawer-body"></div>
</aside>
```

Detail row (drawer body — dùng div/span, không dùng dl/dt/dd vì browser default gây layout vỡ):

```html
<div class="detail-row">
  <span class="detail-dt">Label</span>
  <span class="detail-dd">Value</span>
</div>
```

Tables:

- `.table-card`, `.table-head`, `.table-wrap`, `.data-table`
- `.sortable[data-sort]`, `.pagination`

Badges:

- `.tb-status.s--<key>`
- `.priority-pill.p--normal|urgent|critical`
- `.role-tag-badge.rt--admin|account|staff|client|manager`
- `.user-status.us--active|pending|inactive|suspended|archived`
- `.rating-stars`, `.star-input`

---

## 8. localStorage Keys

| Key | Purpose |
|---|---|
| `mh-theme` | Theme: `light` / `dark` |
| `mh-user` | Current user session |
| `mh-order-draft-v2` | Order Form autosave draft |
| `mh-submitted-orders` | Orders submitted via request.html (demo, max 50) |
| `mh-settings` | Settings panel state |
| `mh-settings-activity` | Settings activity log, last 50 |
| `mh-ai-usage-log` | AI Tools usage log demo, last 50 |
| `mh-ai-saved-outputs` | AI output save demo, last 50 |
| `mh-chatbot-history` | Chatbot message history demo, last 80 |
| `mh-chatbot-feedback` | Chatbot Good/Bad feedback demo, last 50 |
| `mh-extra-tasks` | Tasks tạo mới qua Task Tracker hoặc "Create Task from Order" (cross-page demo, last 100). Mỗi entry có shape giống TASKS[] trong production-board.js, gồm `is_standalone: bool` + optional `order_id`. |
| `mh-lead-tasks` | Kế hoạch nội bộ (Supervisor Planning) khi Supabase tắt — fallback của `MH.store.leadTasks` (shape = bảng `lead_tasks`, gồm `lead_status`/`checklist` jsonb), last 200. |
| `mh-lead-task-comments` | Comment/activity kế hoạch (Supervisor Planning) khi Supabase tắt — fallback `MH.store.leadTaskComments` (shape = `lead_task_comments`), last 500. |

---

## 8b. Dashboard KPI Drilldown

Mỗi KPI card trong `dashboard.html` có `data-card-key` + click handler → redirect tới module tương ứng với `?dl=<card_key>`:

```text
total_orders / new_requests / brief_need_info / completed → database-orders.html
in_production / internal_review / due_soon / overdue / on_time_rate → production-board.html
ready_for_delivery / average_rating / rating_coverage → delivery-log.html
```

Target page reads `?dl=...` → set `state.view` / `state.quick` → render → inject `.drilldown-banner` (label + count + "Xóa filter") trước `.table-card` → smooth-scroll vào table.

Dashboard **Alert Center** dùng cùng cơ chế nhưng kèm `&id=MEDIA-*`: 6 button "Xem" → link tới module + drilldown filter + record ID. Destination module sau khi render thử `find(record matches id)` → `openDrawer` nếu có, toast warning nếu là placeholder demo không tồn tại trong mock data.

## 8d. Order ↔ Task ↔ Delivery relationship

```text
Order  (client/branch submits brief — lives in Database Orders)
  └── Task[]  (internal work item — Task Tracker / Production Board)
        └── Delivery (preview/final — Delivery Log)
```

- Order = client/branch request (`MEDIA-YYYY-NNNN`).
- Task = internal work assigned to Media team (`TASK-NNNN`). One Order → many Tasks.
- Task linkage:
  - `order_id`: nếu task gắn order, ID dạng `MEDIA-*`.
  - `is_standalone: true`: task nội bộ độc lập, không gắn order. Vẫn xuất hiện trong Task Tracker; quick-filter "Standalone".
- Client never sees Tasks; Task Tracker và Task Dashboard chỉ có role admin/account/design/editor.
- Cross-page bridge: tasks tạo mới qua Task Tracker hoặc "Create Task from Order" lưu vào `localStorage['mh-extra-tasks']`. Cả `production-board.js` và `database-orders.js` đều đọc storage này để hiển thị lẫn nhau.
- "Create Task from this Order" trong Database Orders drawer → redirect `production-board.html?createTask=1&order_id=...&project_name=...&task_type=...&priority=...&internal_deadline=...&production_pic=...&content=...` → auto-mở Create Task modal có prefill.

## 8f. Dashboard KPI groups (Modules 3+4)

Cả 3 dashboard (Master / Orders / Task) đều load real-time từ Supabase qua `MH.store`. **Orders Dashboard** chỉ đọc `orders.list()` (NEVER tasks). **Task Dashboard** chỉ đọc `tasks.list()` (NEVER orders). **Master Dashboard** parallel fetch cả 2.

### Orders Dashboard (`order-dashboard.html`) — 13 KPI + 6 breakdowns

- **Order Intake** (5): `total_orders` / `new_requests` (pending) / `checking` / `brief_need_info` (needinfo) / `confirmed` (confirmed && !completed)
- **Production Flow** (3): `in_production` (received/inprogress/revision/feedback_fix) / `ready_for_delivery` (production_status=ready) / `delivered`
- **Feedback & Completion** (5): `waiting_feedback` (delivery_status preview/client_wait/client_rev) / `rated_orders` (satisfaction_score>0) / `average_rating` (no drilldown) / `completed` (production_status=completed) / `cancelled_orders` (account=rejected OR production=cancelled)
- **Breakdowns**: by branch (`data-branch`), type (`data-type`), priority (`data-priority`), account PIC (`data-account-pic`), production status (`data-prod-status`), delivery status (`data-delivery-status`)
- **Drilldown** → `database-orders.html?dl=KEY` via `DRILLDOWN_MAP` 12 keys (matched với `matchesView()` switch trong database-orders.js).

### Task Dashboard (`task-dashboard.html`) — 17 KPI + 6-PIC workload + 2 breakdowns

- **Task Volume** (4): `total_tasks` / `linked_tasks` (order_id && !is_standalone) / `standalone_tasks` (is_standalone OR !order_id) / `new_internal_tasks` (status=pending)
- **Workload** (1 KPI + chart): `unassigned_tasks` (!assigned_to && isOpenStatus) + **Workload by PIC** 6-member bar chart (Duy/Vinh/Linh Chi/Hậu/Đức Anh/Mai Phương, MAX_PER_PIC=8, bar đỏ khi overload)
- **Deadline** (3): `due_today` (diffDays=0) / `due_this_week` (diffDays ∈ [0,7]) / `overdue` (deadline past)
- **Production Status** (6): `status_pending` (=pending) / `in_production` (received/inprogress/revision/feedback_fix) / `internal_review` (=review) / `status_revision` (=revision) / `status_completed` (=completed/delivered) / `status_blocked` (=paused)
- **Performance** (3): `on_time_rate` (= completed / (completed + overdue) * 100) / `completed_this_week` (completed && last_update ≥ NOW-7d) / `avg_completion_time` (avg days created_at → last_update, no drilldown)
- **Drilldown** → `production-board.html?dl=KEY` via `DRILLDOWN_MAP` 15 keys (`state.quick` switch trong production-board.js).

### Master Dashboard (`dashboard.html`) — separated combined view

- Module 5 refine (2026-05-22): Master Dashboard remains combined, but no ambiguous mixed KPI row. Layout is now 4 sections:
  - **Client Orders Overview**: Total Client Orders / Active Client Orders / Need More Info / In Production / Delivered / Waiting Feedback / Completed / Cancelled.
  - **Internal Tasks Overview**: Total Internal Tasks / Active Internal Tasks / Standalone Tasks / Linked Tasks / Due Today / Overdue / Unassigned / Completed This Week.
  - **Alerts**: Client Orders waiting brief info, Client Orders waiting feedback, overdue internal tasks, unassigned internal tasks, high priority tasks/orders.
  - **Team Workload**: tasks by PIC, overdue by PIC, workload pressure threshold MAX_PER_PIC=8.
- Drilldown pattern preserved: visible Module 5 KPI cards use `data-card-key` plus optional `data-drill-key` to route to supported target filters in `database-orders.html` / `production-board.html`.
- **Cleanup 2026-05-22**: 5 legacy hidden blocks REMOVED (~302 lines): old mixed KPI grid + Workflow Health pipeline + Production Status donut + Delivery Funnel + Recent Activity. `loadMasterDashboard()` bỏ 18 dbSetKpi calls dead (legacy keys không còn card target). Loader giờ gọn: `renderModule5Dashboard(O, T)` + Workload by PIC. dashboard.html từ 1138 → 774 lines. Git history giữ rollback.

### Helper functions (inline mỗi dashboard)

- `setKpi(cardKey, val)` — update `.kpi[data-card-key=X] .kpi-value`
- `updateBars(attr, countsMap)` — update bar rows + widths theo max
- `parseDate(s)`, `diffDaysFromNow(d)`, `isOverdueD(d, isCompleted)`, `isCompletedStatus(s)`, `isOpenStatus(s)`

### Why separate Orders vs Task Dashboard?

- Orders = client-facing lifecycle (brief → confirm → produce → deliver → rate)
- Tasks = internal workload (assigned, deadline, on-time rate, who's overloaded)
- Mixing 2 flow tạo confusion. Master Dashboard mới là combined view.

---

## 8e. Internal Task Tracker (Production Board) formalization

- Page H1: `Internal Task Tracker / Task Tracker · Production Board`. Sidebar label: `Internal Task Tracker`. File path vẫn `production-board.html` (no rename, per Module 1 acceptance).
- Page head có button `[+ Giao việc nội bộ]` (gắn `#btn-create-task`). Mở Create Task modal trong cùng page.
- Quick filter chips trên toolbar (`#quick-filter-chips`): Tất cả · Due Today · Due This Week · Overdue · Unassigned · My Tasks · **Standalone Internal**. Lưu trong `state.quickChip`. Khác với summary card `data-quick` (giữ nguyên).
- **Create / Edit Task modal `#task-modal` (Module 2 refactor, 5/2026)**:
  - Modal title default "Giao việc nội bộ mới" (create) / "Sửa công việc nội bộ" (edit).
  - **Radio group "Loại công việc"** (`name="tm-worktype"`) thay cho old checkbox `#tm-standalone`. 2 option:
    - `value="linked"` (default, `#tm-worktype-linked`): Liên kết với Client Order → hiển thị `#tm-order-row` để nhập MEDIA-* code.
    - `value="standalone"` (`#tm-worktype-standalone`): Công việc nội bộ độc lập → ẩn order row, hiện `#tm-standalone-hint` info card "ℹ Công việc này không liên kết với Client Order nào."
  - Save validation: linked option BẮT BUỘC nhập order_id (warning toast nếu trống). Standalone không cần.
  - Prefill từ URL params (`?createTask=1&order_id=...&project_name=...&task_type=...&priority=...&internal_deadline=...&production_pic=...&content=...&standalone=1`) hoặc edit task → set radio đúng theo `is_standalone`.
  - Save mới tạo `TASK-NNNN` (auto-increment dựa trên max TASK ID), push vào TASKS + `mh-extra-tasks` + Supabase `tasks` table.
  - JS helper: `applyWorktypeUI(isStandalone)`, `isWorktypeStandalone()`.
- **Edit Task permission (2026-06-04)**: nút `[Sửa công việc]` (sửa metadata) **CHỈ Admin/Account** — `attachDrawerEditButton` gọi khi `['admin','account'].includes(role)` (BỎ `|| isMyTask`). Bảo vệ handler: `openTaskModal(prefill, editId)` chặn nếu `editId && role∉admin/account` (toast "Bạn không có quyền…"); `save-meta` handler cũng guard. P.I.C (design/editor) drawer: task info read-only, **không** field PIC/deadline/priority, KHÔNG nút Sửa — nhưng **vẫn sửa được Files & Links** (`canEditLinks = role≠client && (isMyTask || admin/account)`) + status flow. Status: P.I.C chỉ `pending→received→inprogress→review` + `revision/feedback_fix→review`; `canTransition`/`updateStatus` chặn P.I.C sang `ready/completed/cancelled/paused`. Admin/Account full control (đổi PIC/deadline/priority/type/order, pause/cancel/close).
- **Drawer "Loại công việc" block** (rename từ "Linked Order"):
  - Nếu task có `order_id && !is_standalone` → badge `.worktype-badge--linked` (navy "Linked to Client Order") + linked-order-card với order_id + project + "Mở Order" button → `database-orders.html?id=<order_id>`.
  - Nếu `is_standalone || !order_id` → badge `.worktype-badge--standalone` (red "Standalone Internal Task") + giải thích "Công việc nội bộ độc lập — KHÔNG gắn Client Order...".

## 8c. Comment system (Production Board)

Task drawer comment thread hỗ trợ @mention + Reply:

- **@mention**: gõ `@` trong textarea → dropdown filter realtime 6 team members + current user. Keyboard nav (↑/↓/Enter/Tab/Esc). Tên có chip `.mention` style khi render. `parseMentions(text)` → array of names. Names có dấu cách (Mai Phương, Linh Chi, Đức Anh) chỉ pick từ dropdown — không gõ tay được.
- **Reply**: button "Reply" mỗi comment (hidden cho đến khi hover) → set `replyingToId` → banner trên composer hiển thị "@Author — snippet" + nút × cancel. Submit → comment có `reply_to` (parent id), render indented (`.is-reply` class).
- **Comment object shape** (`task.comments[]`): `{ id, author, text, time, type, mentions[], reply_to, reply_to_author }`. Existing comments backfill id qua `ensureCommentIds` khi mở drawer.
- **Reply indicator**: comment con có badge "Reply tới @Author" — click scroll smooth tới parent + outline glow 1.4s.
- Data lưu in-memory trong `TASKS[].comments`. Reload mất.

---

## 9. Known Decisions

- Brand colors are CB Centres navy `#191970` and red `#BA110F`; do not revert to indigo/violet.
- Original logo was huge; current `assets/logo.png` is resized for web.
- Dark mode needs explicit fallback for gradient text when text disappears.
- User rejected count-up animation for trust strip; only use count-up where explicitly requested.
- Validation rules to preserve:
  - User Management: no duplicate email, do not deactivate the only admin.
  - Database Orders: push to Production requires confirmed brief + PIC + internal deadline + deliverable.
  - Production Board: PIC cannot directly complete/ready without required link.
  - Delivery Log: Send Final requires checklist 8/8 and final link.
  - Client Portal Preview feedback: chỉ hiện thẻ/nút "Gửi feedback" + "Duyệt Preview" khi `status==='feedback_wait' && preview_link && (feedback_status rỗng || 'waiting_feedback')` (helper `shouldShowPreviewAction`). Sau khi client gửi feedback (`feedback_status='feedback_received'`) / revision (`revision_in_progress`) / approved / final_sent / exceeded_limit → ẩn action, chỉ còn read-only. Tiêu đề theo vòng dùng `getPreviewRound` = `min(revision_round+1, revision_limit)`. Account gửi Preview mới sẽ set `feedback_status='waiting_feedback'` để mở lại vòng kế tiếp.
  - **Feedback Vòng 3 (vòng cuối) → Final**: Vòng 3 VẪN thuộc order hiện tại — KHÔNG tạo task/order mới ngay. Luồng đúng: Client gửi FB Vòng 3 (modal "Feedback Vòng 3 — Final Check") → Account drawer hiện alert + "Gửi feedback vòng 3 cho PIC" (comment→task, task→`feedback_fix`, order→`revision_in_progress`, KHÔNG còn "Tạo task mới từ feedback này") → PIC cập nhật `final_link` (Task Tracker chặn chuyển `review` nếu thiếu Final Link khi `revision_round≥3`) → Account gửi Final (Order Drawer; `feedback_status='final_sent'`, `delivery_status='final'`, `production_status='delivered'`) → Client đánh giá HOẶC bấm "Tạo Order mới từ yêu cầu này" → `request.html?mode=revision&ref_order=ID`. **Admin/Account KHÔNG tạo order thay Client; Client tự tạo.** Order revision lưu `parent_order_id`/`order_origin='revision_over_limit'` (cột tùy chọn `supabase/add-revision-link.sql`; luôn nhúng marker vào `content_brief`) và đi luồng order chuẩn. Sau Final KHÔNG cho gửi feedback thường (final_sent bị `shouldShowPreviewAction` loại + guard submit).
  - Order Drawer (Client Orders) top area là **contextual theo lifecycle**: nút Kiểm tra brief / Yêu cầu bổ sung / Xác nhận brief chỉ hiện ở giai đoạn brief; Push → Production chỉ hiện khi `confirmed && chưa push`; đã push / có `preview_link` / `final_delivery_link` / completed/rated → **ẩn hết** (không chỉ disable) — `updateStepperState` dùng show/hide. Thông tin task hiển thị qua **Linked Task card** (`buildLinkedTaskCard`) trong body, KHÔNG dùng dòng `wf-hint` "Đã push…" nữa (đã bỏ). Thứ tự body: Timeline → Next Action → Linked Task card → summary grid. "Hủy đơn" tách ở `.wf-actions-danger`. Block "Gửi Preview" ẩn khi đã có Final.
  - **Phase 2 — cổng Content Wording (bắt buộc)**: order phải qua Content Wording trước Confirm Brief. Account: Kiểm tra brief → (Yêu cầu bổ sung HOẶC **Chuyển Content Wording** `act-wording` → `account_status='wording'`, `brief_wording_status='assigned'`). Helper `isWordingApproved(o)` = `brief_wording_status ∈ {client_approved, completed}`. Gates: `updateStatus` chặn `confirmed` nếu chưa approved; `updateStepperState` disable Confirm tới khi approved; `pushToProduction`/`buildPushCheck` yêu cầu approved. Block "Brief Wording Workflow" 6 bước trong drawer body. Cột `brief_wording_status/round/pic`, `wording_approved_at/last_updated_at` (migration `supabase/add-brief-wording-fields.sql` — **bắt buộc chạy** vì `account_status='wording'` cần mở rộng CHECK; có grandfather order cũ → 'completed'). Phase 2 CHƯA có Content drawer / Client confirm UI → wording chỉ approved qua DB (simulate) tới Phase sau. Field thiếu = 'none'.
  - **Phase 3 — Content Workbench** (`content-workbench.html` + `assets/content-workbench.js`): trang + Content Wording Drawer thật cho role Content chuẩn hóa brief. Load `orders.list()` lọc `brief_wording_status ∈ engaged` + overlay localStorage cache `mh-wording-drafts` (draft persist khi reload kể cả chưa chạy migration workspace). Workspace fields `wording_brief/objective/core_message/required_info/tone_style/cta/production_note` + checklist 7 mục (`wording_content_checklist` JSON) + links + `wording_account_note/submitted_by/submitted_at`. Status flow: assigned→in_progress→submitted_to_account→(account_revision↺)→sent_to_client (UI client Phase 4). Account read-only workspace, Content/Admin edit. Order Drawer (`database-orders.js buildWordingWorkflow`) có summary + nút "Mở Content Wording" → `content-workbench.html?id=`. Migration optional `supabase/add-brief-wording-workspace-fields.sql` (persist Supabase; nếu không có thì cache localStorage).
  - Settings: do not delete statuses currently used by data.
- `<dl>/<dt>/<dd>` trong drawer body gây layout vỡ (browser default CSS); thay bằng `<div>/<span class="detail-dt/detail-dd">`.
- Profile chip đã được move từ sidebar bottom lên header right. Sidebar bottom dùng App version block đơn giản.
- `#logout-btn` ID nằm trong `.header-profile-menu` và được xử lý bởi từng page JS riêng.
- `app.js` handle toggle cho `#header-profile-chip`; page JS files không cần tự xử lý toggle nữa.
- **Business model — 2 operational flows (5/2026 clarification + Module 1 UI naming)**: Hệ thống có 2 luồng vận hành SEPARATE:
  - **(1) Client Orders**: requests từ client/branch/department submit qua `request.html`. Lifecycle đầy đủ: receive brief → check brief → request more info → confirm brief → production → delivery → feedback/rating → completed/cancelled. Mô hình data: `public.orders` table, mỗi row có `requester_id` link `auth.users`. Quản lý qua **Client Orders** (`database-orders.html` — sidebar label "Client Orders") + **Orders Dashboard** (`order-dashboard.html`).
  - **(2) Internal Tasks**: work items nội bộ Leader/Admin/Account assign trực tiếp cho Media team. Có thể link với 1 Client Order (`order_id`) HOẶC standalone (`is_standalone=true`, no order_id) cho admin/internal workstream. Mô hình data: `public.tasks` table. Quản lý qua **Internal Task Tracker** (`production-board.html` — sidebar label "Internal Task Tracker") + **Task Dashboard** / Internal Work Dashboard (`task-dashboard.html`). **Client KHÔNG được thấy data này** — RLS chặn DB level + role guard client-side redirect role=client → `client-dashboard.html`.
  - **Dashboard mapping (đã wire 21/5)**: Master Dashboard = combined orders + tasks; Orders Dashboard = ONLY orders; Task Dashboard = ONLY tasks.
  - **UI label changes (Module 1, 5/2026)**: KHÔNG rename files/routes. Chỉ update text trong sidebar `<span>`, page `<h1>`, `<title>`, meta description, và JS user-visible strings (toast/chatbot reply/action buttons). File paths giữ nguyên: `database-orders.html`, `production-board.html`, `order-dashboard.html`, `task-dashboard.html`.
- **Dashboard data wiring pattern (5/2026)**: 3 dashboard pages (`dashboard.html`/`order-dashboard.html`/`task-dashboard.html`) inline async `loadXDashboard()` IIFE: (1) await `MH.supabaseReady` (2) parallel fetch `MH.store.orders.list()` + `MH.store.tasks.list()` (3) compute KPI + counts (4) update DOM qua `data-card-key` / `data-pipe` / `data-pic` / `data-branch` / `data-type` / `data-link` attributes. Khi Supabase off → console.warn + UI giữ 0 (no breakage). Helper: `dbIsOverdue(deadline, isCompleted)`, `dbIsDueSoon(deadline, isCompleted)` (48h window), `dbIsCompleted(status)` (status === 'completed' || 'delivered'). Pattern này KHÔNG động database-orders.js/production-board.js (đã có write-through), chỉ wire read-only dashboards.
- **AI Voice TTS engine evaluation (5/2026)** — Supertonic giữ làm production target chính sau khi đánh giá 3 alternative đều fail web compatibility:
  - **VietCloneVoice** (github.com/pvlong19911-cmyk/VietCloneVoice): Windows `.exe` binary, không có WASM/JS port → cần Windows server backend. Loại.
  - **OmniVoice** (github.com/k2-fsa/OmniVoice): PyTorch model, 600+ language support nhưng không có ONNX/WASM export, cần GPU + Python backend. Loại.
  - **sherpa-onnx** (github.com/k2-fsa/sherpa-onnx) + Piper VN models: WASM build có thể chạy on-device thật, có 3 Vietnamese Piper model (`vais1000-medium` 63MB, `25hours_single-low` 20MB, `vivos-x_low` 10MB). NHƯNG yêu cầu build emscripten + cmake local + bundle 75-80MB first load + phá nguyên tắc zero-build. Defer làm Phase 3 task riêng.
  - → Kết luận: giữ Supertonic spec (10 preset M1-M5/F1-F5, 16 ngôn ngữ gồm `vi`, expression tags). Demo runtime Web Speech API tiếp tục là placeholder. Khi nào sẵn sàng build pipeline thật → swap.
- **Web Speech API Vietnamese voice limitation**: `window.speechSynthesis` chỉ có giọng Việt nếu OS đã cài. Windows 10/11 KHÔNG ship vi-VN voices default — cần `Settings → Time & Language → Speech → Add voices → Vietnamese`. Edge browser có thể truy cập Microsoft cloud neural voices (`vi-VN-HoaiMyNeural`, `vi-VN-NamMinhNeural`) khi online. Chrome/Firefox phụ thuộc OS voice. Khi không có vi voice → `getVoices().filter(v.lang.startsWith('vi'))` rỗng → code fallback sang voice tiếng Anh đầu tiên → đọc Tiếng Việt sai phonetic. Diagnostic snippet: `window.speechSynthesis.getVoices().filter(v => v.lang.toLowerCase().startsWith('vi'))`.

---

## 10. Completed MVP & Production Work

AI Tools:

- `ai-tools.html` + `assets/ai-tools.js` built from spec 09.
- 13 mini apps, category tabs/search, role permission, dynamic forms, CB brand preset, mock generation, copy/export/save demo and usage log.
- **AI Voice (Supertonic)** — category `voice`. Engine: github.com/supertone-inc/supertonic (ONNX, ~99M params, 44.1kHz). Demo runtime: Web Speech API (`window.speechSynthesis`). Preset M1-M5/F1-F5, 16 ngôn ngữ, expression tags. Voice Player UI có waveform animation + Play/Pause/Stop + Export SSML. Production handoff: SSML preserve expression tags để swap backend giữ format.

Chatbot:

- `chatbot.html` + `assets/chatbot.js` built from spec 10.
- Dedicated page: suggested prompts by role, context panel, chat thread, safe actions, feedback, history clear.
- Floating widget injected on internal/public pages khi có user session.

Client Portal:

- `client-dashboard.html` + `assets/client-dashboard.js` built from spec 11.
- Gồm: greeting, order status cards, order list, order detail drawer, notification panel, profile.
- Auth guard: chỉ `role === 'client'`; admin/staff bị redirect sang `dashboard.html`.
- Order Form (`request.html`) có auth guard: block submit nếu chưa login, auto-fill requester info, lock email, inject identity vào payload, lưu vào `mh-submitted-orders`, preserve draft trước khi redirect đăng nhập.

Nice-to-have production work:

- Replace inline mock data with backend APIs.
- Implement real auth and password validation.
- Implement real upload/storage.
- Replace simulated notification/test connection flows.
- Replace `window.print()` PDF export if real server/client export is needed.

---

## 11. `check_update` Protocol

When user types `check_update`:

1. Compare actual root/assets files with README and this file.
2. Check `STATUS.md` file sizes and module statuses.
3. Scan `localStorage` keys in JS and update section 8 if needed.
4. Confirm whether new modules/files exist that aren't documented.
5. Update `Last updated` in `_hot.md` and `STATUS.md`.
6. Report the sync summary in 1-2 lines.

---

## 12. `sync_task` Protocol

When user types `sync_task`:

1. Update `STATUS.md`: Completed Modules description (nếu thay đổi feature), File Inventory (KB), Changelog (thêm entry ngày hôm nay).
2. Update `_hot.md`: bất kỳ section nào phản ánh thay đổi về convention, token, role, file map, known decisions.
3. Update `README.md`: nếu có file mới, role mới, hoặc thay đổi về deploy/stack.
4. Cập nhật `Last updated` trong cả 3 file.
5. Báo tóm tắt 1–2 dòng: file nào đã cập nhật và nội dung thay đổi chính.

---

*End of _hot.md*
